/target
/.claude/settings.json
/.claude/settings.local.json
fjord.log
fjord.log.old
fjord.log.[0-9]*
/FLADDER_REFERENCE.md
/HTPC logs
# ^ no trailing slash: this is a symlink to a live NFS log directory, not a
# real directory — a trailing-slash pattern only matches an actual directory
# and silently stops ignoring the entry the moment it becomes a symlink.
# local dev-only build speedup (mold linker + sccache) — machine-specific
# tool paths, not something every clone/contributor is expected to have
/.cargo/

# makepkg artifacts
/fjord/
/src/
/pkg/
*.pkg.tar.*
# same, for `makepkg -si` run inside fjord-bin/ (2026-08-17)
/fjord-bin/src/
/fjord-bin/pkg/
/fjord-bin/fjord-x86_64.tar.gz

# editor-specific, not something every clone/contributor needs
/Fjord.code-workspace