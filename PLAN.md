# Fjord — Development Plan

## Goal

A native Jellyfin frontend for Linux built with Rust and Slint. Uses the mpv render API so mpv renders directly into an OpenGL FBO, enabling `report_swap()` for vsync feedback — the approach that avoids choppy playback on NVIDIA legacy Wayland drivers.

---

## Completed

| Phase | Summary |
|-------|---------|
| 1 — Foundation | Slint window, libmpv link, logging |
| 2 — Player | mpv render API, double-buffer FBO, vsync via `report_swap()`, audio passthrough, hwdec, playback reporting |
| 3 — Jellyfin API | Auth, library browse, continue watching / next up / recently added, direct-play URL, session persistence, auto-login |
| 4 — UI | Login, browse, home/movies/TV dashboards, posters, three playback modes, player controls overlay, settings screen, detail page, resume, seek bar |
| 5 — HTPC Polish | Resume freshness, server-side search, lazy library grid, full keyboard nav, context menu (`C` key + right-click), card badges, settings two-pane layout, Rust key handler with configurable bindings, disk caches (home/movies/series), `item-type` routing, canonical user state store |
| 6 — Packaging | PKGBUILD, desktop file, SVG icon, per-machine DeviceId |
| 7 — NVIDIA legacy fix | NVDEC stride diagnosis, `vf=auto` fix, expanded stats overlay |
| 8 — Code organisation | Split `main.rs`/`main.slint` into focused modules, `global AppState` singleton |
| 9 — Bug fixes & polish | Crash on background-play replacement, stop report reliability, screensaver inhibition, Up Next banner, volume overlay, intro-skip race fix, mouse hover on cards/browse, browse search mouse focus, subtitle track labels, subtitle language prefs, "Ends at" clock, settings hover, random sidebar icon, transparent SVG icons, mark-played visual update in dashboards/library |
| 10 — Code review CR1 (2026-06-18) | CR-1–10: stale intro/credits tasks, Up Next short-clip guard, report ordering, pause desync, semaphore bypass, auto-login timeout, context-menu stale state, missing SeriesId, NW timer stamp, countdown TOCTOU. CL-1–6: reset_playback_ui helper, cache_path helper, generic load/save_cache, context-menu state helper, fetch_image_cached, dead stats branch. UI-1–6: episode right-click, browse right-click, TrackPanel extract, dbl-click fullscreen, "Series →" button, seek-drag throttle + commit. |
| 11 — Code review CR3 (2026-06-20) | CR3-1–9: hidden VLH activation, stale dropdown flag, SPDIF warning with all-off formats, seek-dragging stuck on Wayland, deser_deinterlace null crash, language list duplication, header stale, default_true dedup, CLAUDE.md table errors. |
| 12 — Code review CR4 (2026-06-20) | CR4-1–10: Player::new error cleanup, JoinSet panic flush in all poster loaders, settings scroll for all sections, Up Next countdown off-by-one, movies semaphore, auto-advance window guard, mid-session 401 redirect, dropdown model dedup, dead VLH up-nav guard, .expect() in library crate. |
| 13 — EAC3 passthrough diagnosis (2026-06-20) | Root cause (#39): tsched software timer caused PipeWire RT thread to miss 21.3 ms deadline at 192 kHz IEC61937 rates under GPU load. Fix: `api.alsa.disable-tsched=true` (hardware IRQ wakeups) + `suspend-timeout-seconds=2`. Now a Settings toggle. Do not use `api.alsa.headroom` — shifts audio timeline, causes frame drops under `display-vdrop`. |
| 14 — Settings: SPDIF per-format toggles, HDR passthrough row, virtual rows (2026-06-21) | Per-format SPDIF toggles (AC3/EAC3/DTS/DTS-HD/TrueHD) replace single passthrough switch. Tone-mapping row hidden when HDR passthrough on. Video-latency-hacks row hidden unless display-resample active. Cross-section passthrough+display-resample conflict warning. |
| 15 — Audio output device selector (2026-06-21) | Dropdown in Settings → Audio populated from `mpv --audio-device=help`. Device stored in `Config.audio_device`; applied to mpv at playback start. Content-driven popup width; keyboard nav fixed. |
| 16 — PipeWire IRQ scheduling toggle (2026-06-21) | Settings → Audio toggle (visible when SPDIF on + PipeWire/auto device). Writes/deletes `~/.config/wireplumber/wireplumber.conf.d/fjord-alsa-irq.conf` and restarts WirePlumber on change. Config persists after exit; syncs down to false on startup if file missing. |
| 17 — Intro Skipper API v2 + generalized skip segments (2026-06-21) | Migrated from two old endpoints to single `GET /Episode/{id}/Timestamps` returning all 5 segment types. `EpisodeTimestamps` model with `Segment { start, end }` for Introduction, Credits, Recap, Preview, Commercial. Single generic skip overlay replaces `show-skip-intro`. Timer checks segments in priority order (Intro → Recap → Preview → Commercial). Up Next banner: `next-ep-banner-focused` (0=Play Now, 1=Skip); Left/Right toggle, Enter activates. |
| 18 — Per-segment skip modes + configurable timers (2026-06-21) | Each of Intro/Recap/Preview/Commercial has 4 modes: `always-skip`, `ask`, `ask-timed` (auto-skips on countdown expiry), `never-skip`. Credits has 3 modes. All 10 new `Config` fields persisted in JSON. Settings → Player section extended with rows 4–13 + INTRO SKIPPER / CREDITS section headers. `VideoState` extended with `skip_segment_handled`, `skip_timed_shown_at`, `skip_timed_prompt_secs`. |
| 19 — Movie detail enrichment: cast, collection, similar (2026-06-21) | Director, writer, tagline, studio; `BackdropHero`/`PosterBlock`/`MetaLine` atoms extracted to `widgets.slint`. `CastRow` atom with async portrait fetch. `fetch_movie_collections` background task builds BoxSet membership map; collection `SectionRow`. `get_similar_items` "More Like This" `SectionRow`. |
| 20 — CR5: post-enrichment bug fixes (2026-06-21) | `fetch_movie_collections` now spawned in auto-login path. Portrait index mismatch fixed. `SectionRow.item-play` passes `item_type`. CastRow focus ring visibility fix. Back from detail resets stale collection/similar models. BoxSet items sorted by ProductionYear. |
| 21 — Detail page keyboard nav + stop-returns-to-detail (2026-06-21) | `open_detail` calls `invoke_grab_keyboard_focus()`. `VideoState.from_detail` flag restores detail page on stop/natural-end. `on_minimize_player` clears flags. `main.slint` hides DetailPage when `is_playing`. |
| 22 — Series season episode cache + race fix (2026-06-22) | Season episodes cached in `FjordState.series_episode_cache`; cleared on series switch. `series_season_generation` counter prevents stale async results from rapid tab navigation. |
| 23 — Series detail UX polish (2026-06-22) | "✓ Watched" button on series detail. `PosterBlock` extended with played/resume/unplayed badges. Season row focus indicator (accent bottom border). C key on Next Up card. Default focus on episode row; Next Up steals focus when data arrives. |
| 24 — Back button + series/detail header keyboard nav (2026-06-22) | `series-focused-btn` (-1=not in header, 0=Back, 1=♥, 2=✓). Detail `detail-focused-btn = -1` = Back. Season `season-focused-back`. All Back buttons gain `kbd-focused` ring. |
| 25 — Crash fix: series screen "Recursion detected" (2026-06-23) | `kb-x` in season tabs replaced `self.width` with `root.width` to break layout cache re-entrancy cycle. |
| 26 — UI polish: backdrop header, icon circle buttons, ends-at, load-then-show (2026-06-23) | "Ends HH:MM" below action buttons. `IconCircleButton` component (38 px circle). Backdrop fills header block height. `open_detail`/`open_series_screen` defer show until `spawn_main` completes; loading overlay with spinner + progress bar. |
| 27 — UI polish: icon centering, spinner size, portrait preload, progress bar (2026-06-23) | `IconCircleButton` text centred (explicit width/height), font-size 20 px. Spinner dots 14 px. Cast portraits fetched before page shown (no trickle-in). `app-loading-progress` property; 240 px animated progress bar in loading overlay. |
| 28 — Person detail screen (2026-06-23) | Enter on any cast member opens PersonScreen: portrait + bio + filmography SectionRow. `AppMode::Person` (priority above Detail). `get_person_filmography` API endpoint. `CastRow.item-selected` callback wired from detail/series/season screens. Mouse click on cast card also opens person. `close-person` Back button + keyboard Back. Loading overlay (spinner + progress bar) matching deferred show pattern of detail/series screens. |
| 29 — Player: minimal keyboard-pause bar (2026-06-23) | Space-to-pause no longer reveals the full controls bar. Instead shows a slim 52 px minimal bar (seek progress + current/total time + "Ends at") via `pause-bar-visible` flag. Space-to-resume immediately hides both the minimal bar and the full controls (even if the full bar was open from mouse). Mouse click resume also clears `pause-bar-visible`. |
| 30 — Player: seek accumulation OSD (2026-06-23) | Keyboard Left/Right accumulate into a debounced seek instead of seeking immediately. OSD shows direction + total delta + target time ("▶▶ +20s → 1:23:45"). Seek executes ~480 ms after the last key press. Rapid presses add up. Mouse button seeks remain immediate. |
| 31 — Mini-player bar redesign (2026-06-24) | Replaced sidebar "Now Playing" card with `MiniPlayerBar`: full-width bar docked at top, window-aware (all screens offset by `bar-h`). Mode 3 (!video-behind-ui): 108px with live thumbnail + title + buttons. Mode 2 (video-behind-ui): 56px compact bar (no thumbnail, video fills window) with NOW PLAYING label + title + FjordButton Resume/Stop. Video-behind-UI uses dual video layers in main.slint: layer 1 (z=below AppShell) shows video through transparent screen roots on dashboard/library; layer 2 (z=above AppShell, only when overlay open) prevents library cards from ghosting through detail/series/season/person screens. Dim overlay #00000044 (~27%). `float-card-focused` (-1/0/1); `focus_bar_on_up` called at end of every mode arm — Up returns `false` from all screens when at topmost position (Back button in detail/season, header in person, no-prev-section in dashboard, search header in library) so the bar is reachable via Up from any screen. |
| 32 — Code review CR6 (2026-06-24) | 13 issues from full-codebase review. CR6-1: sign-out now clears episode/collection caches, `movies_fetched`, and all overlay AppState flags. CR6-2+12: consolidated XDG path helpers (`xdg_config_base`/`xdg_cache_base`) in `config.rs`; `$HOME` unset logs a `tracing::error!` instead of silently using a relative path. CR6-3: removed `next_ep_pending = None` from countdown task's `!still_playing` branch — natural-end path owns that field exclusively (race nearly guaranteed with fallback 30s trigger + 30s countdown). CR6-4: `get_all_movies` and `get_all_series` now paginate via shared `get_all_paged()` helper (parallel 1000-item pages); Jellyfin's `MaxPageSize` can no longer silently truncate large libraries. CR6-5: season screen deferred to match detail/series — portraits fetched in parallel before page is shown (no trickle-in). CR6-6: `spawn_collection` retry loop bails early when detail page moves on, releasing `Arc<JellyfinClient>` promptly. CR6-7: `person.rs` Left/Right return `false` in header row (were dead keys). CR6-8: removed duplicate `HomeData.recently_added` field (clone of `recently_added_tv`). CR6-9: `invoke_from_event_loop().ok()` → `let _ =`. CR6-10: `on_resume_player` resets `float_card_focused = -1`. CR6-11+13: extracted `push_decoded_section` and `push_decoded_series` helpers, eliminating ~130 lines of duplicated decode+push logic in `poster.rs`. |
| 33 — Code review CR7 (2026-06-24) | 15 findings from full-codebase review (Phases 21–32). CR7-1: always-skip auto-advance fallback when EOF beats next-up fetch (captures `playing_series_id` pre-teardown, spawns fresh `get_next_up_for_series` if `next_ep_pending` is None). CR7-4: Next Up episode `is_favorite` hardcoded false — now reads `user_data.is_favorite`. CR7-5: `context_play_from_start` used `next.series_id` (Jellyfin can omit it) instead of known `id`. CR7-6: TOCTOU between generation check and `next_ep_pending` write merged into single lock scope. CR7-7: context menu `focused` not reset when item marked played — Enter would invoke Resume on fully-played item. CR7-8/9: Left from ♥ (btn=1) in season and series header did nothing (`> 1` → `>= 1`). CR7-10: `update_series_unplayed_count` now also updates `AppState.series_unplayed_count` when the series screen is open. CR7-11/12: loading-progress 50% invokes in season and person screens now guarded by stale season-ID / person-ID check. CR7-13: `spawn_series_poster_loading` now deduplicates IDs before spawning (prevents premature push on duplicate server entries). CR7-14: sign-out clears `last_nw_mov_refresh` / `last_nw_tv_refresh`. CR7-15: CLAUDE.md updated — AppMode variant count 8→10, added `person::handle_key` + `season::handle_key` to dispatch table, ResumePlayer exclusion list updated. |
| 34 — Sort, filter, and alphabet scrubber in library grid (2026-06-25) | Client-side sort (Name A-Z/Z-A, Year↓/↑, Random via LCG pseudo-shuffle) and Unwatched/Favorites filters applied to cached `all_movies`/`all_series`. Sort persisted per library type in `Config.library_movies_sort`/`library_series_sort`. `refresh_library_display` central function (browse.rs) rebuilds `library-display` and `library-alpha-offsets` (27-element vec, A-Z+#, each -1 when letter has no items). Sort bar UI (40 px strip) with 5 sort pills + 2 filter toggles. Alphabet scrubber: right-edge 22 px strip, visible only when sort=A-Z and no query; click or A-Z key jumps grid. Full arrow-key nav chain: grid → search → sort bar → Back button → mini-player (each layer reachable via Up). Sort pills auto-apply on Left/Right navigation (no Enter needed); filter toggles (Unwatched/Favorites) still require Enter and keep the bar focused. Focus indicator: 2 px border (accent when inactive, white ring when on the active pill) + surface-overlay background so cursor is always visible. Down from sort bar moves to search field (not grid). `library-back-focused` state: Back button gets kbd-focused ring; Enter/Esc closes library; Down returns to sort bar. Loading overlay and LoadingSpinner improved: 20 px dots (was 14 px), 260 ms cadence, 75% dim (was 53%), 6 px progress bar (was 4 px, 320 px wide). |
| 35 — Error toast notifications (2026-06-25) | `toast-message: string` + `toast-visible: bool` added to `AppState`. `ToastNotification` component in `widgets.slint`: bottom-center pill, dark semi-transparent background, 4 px red left accent stripe, word-wrap text. Positioned and sized by caller (main.slint). Z-order above ContextMenu (last element in MainWindow). `show_toast(ww, msg)` helper in `main.rs`: any-thread safe via `invoke_from_event_loop`. Auto-dismiss: local `_toast-vis` mirror property + `changed _toast-vis` fires `toast-timer` (4 s Slint Timer, self-stops after trigger). Wired into: `start_playback` Player::new failure ("Couldn't start playback — check your server connection"), `context-mark-played` failure ("Couldn't update watch status"), `context-toggle-fav` failure ("Couldn't update favourite"). |
| 37 — Chapter navigation (2026-06-25) | `Player::get_chapters()` reads `chapter-list/{N}/time` + `chapter-list/{N}/title` after 2 s. `Player::chapter_step(±1)` uses `add chapter`. `VideoState.chapters: Vec<(f64,String)>` (retries up to 30 ticks if count=0). **Seek bar tick marks**: `AppState.chapter-marks: [float]` (normalised 0–1); rendered as 2 px semi-transparent white rectangles inside `seek-track`. **Keys**: `,` = prev chapter, `.` = next chapter (`NextChapter`/`PrevChapter` actions in player map; excluded from shows_controls). **Chapter OSD**: `chapter-osd-text` + `chapter-osd-visible` on AppState; 36 px top-left pill shows "▸ Chapter Name" for ~2 s (`chapter_osd_ticks` countdown in 16 ms timer). OSD name computed immediately from `vs.chapters` + current position without waiting for mpv event. |
| 38 — Sub/audio delay adjustment (2026-06-25) | `Player::adjust_sub_delay(ms)` / `adjust_audio_delay(ms)` call `add sub-delay`/`add audio-delay` and return the new value. **Keys**: `z`/`Z` nudge sub-delay ±100 ms, `x`/`X` nudge audio-delay ±100 ms (matches mpv defaults; remappable; 4 new `Action` variants). **Delay OSD**: `delay-osd-text` + `delay-osd-visible` on AppState; `delay_osd_ticks` countdown in 16 ms timer (~2 s); pill at y:68 px (below chapter OSD at y:24 px to avoid overlap). `fmt_delay_ms(label, secs)` helper in `controls.rs`. Reset cleared on `reset_playback_ui`; no persistence (mpv state resets with each new Player). |
| 39 — Ratings and genres on detail/series pages (2026-06-25) | `detail-rating-label: string` ("★ 7.4") and `detail-genres: string` ("Drama, Crime") added to `AppState`; same for series (`series-rating-label`, `series-genres`). Populated in `detail.rs` and `series.rs` `spawn_main` from `MediaItem.community_rating` + `genres`. `MetaLine` widget renders rating in gold (`#f5c518`) after the year/runtime chip. Genres rendered as plain text line below MetaLine in both `detail.slint` and `series.slint`. |
| 40 — Collections library screen (2026-06-25) | New sidebar nav order: Home(0), TV Shows(1), Movies(2), Collections(3), Music/placeholder(4), Browse All(5). Nav references updated across `layout.slint`, `main.slint`, `app_state.slint`, `browse.rs`, `keys.rs`, `home.rs`. `AppMode::Collection` added between Series and Player. `library-has-filters: bool` property (false for nav=3) hides Unwatched/Favorites filter toggles and caps sort-bar cursor. `library_collections_sort` persisted in `Config`. `FjordState.all_collections`/`collections_fetched` for lazy fetch. `on_open_library(3)` fetches BoxSets once per session via `get_all_boxsets()`. `CollectionScreen` (new `collection.slint` + `collection.rs`): backdrop hero + Back button + member poster grid. `on_open_collection` wired in `main.rs`: fetches BoxSet items + all posters + BoxSet poster/backdrop in parallel, defers `show-collection` until all data ready. `handle_key` covers grid nav, Enter→detail, C→context-menu, Back-button focus. |
| 41 — WebSocket real-time events (2026-06-25) | `JellyfinClient::ws_url()` builds `ws[s]://host/socket?api_key=…&deviceId=…`. `ws.rs` module (tokio-tungstenite): persistent reconnect loop with exponential backoff (1 s → 60 s). **LibraryChanged** → debounced 5 s refresh of home dashboard rows (re-runs `fetch_home_data`, updates `AppState` models + poster cache, saves `home.json`). **UserDataChanged** → `update_card_in_all_models` patches played/fav on every visible card immediately (also updates Rust-side vecs via `update_item_user_state`). **ForceKeepAlive/KeepAlive** → sends `{"MessageType":"KeepAlive"}` response. `FjordState.ws_abort: Option<AbortHandle>` stores the task handle; `abort()` called on sign-out. WS started after both auto-login and manual login. |
| 42 — Code review CR8 (2026-06-25) | 6 findings from Phase 40 review. CR8-1: `open_collection_screen` now returns early on `get_boxset_items` error and calls `show_toast` ("Couldn't load collection…") instead of showing a blank screen. CR8-2: `layout.slint` double-click handlers for TV/Movies/Collections now call `open-library(nav)` — previously skipped, preventing lazy network fetch, sort restore, and `library-has-filters` initialisation. CR8-3: sign-out block now calls `set_show_collection(false)` and `set_all_collections(empty_model)` — previously `show-collection` could stay true after sign-out, routing keyboard events to the collection handler with a null client. CR8-4: stale-request guard added to `invoke_from_event_loop` closure in `open_collection_screen` — aborts if `collection-id` no longer matches (rapid double-open). CR8-5: `items_to_cards` now copies `series_id` from `MediaItem` — `update_card_in_all_models` can now match episode cards in `collection-items` by series. CR8-6: backdrop fetch in `open_collection_screen` is now conditional on `backdrop_image_tags` (fetches `get_item_detail` in the join, skips the backdrop HTTP call for BoxSets that have none). |
| 42 — Poster / backdrop cache cleanup (2026-06-26) | `run_poster_cache_cleanup(movie_ids, series_ids, collection_ids)` added to `home.rs`. Spawned as a background Tokio task after every auto-login. Builds a known-ID set from `all_movies ∪ all_series ∪ all_collections`, walks `~/.cache/fjord/posters/` and `~/.cache/fjord/backdrops/`, deletes files whose name (= item ID) is not in the set. Two guards: (1) skip if combined ID set is empty (handles network error / first run); (2) 24 h minimum interval via `~/.cache/fjord/last_cleanup` (ASCII Unix timestamp). `poster_cache_dir()` and `backdrop_cache_dir()` helpers added to `config.rs` alongside existing `poster_cache_path(id)` / `backdrop_cache_path(id)`. Portrait/season/episode cache files that fall outside the known ID set are re-fetched transparently on next access. |
| 43 — Collections dashboard (2026-06-26) | `CollectionsDashboard` component (new, in `home.slint`): two `SectionRow` rows — "Recently Added" and "Unwatched" — shown when `active-nav == 3 && !show-library`. Two new Jellyfin API methods in `fjord-api/client.rs`: `get_recently_added_collections` (15 newest BoxSets by DateCreated) and `get_unwatched_collections` (15 unplayed BoxSets, random order). `HomeData` extended with `recently_added_collections` + `unwatched_collections`; both fetched in parallel in `fetch_home_data` and set in `push_home_data`. `home_data_sections` array extended from 9→11 elements (indices 9+10); `spawn_poster_loading` updated to handle 11 sections; `push_section_model` cases 9→`recently-added-collections`, 10→`unwatched-collections`. `on_item_play` in `main.rs` now routes BoxSet IDs (found in `all_collections`) to `collection::open_collection_screen` before attempting playback. Sign-out clears both new AppState properties. |
| 43b — CR9: Collections dashboard bug fixes (2026-06-26) | **Critical**: `on_item_play` BoxSet routing now falls back to the always-populated dashboard models (`recently-added-collections` / `unwatched-collections`) when `all_collections` is empty (only populated after the library grid opens). **High**: `spawn_collections_poster_loading` `set_library_display` guard now requires `active-nav == 3` — previously could overwrite the movies grid if the user switched tabs during an async poster fetch. Same guard added to `spawn_movies_poster_loading` (`active-nav == 2`). **Medium**: `remove_from_dynamic_rows` now also filters `unwatched_collections` — played BoxSets were previously left in the Unwatched row with a ✓ badge. **Low**: `spawn_collections_poster_loading` now calls `set_all_collections(empty)` immediately for empty BoxSet lists (the while-let JoinSet loop was skipped, leaving a stale model). |
| 43c — CR9: Collections dashboard cleanup (2026-06-26) | **Cleanup**: `spawn_movies_poster_loading` and `spawn_collections_poster_loading` collapsed into a single `spawn_library_poster_loading(…, LibraryKind)` + shared `push_library_cards` helper, with thin public wrappers preserved for call sites. `LibraryKind` enum encodes the three things that differ: `item_type`, `active_nav` guard, and which AppState setter to call. **Altitude**: `push_section_model` now takes `HomeSection` (named enum with `#[repr(usize)]`) instead of `usize`. `home_data_sections` returns `[(HomeSection, Vec<MediaItem>); 11]`; `spawn_poster_loading` accepts the same type and builds a `[HomeSection; 11]` array to pass through `push_decoded_section`. `wire_nw_timer` uses `HomeSection::NotWatchedMovies as usize` and `HomeSection::NotWatchedTv as usize` for array indexing — silently wrong integer insertions are no longer possible. `HomeSection::empty_array()` provides the zero-filled base for partial fills. **Convention**: `app_state.slint` imports moved to after the `// ──` header block as required by CLAUDE.md. |
| 44 — Collections library warm-start cache (2026-06-26) | `load/save_collections_cache` added to `home.rs` (mirrors movies/series pattern). On warm start, `collections.json` is loaded into `FjordState.all_collections` + `AppState.all_collections`, `collections_fetched` set true, and poster loading started — so the Collections grid shows content immediately on the first open instead of appearing blank while the network fetch completes. `save_collections_cache` called after each successful `get_all_boxsets()` fetch in `on_open_library(3)`. CLAUDE.md disk-caches section updated. |
| 45 — Artist library screen (2026-06-26) | Music sidebar Enter → artist library grid → ArtistScreen → AlbumScreen. `get_album_artists()` + `get_artist_albums()` API methods. `LibraryKind::Artists` + `spawn_artists_poster_loading()`. `FjordState.all_artists`/`artists_fetched`; `artists.json` warm-start cache. `library_artists_sort` in `Config`. `AppMode::Artist` (between Series and Collection in priority). `artist.slint` ArtistScreen: circular portrait + meta + album grid. `artist.rs` `open_artist_screen` (parallel albums + portrait fetch, semaphore(8) poster load, gen-guarded). `on_open_detail` routes `MusicArtist` to `artist::open_artist_screen`. `run_poster_cache_cleanup` extended with `artist_ids` parameter. `library-has-filters` false for nav=4. Main.slint: nav=4 + `show-library` → `LibraryGrid` "All Artists"; `ArtistScreen` overlay between SeasonScreen and CollectionScreen; second video layer condition includes `show-artist`. |
| 46 — Music library Artists/Albums view toggle (2026-06-27) | Sort bar on the Music library grid (nav=4) gains [Artists] / [Albums] view toggle pills at cursor positions 5 and 6. View pills require Enter to apply (like filter toggles) — cursor navigates freely without switching the view, so the user can move from Albums to the sort pills without accidentally resetting the view. `library-music-view: int` (0=Artists, 1=Albums) in `AppState`; radio-button semantics (always one selected, both-unselected is impossible). `library_music_view: u8` persisted in `Config`. `get_all_albums()` API method (all MusicAlbums, SortBy=SortName). `LibraryKind::Albums` variant added; `matches_library_display()` method guards against Artists/Albums overwriting each other's grid on async completion. `spawn_albums_poster_loading()` wrapper. `FjordState.all_albums`/`albums_fetched`; `albums.json` warm-start cache; `load/save_albums_cache` in `home.rs`. `library_albums_sort: u8` persisted in `Config`. `on_open_library(4)` fetches artists and albums in parallel (each lazy, once per session); restores `library_music_view` and correct sort from config. `on_library_music_view_changed` persists config, restores sort, and calls `refresh_library_display`. Sign-out clears `all_albums`, `albums_fetched`, `set_all_albums(empty)`, `set_library_music_view(0)`. `run_poster_cache_cleanup` extended with `album_ids` parameter. |
| 47 — Playback queue (2026-06-27) | Context menu gains "Play Next" (row 2, insert at front) and "Add to Queue" (row 3, append to back) for any item — movies, episodes, album tracks. `QueueItem { id, item_type, series_id, title }` struct in `playback.rs`. `VideoState.queue: VecDeque<QueueItem>`. Natural-end path in `wire_mpv_timer` pops the queue when `!had_series` and `next_ep_pending` is None. `wire_queue_callbacks` in `context_menu.rs` wires the two callbacks; title is looked up from `FjordState` models (all_movies/all_series/all_albums/all_artists/series_episode_cache) at enqueue time — no Slint callback signature changes. `queue-count: int` in AppState; set on enqueue and after each pop. Cleared on sign-out. Series Up Next banner unchanged (series auto-advance takes priority over queue). |
| 48 — Alphabet scrubber keyboard nav + Music sort bar redesign (2026-06-27) | Removed A-Z letter-key shortcuts from `dispatch_library` (were intercepting `C`=context-menu and other single-letter actions). Replaced with a proper 5th library focus state: `library-scrubber-focused: bool` + `library-scrubber-cursor: int` (0=A..26=#) in `AppState`. Entry: sort bar Right past last pill when sort=A-Z and no query. In scrubber: Up/Down navigate A-Z/#, Enter jumps grid + returns to grid focus, Back/Left returns to sort bar. Scrubber cell for focused letter renders with `accent-muted` background + accent bold text. All sort pills now require Enter to apply (was auto-apply on navigate); cursor starts on the active sort pill when entering the sort bar (`sort_bar_init_cursor` helper). Music sort bar layout redesigned: [Artists(0)][Albums(1)] at LEFT (reached by pressing Left from sort pills) | Sort: [A→Z(2)][Z→A(3)][Year↓(4)][Year↑(5)][Shuffle(6)] | [Favorites(7)] at RIGHT — Music max cursor now 7. `pill-focused` in UI uses `si + (nav==4 ? 2 : 0)` offset. |
| 49 — Bottom player bar + Music player bar (2026-06-28) | Video mini-player moved from top to bottom. `MusicPlayerBar` (72 px, bottom) for audio-only playback: `item_type == "Audio"` sets `is-audio-playing` (no fullscreen player); album art + title/artist (click → open-album) + ⏸/▶ ⏹ + progress + elapsed/total. `start_playback` gains `audio_meta: Option<(artist, album_art_id)>`. All screens bottom-padded via `total-bar-h = bar-h + music-bar-h`. |
| 50 — Playlist backend + Prev/Next/Shuffle/Repeat (2026-06-28) | Two-collection model: `playlist: Vec<QueueItem>` + `playlist_index` (album/artist track list) and `queue: Vec<QueueItem>` (context-menu overflow, plays after playlist). `QueueItem.audio_meta` per item. `RepeatMode` (Off/All/One); LCG Fisher-Yates `shuffle_order`; `playlist_prev`/`playlist_next` helpers (prev: pos < 2 s → back, else restart). New actions `PrevTrack`(`[`)/`NextTrack`(`]`)/`ToggleShuffle`/`CycleRepeat`, global when audio playing. Music bar right zone: ⏮ ⏭ ⇌ ↺ (slots 4–7). Natural-end respects RepeatMode. |
| 51 — Queue viewer panel (2026-06-28) | Right-side 400 px overlay (dim backdrop, click-outside-closes). Entry: `q`/`Q` (player or audio playing) or ⋮ button (slot 8). `AppMode::QueuePanel` (after ContextMenu). Rows: 40×40 art + index + title/artist; current item accented; kb-y auto-scroll. Keys: Up/Down navigate, Enter → `queue-jump`, Delete → `queue-remove` (splices + rebuilds shuffle_order), Back/q close; Clear All clears both collections. `push_queue_display` rebuilds `queue-items` after every mutation; `spawn_queue_poster_loading` fills art via `set_row_data` (poster-id guarded). |
| 52 — Lyrics (2026-06-28) | `GET /Audio/{id}/Lyrics` (Jellyfin 10.9+, 404-tolerant) fetched at audio playback start → `VideoState.lyrics` + `AppState.lyrics-lines`. `LyricsView` full-window overlay (600 px column, kb-y auto-scroll, active line accent 16 px bold, ±1 line muted; unsynced lyrics render static). Toggle: ♪ slot 9 (only when `lyrics-available`) or `L`. Active-line tracking every ~500 ms (`rposition` on start_ms). Cleared on stop and track change. |
| 53 — Code review CR10 (2026-07-06) | Full-codebase review, 25 findings, all fixed same day (commits 449b84d…7e6d94b). **Critical:** CR10-1 `Handle::current()` panics in artist/album fav/played callbacks (captured rt handles); CR10-2 stale playlist/queue resurrected after unrelated playback (`keep_playlist` flag consumed by `start_playback`; `do_stop_playback` clears both); CR10-3 Jellyfin token logged in stream URLs (`redact_api_key`); CR10-4 Quit binding silently overwritten by queue-panel q/Q → Quit is now **Ctrl+Q**, handled as a global pre-dispatch in `handle_key` (works in every mode; q/Q never did). **High:** CR10-5 shift+letter combos never matched plain uppercase bindings (`lookup_action` shift-stripped retry — Z/X delay-decrease was dead); CR10-6 queue panel now renders context-menu queue rows (`QueueEntry.is-queued`) and `push_queue_display` owns `queue-count` = `upcoming_count`; CR10-7 Series cards resolved to next-up episode on enqueue (`queue_from_context_menu` + `enqueue_item`); CR10-8 Play Next under shuffle inserts after the current shuffle position (was dead slot 1); CR10-9 `push_decoded_series` nav guard 2→1; CR10-10 `update_item_user_state` covers albums/artists/episode caches; CR10-11 ws.rs UTF-8 byte-slice panic could kill the websocket loop permanently (`chars().take`); CR10-12 sign-out preserves `device_id` + settings (was deleting config.json); CR10-13 Up-Next resolves the next episode read-only via new `get_series_episodes` (no more premature `mark_played` at banner time). **Medium:** CR10-14 reverse-proxy subpath support via `api_url` helper (38 endpoints + `authenticate`); CR10-15 transient mpv error events no longer end playback; CR10-16 quit stop-report bounded to 5 s; CR10-17 `queue_remove` of the playing row is a no-op; CR10-18 queue title fallback scans visible card models (no GUIDs in panel); CR10-19 series/season Down falls through to the bottom bars; CR10-20 `series_open_id` claimed synchronously, `spawn_main` writes gen-guarded; CR10-21 `MpvRenderCtx` cb_data teardown verified race-free against mpv render.c (documented, no code change). **Low:** CR10-22 timestamps doc comment; CR10-23 real IsPaused in progress reports; CR10-24 case-insensitive library sorts (`sort_by_cached_key`); CR10-25 CLAUDE.md album-screen doc drift (✓ button removed). Runtime checks for the HTPC: artist/album ♥, Z/X during playback, Ctrl+Q, album→movie→natural-end. |
| 54 — Cache invalidation overhaul (2026-07-06) | Four-stage fix for the item-loading/staleness audit (S1–S4). **S1**: warm-start loads the JSON list caches for display only — `*_fetched` stays false, so the first grid open each session does its background network refresh again (previously the flags were pre-set and movies/collections/artists/albums were never re-fetched once a cache file existed). **S3/ws**: `LibraryChanged` payload (`ItemsAdded`/`Updated`/`Removed`) now drives invalidation — any event clears all `*_fetched` flags and refreshes the open grid via `spawn_library_fetch` (extracted from `on_open_library`); removed ids are purged from FjordState vecs, episode cache, collection map, every visible model (`remove_item_from_all_models`) and their poster/backdrop files+sidecars; the series list piggybacks on the 5 s debounced home refresh. **S2**: artwork revalidation — `MediaItem.image_tags` (+`primary_image_tag()`), `.tag` sidecars next to poster/backdrop files, `fetch_poster/backdrop_cached_tagged(expected_tag)` re-downloads on mismatch (stale copy served if the re-download fails); tags passed by all grid/row/thumb loaders + post-detail backdrop fetches; orphan cleanup strips `.tag`/`.tmp` suffixes. **S4**: self-healing 404s — `purge_deleted_item` + `is_not_found`; wired into `on_item_play`, detail/series `spawn_main`, and collection/album/artist opens (deleted parents show as empty-200 item queries, caught via the parallel detail 404). Also playlist review fixes shipped same day: queue plays after playlist exhaustion, `playlist_prev` shuffle-order gate, `rebuild_shuffle_order` on Play All, art/lyrics generation guards, slot-9 ghost focus, dead fullscreen audio-art path removed. |
| 55 — Jellyfin-style cards + Latest rows (2026-07-06) | **Two-line cards everywhere**: `CardItem.subtitle` + `MediaItem::card_title()`/`card_subtitle()` — episodes show series name / "S1:E3 - Title", series show "2019 - Present" (new `Status`/`EndDate` fields), albums show artist, movies year; series/season episode cards show episode name / "S1:E3"; `year` kept for sorting; `MediaCard` second row renders subtitle. **Recently Added = Jellyfin Latest**: new `get_latest(types)` (`/Users/{id}/Items/Latest`, `GroupItems=true`, bare-array response, played incl.) replaces the DateCreated-sorted Series/Movie/Album fetches, so the rows match the Jellyfin web home (shows surface when new episodes arrive). |
| 56 — Queue persistence + queue fixes (2026-07-06) | **Queue survives new playback**: `start_playback` never wipes playlist/queue (playing music = "insert at top of queue"; Play All keeps `vs.queue`; Stop keeps both; sign-out clears; `keep_playlist` removed). **Class-gated auto-advance**: natural end only advances audio→audio / video→video (`current_is_audio`), so a movie never auto-starts queued music. **Real queue titles**: `AppState.context-menu-title` set by every menu-open site; GUID fallback gone. **Idle queue panel**: `q` opens whenever `queue-count > 0` (empty → toast). **Container expansion**: queueing a MusicAlbum/MusicArtist enqueues its Audio tracks (album order preserved for Play Next); BoxSet toasts. |
| 57 — Jellyfin playlists (2026-07-06) | **API**: get_all_playlists (audio-only) / get_playlist_items (PlaylistItemId) / create_playlist / add_to_playlist / remove_from_playlist; MediaItem gains MediaType/PlaylistItemId/ChildCount/AlbumId. **Library**: [Artists][Albums][Playlists] view pills (sort pills shift to 3-7, Favorites 8), `playlists.json` cache, ws invalidation, cleanup known-set. **Playlist screen**: AlbumScreen in playlist mode — position numbering, per-row ✕ + Delete key removal (EntryIds), Play All, per-track album art (TrackItem.album-id). **Add to Playlist**: context-menu row 7 (music items) → PlaylistPicker modal (＋ New playlist… with typed name, existing list); albums expand to track ids; toasts + background list refresh. **Dashboard**: Music dashboard gains a Playlists row (section 3, `music-playlists` model, HomeSection::Playlists=16). |
| 58 — Music playback bug batch (2026-07-06) | Six user-reported fixes (commits c4ea624…2d5f357). **Play from Start on album/artist/playlist** built a video-player session on the raw container id — now expands to tracks (`music_container_tracks`, shared with queue expansion) and starts the music player; `on_item_play` guards MusicArtist the same way. **Queue panel shows current + upcoming**: finished/skipped playlist rows hidden (shuffle-aware play order; full list kept under Repeat All/One); jumping forward drops the previously-playing row; synthetic now-playing top row for off-list plays (`VideoState.now_playing`); `QueueEntry.index` = underlying position (-1 synthetic), visible number = display row. **❤ glyph** unified on album/artist fav buttons (was ♥). **Track fav indicator**: `update_card_in_all_models` patches the `album-tracks` TrackItem model. **Queue panel keyboard**: Up from top row → Clear All (cursor -1, Enter clears), Left closes the panel. |
| 59 — Empty Recently Added / Recently Played music rows (2026-07-06) | Both rows were empty because of Jellyfin server-side query semantics (reproduced with direct API calls against the live server). **Recently Added**: `/Items/Latest` returns nothing for `IncludeItemTypes=Audio` without a ParentId — new `get_latest_music()` resolves the music library view (`/Users/{id}/Views`, `CollectionType=="music"`, new `MediaItem.collection_type`) and scopes Latest to it (grouped → MusicAlbum items). **Recently Played**: `Filters=IsPlayed` on MusicAlbum needs EVERY track played (0 such albums here despite 55 played tracks) — `get_recently_played_albums` now mirrors jellyfin-web: played Audio tracks by DatePlayed → parent albums, deduped, play-recency order restored after the Ids batch fetch. |
| 60 — Split audio device: normal vs passthrough (2026-07-07) | New `Config.audio_device_passthrough` + Settings → Audio row 7 "Passthrough output" (dynamic device dropdown, hidden when SPDIF off; "" / Autoselect = same as audio output; IRQ row shifts to 8, audio-lang to 9). Routing resolved in `start_playback`: music (`item_type=="Audio"`) always plays PCM on the normal device and gets `audio-spdif` cleared; video playback switches to the passthrough device when SPDIF is enabled and one is configured. PipeWire IRQ gating (settings row visibility + WirePlumber config apply + startup sanity check) now keys off the EFFECTIVE passthrough device (passthrough if set, else normal). Previously one device fed everything, so music was locked to the bitstream device whenever passthrough was configured. |
| 61 — Audio channels setting (2026-07-07) | Settings → Audio row 1 "Audio channels" exposes mpv `--audio-channels` (`Config.audio_channels`, default auto-safe = option not passed; choices auto-safe/auto/stereo/5.1/7.1/"7.1,5.1,stereo"). Fixes multichannel PCM (5.1 FLAC, decoded AAC 5.1) being downmixed to stereo by mpv's auto-safe default on direct ALSA devices — "7.1,5.1,stereo" lets ALSA negotiate the best supported layout. SPDIF rows shift to 2-9, audio-lang to 10. |
| 62 — WebSocket keep-alive loop + log hygiene (2026-07-07) | **Keep-alive feedback loop**: Jellyfin acks every client KeepAlive with another KeepAlive; Fjord replied to acks → wire-speed ping-pong (~9k msgs/s, 66M debug lines, 6.4 GB fjord.log since ws landed). `run_session` now sends its own KeepAlive every 30 s (tokio interval in select! with the read loop) and ignores ForceKeepAlive/KeepAlive from the server. **Log hygiene**: fjord.log rotated to fjord.log.old on every start (previously appended forever, `rolling::never`); file layer filtered to info (console keeps debug; `RUST_LOG` overrides both). Historical note: the only real panics in the 6.4 GB log were the June 22 "Recursion detected" series-screen crash, already fixed. |
| 63 — Poster decode-to-size (2026-07-07) | `decode_scaled(bytes, max_dim)` in poster.rs: `decode_poster_buffer` caps the longest side at 600 px (posters/portraits/thumbs/album art — a 2:3 poster decodes to 400×600 ≈ 1 MB RGBA instead of ~6 MB at server resolution), new `decode_backdrop_buffer` caps at 3840 px for the four full-window backdrop sites (detail/series/season/collection) — 4K sources display 1:1; the poster cap is safe at any resolution because cards max out at 190×304 logical px (breakpoints add columns, not size) so 600 px covers up to 2× HiDPI scale. Cuts model memory ~6-10× on large libraries with no visible quality change (cards render ≤400 px even on 4K). |
| 64 — Gapless music playback (2026-07-07) | Track transitions no longer rebuild the mpv player. The 16 ms timer preloads what natural end will play (`peek_natural_next`/`natural_next_index` — mirrors the timer advance incl. RepeatMode::One=repeat-current, shuffle order, queue-head class gating) into the running instance via `Player::append_gapless` (loadfile append; mpv's default gapless-audio=weak does the seamless join) when <12 s remain and the next item is Audio. `PollResult::TrackChanged` (EndFile with a pending append) commits: `commit_natural_next` advances index/queue, generation bump, `apply_audio_track` (music-bar UI + art + lyrics, condensed mirror of start_playback's is_audio block), stop/start progress reports, queue display refresh. `invalidate_preload` (+`Player::cancel_pending` = playlist-remove 1) fires on shuffle/repeat toggles and queue edits; `tear_down_player` clears the field. `Config.gapless_audio` toggle (Settings → Audio row 11 "Gapless music playback", default on). Video and Up-Next paths unchanged. |
| 65 — Enter on a track plays the album from that track (2026-07-07) | `on_play_album_track` now builds the full visible tracklist (album OR playlist screen) into `vs.playlist` with `playlist_index` at the chosen track — the rest of the album follows (gapless applies), matching Jellyfin/Spotify. Keeps `vs.queue` (Phase 56). Also drops the per-play `get_item_detail` roundtrip — title/artist/art ids come from the TrackItem model. |
| 66 — Now Playing screen (2026-07-07) | Fullscreen music view (`ui/now_playing.slint`, `NowPlayingScreen`) shown when `show-now-playing && is-audio-playing`: 380px album art + title/artist + click-to-seek progress + transport row (Open Album/⏮/⏸‑▶/⏭/⇌/↺) on the left; synced lyrics (reuses `lyrics-lines`/`lyrics-active-idx`, same kb-y auto-scroll idiom as `LyricsView`) on the right; "Up Next" horizontal strip (`queue-items`, kb-x auto-scroll, click/Enter → `queue-jump`) along the bottom; mouse-only ✕ top-right. Keyboard state `now-playing-in-strip`/`now-playing-ctrl-focused`/`now-playing-strip-focused`; new `AppMode::NowPlaying` (checked after QueuePanel, before Person) + `handle_key_now_playing` (Up/Down toggle row, Left/Right move, Enter activates, Back closes) — PrevTrack/NextTrack/ToggleShuffle/CycleRepeat/ToggleLyrics/Space-pause already fire via existing global pre-dispatch before this arm sees them. **Entry points**: music bar's art/title click now opens Now Playing (`open-now-playing` callback, was `music-bar-open-album` — that action moved inside the screen as ctrl-slot 0); `m`/`M` key toggles from any mode when audio plays; auto-open after exactly 30 s idle (`VideoState.music_idle_ticks`, reset on every keypress + pinned to 0 while the screen is open, so any close path needs a fresh 30 s before re-firing) gated by `Config.now_playing_auto_open` (Settings → Audio row 12, default on — fixed threshold, not user-tunable in v1). `reset_playback_ui` + sign-out clear `show-now-playing`. |
| 67 — Critical fix: Now Playing idle auto-open self-deadlocked the UI (2026-07-07) | Diagnosed from a real crash: `coredumpctl` showed SIGABRT with a full core; `journalctl --user _PID=<pid>` (captures stdout/DEBUG level, unlike the info-filtered fjord.log since Phase 62) showed the ws keep-alive task ticking normally for 2+ minutes with zero other output — the UI thread was frozen while background tokio tasks kept running, and mpv's own audio thread kept playing independently (matches "music still plays, interface frozen"). Root cause: the idle-auto-open block in `wire_mpv_timer` called `invoke_open_now_playing()` while still holding the `VideoState` mutex lock (`vs`); that callback synchronously calls `refresh-queue-display`, whose handler re-locks the SAME mutex on the SAME thread — `std::sync::Mutex` isn't reentrant, so this self-deadlocked the UI thread permanently on the first idle auto-open. Fixed by computing a local `auto_open_now_playing: bool` inside the locked block and firing `invoke_open_now_playing()` only after the lock is released — the exact deferred-callback pattern the function already used for `gapless_commit`/`banner_trigger`. |
| 68 — Fix: Now Playing keyboard nav hijacked by stale bar focus (2026-07-07) | The music-bar-focused and mini-player-bar-focused pre-dispatch blocks in `handle_key()` run BEFORE the per-screen `match mode` dispatch and only excluded `AppMode::Player`/`ContextMenu`(/`QueuePanel`) — not `NowPlaying`. If either bar had a leftover focus slot (`music-bar-focused >= 0` or `float-card-focused >= 0`) from before Now Playing opened, its arrow keys/Enter kept routing to that stale bar state instead of `handle_key_now_playing`, matching the report "keyboard nav still on the previous screen like the miniplayer." Both blocks now also exclude `AppMode::NowPlaying`. |
| 69 — Volume controls for music playback (2026-07-07) | Music-only playback had no way to change volume — `VolumeUp`/`VolumeDown` only existed in the player-only keymap, so `Up`/`Down` meant volume solely inside the fullscreen video player. Added 🔉/🔊 buttons, keyboard-navigable like every other control (not a new global keybinding): music bar slots 10/11 (after ♪ Lyrics, always shown — Right from 8/9 reaches them, skipping 9 when lyrics unavailable) and Now Playing transport-row ctrl-focused 6/7; both call the existing `volume-up`/`volume-down` AppState callbacks (`on_volume_up`/`on_volume_down` in `controls.rs`, unchanged — they already operate generically on `vs.player`, video or audio). The feedback toast (`VolumeOverlay`, level bar + %/passthrough text) was fullscreen-player-only; extracted from `player.slint` into a shared `widgets.slint` component and instantiated once at the `MainWindow` root (like `ToastNotification`) so it now shows over the music bar and Now Playing too. |
| 70 — Fix: music-bar volume buttons clipped off-window (2026-07-07) | The right-zone HorizontalLayout (⏮⏭⇌↺⋮♪🔉🔊) had a stale hardcoded `width: 240px`, sized for the original 6-icon set before Phase 69 added 🔉/🔊 — 8 × 36px icons + 7 × 8px gaps needs 344px, so the two volume buttons rendered past the container's bounds and were clipped at the window edge (only a sliver visible). Widened to 344px. |
| 71 — Fix: Now Playing Up Next strip title clipped to invisible (2026-07-07) | `strip-h` (132px) left the Flickable only ~80px tall — barely enough for the 72px art + 4px spacing, leaving ~4px for the title line, which rendered essentially clipped to nothing ("just album poster, can't see the song title"). Raised `strip-h` to 156px (~104px Flickable height, ~28px of headroom under the art) and gave the title `Text` an explicit `height: 16px` so its size is deterministic rather than relying on layout auto-sizing inside the tight Flickable. |
| 72 — Fix: Now Playing lyrics frozen/tiny; unified with LyricsView (2026-07-07) | Lyrics active-line tracking in `wire_mpv_timer` was gated on `show-lyrics` alone — the boolean for the separate fullscreen overlay. Now Playing reads `lyrics-active-idx` directly without ever setting `show-lyrics`, so its inline panel never advanced (looked frozen) and never auto-scrolled (kb-y depends on the index changing). Gate widened to `show-lyrics || show-now-playing`. Also bumped text size in both surfaces per request — active line 16/22→28px bold, other lines 14-15/17→18px, line height 40-44/52→64px — `now_playing.slint` and `widgets.slint`'s `LyricsView` now share identical values so they read the same regardless of which one is open. |
| 73 — Fix: lyrics still not scrolling; direct viewport-y binding (2026-07-07) | Phase 72's gating fix alone didn't resolve it — retested on a fresh `fjord-git r663.9b6780b-1` build. The kb-y-property + `changed kb-y => { fl.viewport-y = kb-y; }` idiom (used everywhere else for keyboard-driven lists) left both lyrics surfaces pinned at the top: the imperative write inside `changed` wasn't reliably taking effect against the Flickable's own internals. Switched both `now_playing.slint` and `widgets.slint`'s `LyricsView` to a DIRECT `viewport-y: clamp(...)` binding (still `parent.height` + content `preferred-height`, never `self.height`/`self.viewport-height` — the established self-reference rule) so Slint keeps it live-synced on every `lyrics-active-idx` change; trades away manual mouse-wheel drag, acceptable for a passive auto-follow view. Also enlarged text again per request: active line 28→40px, other lines 18→24px, line height 64→76px, both surfaces still matching. |
| 74 — Real root cause: lyrics ticks read as milliseconds (2026-07-07) | Phase 73's diagnosis was wrong — verified this time by querying the Jellyfin `/Audio/{id}/Lyrics` endpoint directly for the track that was playing: all 72 lines had valid, non-zero `Start` timestamps (e.g. 34,000,000 for a line at 3.4 s), so the data was correctly synced and the Slint binding mechanism was never the problem. The actual bug: `get_lyrics` in `fjord-api/client.rs` stored Jellyfin's raw `Start` value (ticks, 100 ns units — same scale as `RunTimeTicks`) directly into the tuple documented everywhere as `(start_ms, text)`, with no conversion. 34,000,000 ticks (3.4 s) was compared against a real millisecond position as if it meant 34,000,000 ms (~9.4 h) — playback position never gets remotely close, so `lyrics-active-idx` stayed permanently stuck and nothing ever highlighted or scrolled, on every track, regardless of the Slint binding mechanism used. Fixed with a single `/ 10_000` at the parse site; verified by simulating the fixed conversion against the real Hamilton lyrics data end-to-end (line index correctly advances at 3.4 s, 7.3 s, 11.2 s, matching real timestamps). Reverted Phase 73's direct-`viewport-y`-binding change back to the standard kb-y-property + changed-handler idiom (restores native mouse-wheel scroll on the lyrics panel; it was never broken, so there was nothing to fix there) — kept the larger text sizes (40px active / 24px other / 76px line height, both surfaces still matching). |
| 75 — Smoother lyrics auto-scroll (2026-07-08) | Now that lyrics genuinely track playback, the per-line jump felt jarring at 200ms ease-out — especially on lines close together (Hamilton's rapid verses can advance the active index by more than one line between the ~480 ms position-poll ticks). Slowed to 450ms ease-in-out in both `now_playing.slint` and `widgets.slint`'s `LyricsView`, so multi-line jumps glide instead of snapping. |
| 76 — Lyrics highlight transition still snapped (2026-07-08) | The 450ms scroll animation only covered `viewport-y` — the per-line `Text`'s `font-size`/`color`/`font-weight` swap (24px muted → 40px accent bold) happened instantly the moment `lyrics-active-idx` changed, so the highlight itself still looked like a hard cut even though the container glided. Added `animate color, font-size { duration: 350ms; easing: ease-in-out; }` to the per-line `Text` in both `now_playing.slint` and `widgets.slint`'s `LyricsView` (font-weight left unanimated — Slint doesn't smoothly interpolate discrete int weight steps). |
| 77 — Perf: dedupe redundant per-tick mpv position reads (2026-07-08) | User asked for anything optimizable given old HTPC hardware. Audited the 16 ms `wire_mpv_timer` hot path (runs ~62×/sec during all playback) and found chapter tracking, the skip-segment check, and the Up Next banner check each independently called mpv's `get_position()`/`get_duration()` (an FFI round-trip into libmpv) every tick, for the entire duration of any video — up to 4 redundant reads/tick of the exact same values. Consolidated into one shared `live_pos`/`live_dur` read per tick, reused by all three call sites. Zero behavior/timing change (same 16 ms cadence everywhere), pure reduction in FFI call volume — the highest-confidence, lowest-risk win available on this hot path. Considered but declined (user's call): defaulting console/journal logging from debug to info to cut routine journal I/O — kept at debug since that's the exact trail that diagnosed Phase 66's deadlock crash; RUST_LOG remains available to dial verbosity down per-run if ever wanted. |
| 78 — Code review CR11 (2026-07-08) | Full review of everything added since CR10 (~3600 lines: gapless playback, persistent queue/playlist model, Now Playing screen, playlist feature, audio routing settings) — 16 findings, all fixed same day. **High:** CR11-1 Collection card "Play from Start" tried to play a dead `/Videos/{boxsetId}/stream` URL — the "Add to Queue" path already guarded BoxSet with a toast, this entry point never got the same check; CR11-2 the 5 s debounced post-`LibraryChanged` home/series refresh in `ws.rs` wasn't covered by `ws_abort` (only the outer reconnect loop is) — sign-out didn't cancel it, so a change event firing right before logout could write a departed (or, on a shared HTPC, a different) session's data into `FjordState`/`AppState` afterward; now session-guarded via `Arc::ptr_eq` against the live client before AND after the fetch; CR11-3 `artist.rs`/`collection.rs`/`album.rs` screen-opens never called `invoke_grab_keyboard_focus()` (every other screen does) — keyboard could go dead entering these screens after mouse-only use; CR11-4 the mini-player-bar-focused pre-dispatch guard in `keys.rs` excluded `Player`/`ContextMenu`/`NowPlaying` but not `QueuePanel` (the parallel music-bar guard did) — stale bar focus could hijack Queue Panel keys. **Medium:** CR11-5 the `ResumePlayer` ('r') pre-dispatch didn't exclude `QueuePanel`/`NowPlaying`; CR11-6 the music-bar ⋮ Queue button opened the panel straight from Slint, skipping the keyboard focus re-grab every other entry point does — new `AppState.open-queue-panel()` callback mirrors the 'q' keyboard path plus the re-grab; CR11-7 `refresh_playlists` never refreshed an already-open playlist detail screen — adding a track to the playlist you're currently viewing left its tracklist stale until closed/reopened; now reopens it when the mutated playlist matches; CR11-8 `LyricsView` had no `!show-now-playing` guard — pressing `L` while Now Playing was open stacked a redundant fullscreen lyrics popup on top of its own embedded lyrics panel; CR11-9 Now Playing's lyrics lines were missing `horizontal-alignment: center` and used 24px padding vs `LyricsView`'s 32px, despite CLAUDE.md's explicit "unified" claim — fixing the width binding (`width: parent.width` on a bare `Text` inside the VerticalLayout) initially caused a real Slint binding-loop compile error (`width -> layoutinfo-h -> min-width -> viewport-width -> width`), the same self-referencing-layout-cache class of bug already documented for `viewport-y` — resolved by wrapping each line in a `TouchArea` (as `widgets.slint` already does), which isn't layout-sized from its child; CR11-10 the gapless preload check read `get_position()`/`get_duration()` independently every tick instead of the tick's already-computed `live_pos`/`live_dur` (Phase 77's dedup didn't reach it — it lives outside the `if vs.player.is_some()` block). **Low:** CR11-11 `Player::poll()`'s `EndFile` handling ignored the `reason` field — any end-of-file with a pending gapless append was treated as a successful transition even if it ended via error/stop/quit, potentially showing a track as "now playing" (with a start/stop report sent) that never produced audio; now only `Eof` claims the transition, anything else discards the pending entry; CR11-12 a failed `append_gapless` retried the identical peek+append every tick for the whole ~12 s pre-end window (up to ~750 wasted IPC calls) — added a self-decrementing `gapless_retry_cooldown` (~1 s backoff, no explicit reset needed on track change); CR11-13 `cancel_pending` blindly issued `playlist-remove 1` without checking mpv's own `playlist-pos` — a shuffle/repeat toggle landing in the same tick as mpv's own internal gapless transition could cut off audio mpv was already playing; now checks `playlist-pos` first and no-ops if mpv already made that entry active; CR11-14 `playlist_picker_create` skipped the empty-track-list check its sibling `playlist_picker_select` had — a 0-track album silently created an empty playlist; CR11-15/16 stale doc comments in `settings.slint` (Audio row indices) and `app_state.slint` (`now-playing-ctrl-focused` missing VolDown/VolUp) corrected. Runtime checks for the HTPC: BoxSet card Enter, sign-out during an active library-changed event, artist/album/playlist keyboard nav after mouse use, queue panel via mouse ⋮ after mouse-only use, `L` during Now Playing, adding a track to the open playlist, a track failing mid-gapless-preload. |
| 79 — Fix: long lyric lines cut off with "…" (2026-07-08) | Both lyrics surfaces (`LyricsView`, `NowPlayingScreen`) used `overflow: elide` on a fixed-height single line, so any line too wide for the panel (common on lyrically dense songs — Hamilton's rap verses, this session's original test case, are exactly this) got truncated with an ellipsis instead of scrolled into view. Switched to `wrap: word-wrap` on a `min-height: 76px` row (Text's height is left unset so it reports its real wrapped size upward instead of being fixed and then eliding) — long lines now wrap to 2+ lines instead of ever being cut off; short lines keep the usual 76px row rhythm. Left the `kb-y` auto-scroll formula as a fixed `idx * 76px` per row rather than computing a real cumulative sum of per-line wrapped heights (would need each line's rendered height fed back from Slint, which isn't straightforwardly queryable from outside a `for` loop) — in practice this means a wrapped line only nudges the centering of later lines by about one row's worth until the next re-render, a minor approximation rather than lines becoming unreachable (the Flickable's own bounds still clamp scrolling to the real content range). Revisit with a precomputed per-line height model if that drift turns out to be noticeable on a real long song. |
| 80 — Lyrics: wider panel + pre-reserved wrap height (2026-07-08) | Two follow-ups to Phase 79 from a screenshot showing the standalone `LyricsView` on a real HTPC-sized window. **Wider column**: the centred panel was capped at a flat 600px, leaving large empty margins either side on typical displays — widened to `min(parent.width - 160px, 1200px)`, then simplified same day to a flat `parent.width - 160px` (80px padding each side, no upper cap) after reconsidering: each lyric line is short and horizontally-centred rather than flowing paragraph text, so there was nothing for a cap to actually protect against — it only shrank the usable column on large displays for no benefit. **Pre-reserved wrap height**: a line that fits on one line at the small 24px inactive size but needs 2 lines once it lights up at 40px bold previously reflowed the whole list by a row's height at the exact moment it activated, stacked on top of the font-size/color animation. Added an invisible `sizer` probe per row — same text, always measured at the ACTIVE font/weight (40px/700) — and floor each row's `min-height` at `sizer.preferred-height` regardless of whether that row is currently active, so the space is reserved up front; the visible (dynamically-sized) Text is then centred manually within that row via `y: (parent.height - self.height) / 2` (its own height can't be bound to `parent.height` — that's the exact self-referencing-layout-cache cycle fixed in CR11-9, since `sizer`, not this Text, is what the row's height derives from). Net effect: activating a line now only animates font-size/color: the row was already the right height. Note this makes the Phase 79 auto-scroll approximation (fixed 76px/row) *less* accurate on wordy songs, since more rows are now permanently taller than 76px rather than only transiently — same accepted trade-off, flagged again here in case it turns out to matter once tested on real long tracks. |
| 81 — Now Playing: Up Next chip titles wrap instead of eliding (2026-07-08) | Same complaint as Phase 79, this time for the "Up Next" strip's 92px-wide chips: `overflow: elide` on a fixed 16px-tall single-line Text cut a long track title down to its first few words. Switched to `wrap: word-wrap` on a fixed 2-line-tall `height: 32px` box (`vertical-alignment: center` added so a short, single-line title still sits centred rather than pinned to the top of the taller box) — no self-referencing-layout-cache risk here since both `width`/`height` are plain literal constants, not `parent.width`/`parent.height` bindings, unlike the lyrics fix. All chips in the strip share one fixed-height `Flickable`/`HorizontalLayout` row (unlike the lyrics `VerticalLayout`, where each row can have its own height), so there's no per-chip dynamic sizing here — `strip-h` raised from 156px to 172px (the exact +16px the title box grew by, 16px→32px) to give every chip room for a 2-line title uniformly. Titles needing a 3rd line still elide as a fallback — a reasonable bound for something this narrow. |
| 82 — Now Playing: Up Next chip titles too dim (2026-07-08) | Non-current chip titles used `Theme.text-muted` (#8e8e93, medium gray), hard to read against the dark strip background — switched to `Theme.text` (#f2f2f7, near-white), the same primary text color used everywhere else. The currently-playing chip keeps `Theme.accent` to stay visually distinct from the rest. |
| 83 — Now Playing: left column elements weren't sharing a center line (2026-07-08) | Screenshot showed the progress bar and transport-button row visibly shifted left relative to the album art and title/artist text. Root cause: only the `art` Rectangle had manual `x: (parent.width - self.width) / 2` centering; the title `VerticalLayout`, progress `VerticalLayout`, and transport-button `HorizontalLayout` (all fixed/content-width, narrower than the outer column) relied on the outer `VerticalLayout`'s default cross-axis behavior instead, which isn't centering — its `alignment: center` only governs the vertical (main-axis) stacking, not horizontal position of children with an explicit or content-derived width. Added the same manual centering expression to all three, so every element in the column now shares one true horizontal center line regardless of its own width (380px for title/progress, content-width for the button row, which has no explicit width of its own — `self.width` there is fine to reference since it's derived from content, not from the very `x` binding being computed, so no self-referencing-layout-cache risk). |
| 84 — Investigating: video doesn't start after music with SPDIF passthrough (2026-07-08, in progress) | User report: with a hardware bitstream device set as the SPDIF passthrough device and PipeWire as the normal device, starting a video while music is playing leaves the video stuck — it only starts after a seek. Code review of `start_playback`/`Player::new` found no obvious Fjord-side blocking call (the audio-device switch is a plain option string; mpv opens devices lazily on its own thread) — leading hypothesis is a real mpv/hardware interaction: `video-sync=display-vdrop` (the user's actual setting, confirmed) still gates video display on the audio clock, and a hardware SPDIF/HDMI receiver can take a real multi-second delay to lock a new bitstream format, especially right after the previous session was using a completely different audio path (PipeWire PCM for the music). A user-supplied `fjord.log`/`fjord.log.old` pair didn't confirm this directly: (1) it revealed a **separate, real "Recursion detected" Slint panic** (properties.rs:583) right after a `stop_playback`, backtrace fully unsymbolicated — not yet root-caused, needs a reproducible trigger; (2) it revealed that after a mid-session settings change, the "Audio device" (normal) setting itself resolved to the same hardware device as the passthrough target (`alsa/hdmi:CARD=NVidia,DEV=0`), not PipeWire — meaning the capture doesn't necessarily represent the originally-reported normal=PipeWire/passthrough=hw configuration; (3) even so, none of the logged timestamps ("mpv player started" → "mpv render context created" → "active decoder") showed an anomalous multi-second gap in any capture — because the existing logging only proves the render *context* was constructed, not that a frame was ever actually drawn. Added a new log line, `first frame rendered {:.3}s after player start`, right where `ctx.render()` first succeeds after each `Player::new` (`VideoState.first_frame_logged`, reset per-play) — this is the missing telemetry: a genuine stall would now show up as a large gap between "player started" and "first frame rendered" instead of looking identical to a normal fast start. Waiting on a fresh repro with this logging plus confirmation of the actual normal/passthrough device configuration before making any playback-behavior change. User then ran a deliberate A/B test: normal=PipeWire + passthrough=a *different* hw ALSA device reproduces the stall every time; normal=hw ALSA + passthrough=the *same* hw ALSA device does not. This rules out `video-sync=display-vdrop` as the cause (both configs use it identically) and points specifically at the **PipeWire → raw ALSA handoff**: PipeWire keeps its claim on an ALSA hardware node open until WirePlumber's own idle-suspend timeout elapses (not instantly on last client disconnect), so mpv's raw `hw:`/`alsa:` open for the passthrough device can fail/stall if it lands inside that window right after music (PipeWire) stops — matching the user's separately-reported history of needing to close other audio programs for passthrough to work at all. A manual seek doesn't fix anything directly; it just buys enough wall-clock time for PipeWire to finish suspending before mpv's next attempt. Still waiting on the actual `first frame rendered` delay from a repro of the genuinely-broken (differing-device) config to size a fix (e.g. an automatic retry after N seconds) rather than guessing a timeout. User then reported the exact symptom directly (no log needed): stats overlay confirms the file is loaded, the player is just black, pause/resume does nothing, only a manual seek starts it. This ruled out the `first_frame_logged` telemetry from Phase 84 as useless for this bug — `mpv_render_context_render()` succeeds and paints whatever's in the FBO regardless of whether mpv has fresh decoded content, so that log line was firing almost immediately even during the stall, never during it. See Phase 85 for the actual fix. |
| 85 — Fix: auto-recover from the passthrough playback stall (2026-07-09) | Given pause/resume doing nothing but a seek fixing it immediately, the real signal is **position not advancing despite not being paused** — directly checkable, unlike render-call success. Added `VideoState.stall_baseline_pos` (the expected start position — 0.0 or a resume point, captured in `start_playback`) and `stall_recovery_attempted` (one-shot guard). In `wire_mpv_timer`: if 5s have elapsed since player start, live position hasn't advanced ≥1s past the baseline, mpv isn't paused, and mpv isn't legitimately `paused-for-cache` (real network buffering also shows no position progress without touching the user-facing `pause` property — seeking during genuine buffering would make it worse, not better), automatically issue `Player::seek_backward(10.0)` — the exact class of action the user found fixes it manually — and log a warning. The 5s threshold is a judgment call, not a measured one (normal 4K HEVC startup takes ~2s per earlier logs), chosen to comfortably clear real startup delay without leaving a stuck video black for long. Root cause is still believed to be PipeWire's ALSA-node release timing on the handoff to a different raw ALSA passthrough device (Phase 84's A/B test); this doesn't fix that timing issue itself, it papers over it by automating the same recovery action already confirmed to work, since fixing PipeWire's own suspend behavior from Fjord's side isn't practical. |
| 86 — Log the build version at startup (2026-07-09) | User asked if a shared `fjord.log` could reveal which build produced it — it couldn't; nothing logged Fjord's own version, only the Jellyfin server's. (Initial diagnosis of the preceding crash log mistakenly cited the *dev machine's* local `PKGBUILD` `pkgver` line as evidence of the HTPC's build — wrong: that file's `pkgver` is only updated by a `makepkg` run on whichever machine actually built it, and nothing links the dev machine's copy to the HTPC's. Corrected after the user pointed it out; the actual build version of that earlier crash log remains unknown.) `build.rs` now shells out to `git rev-list --count HEAD` / `git rev-parse --short HEAD` / `git status --porcelain` at compile time (matching the PKGBUILD's own `r<count>.<hash>[-dirty]` scheme) and exposes it via `cargo:rustc-env=FJORD_BUILD_ID`; `main()` logs `fjord version: {FJORD_BUILD_ID}` as the second line of every run, right after the log-file-path line. Every future `fjord.log` now self-identifies its exact commit (and whether the working tree was dirty when built) directly from the machine that produced it — no more guessing from an unrelated file. |
| 87 — Connection-issues review + startup connectivity gate (2026-07-09) | User asked for a review of how the app detects and responds to server connectivity problems. A general-purpose research agent surveyed `client.rs`/`ws.rs`/`main.rs`/`auth.rs`: every API call shares one 30s-timeout `reqwest::Client` with no retry logic, collapsing DNS/connect/timeout/5xx/malformed-JSON into the same opaque `anyhow::Error`; only two call sites anywhere check for a dead session (401); manual login used a bare `reqwest::Client::new()` with **no timeout at all**; toast-vs-silent-warn coverage was inconsistent for no principled reason (Artist/Album/Playlist/Collection screen-opens toast on failure, Detail/Series/Season don't); and the WebSocket has zero visible UI indicator regardless of connection state. Confirmed concretely against a real dev-machine `fjord.log` from a fully offline cold start: the one-shot `check_auth` probe timed out (30s) but the app proceeded anyway per the documented "ignore and proceed" design, then fired all 17 home-dashboard fetches in parallel — every one timed out, twice (~47s apart), all silent — and the WebSocket didn't attempt its first connection until **3+ minutes after startup**, queued behind the serial auto-login → home-fetch → series-fetch chain. **Decision** (after discussion): don't retrofit a toast onto every one of the ~20 silent call sites; instead gate the whole cold-start experience on the auth probe succeeding. Implemented: `client.rs::check_auth` now uses its own 8s timeout (down from the 30s default) since it's the one call the entire startup UI blocks on; `main.rs` no longer pushes cached home/movies/series/collections/artists/albums/playlists into AppState before the probe resolves — new `push_cached_data()` does that only after a successful probe, and new `spawn_auto_login()` (re-invokable, so the Retry button can call it again with fresh clones) branches three ways: reachable → `push_cached_data` + the existing background refresh/websocket-start flow, unchanged; 401 → `show-login` (unchanged); anything else → new **`OfflineScreen`** (`connect.slint`) with a Retry button, no cached content shown at all. New **`ConnectingScreen`** covers the gap between "window created" and "probe resolved" instead of a blank/undefined state. Both screens are new top-level routing slots in `main.slint`, checked before `show-login`. `keys.rs::handle_key` swallows all keys on `show-connecting` and maps Enter → `retry-connection` on `show-offline` (OfflineScreen's button has no native widget focus path, unlike LoginScreen's `LineEdit`s). Also fixed while in the area: `auth.rs::do_login`'s manual-login HTTP client now has the same 30s timeout as everywhere else (previously unbounded — the worst single offender identified in the review). Explicitly deferred: a shared app-wide "can't reach server" indicator for *mid-session* failures (not just startup), and centralizing 401 detection beyond the two existing call sites — both real findings from the review, kept as future work rather than expanding this change further. Separately clarified during this work: the on-disk caches are purely a "paint something instantly" mechanism, not incremental/delta sync — every refresh (cold start, grid open, WS-triggered invalidation) re-fetches the entire list from Jellyfin; the WS's `LibraryChanged` item IDs are only used to purge removed items and force a full re-fetch, never to patch in just the changed items. **Follow-up same day**: user caught that `OfflineScreen` was a dead end for a *permanent* failure (server decommissioned, wrong saved URL) — only a Retry button existed, with no way to change the server/credentials or quit. Also caught in fixing this: my original `show-connecting`/`show-offline` guards in `handle_key` returned unconditionally before the later global Ctrl+Q pre-dispatch could ever run, silently killing the Quit shortcut on both new screens. Fixed both: `OfflineScreen` now has three buttons (`offline-focused` 0/1/2, Left/Right + Enter) — Retry, **Change Server** (`AppState.sign-out`, safe to call pre-login since it only clears already-empty/default state and the saved session fields), and **Quit** (`AppState.quit`); Ctrl+Q is also explicitly checked and handled before the two screens' early returns, working regardless of button focus, matching the global shortcut used everywhere else in the app. |
| 88 — Fix: LoginScreen also had no Quit button (2026-07-09) | Same gap as Phase 87's OfflineScreen fix, but pre-existing rather than introduced this session — LoginScreen has never had a Quit affordance. `handle_key` returns `false` for the whole `show-login` state to let `LineEdit` handle normal typing/tabbing, which meant Ctrl+Q never reached the global Quit pre-dispatch either (same underlying bug class as the connectivity screens, just older). Added a small "Quit" `FjordButton` in the screen's bottom-right corner (`login.slint`), and carved Ctrl+Q out of the `show-login` early return in `keys.rs` before the `LineEdit`-passthrough `return false` — safe alongside normal typing since Ctrl+Q isn't used for text editing in any field. |
| 89 — Incremental WebSocket-driven sync: library lists + focus safety + targeted refreshes (2026-07-09) | Phase 87 noted every refresh (cold start, grid open, WS invalidation) re-fetched entire lists even though the WS already reports which item IDs changed. Full design went through three rounds of user pushback on premature scoping-out during planning (see `[[plan groovy-stirring-phoenix]]`); final scope: **§0 focus safety** — new `reanchor_focus(model, id)` (`context_menu.rs`) finds an item's new row index by id after a model mutation; `remove_item_from_all_models` now captures whether `library-focused`/`series-focused-ep`/`season-focused-ep` pointed at the row being removed and clamps into new bounds afterward (previously these three plain integer indices could silently end up pointing at a different card after any removal — pre-existing gap, not introduced by this phase, fixed while touching the code). **Phase 1** — `JellyfinClient::get_items_by_ids` (chunked 200/req, concurrent `JoinSet`+`Semaphore(4)`, `Fields` covers all six list types + `DateCreated`/`SeasonId`/`SeriesId`/`IndexNumber`/`ParentIndexNumber`); `upsert_media_item` (`config.rs`), `upsert_cards_in_model` (`context_menu.rs`, poster-preserving), `fetch_posters_for_delta` (`poster.rs`) new delta-merge helpers — deliberately NOT reusing the six `spawn_*_poster_loading` loaders, which wholesale-replace their model. `ws.rs`'s `LibraryChanged` handler no longer force-refreshes an open grid or calls `get_all_series` (which had a latent bug: its old `items_to_model` path never set `poster`/`has_poster`, so the TV grid went poster-less after every WS series change) — added/updated ids accumulate into a shared `pending_upsert_ids` set, debounced 5s, fetched via `get_items_by_ids`, bucketed by type, upserted into `FjordState.all_X` + on-disk cache + the Slint model + `library-display` (via new `upsert_library_bucket`, with §0 re-anchoring) if that grid+view is open. **Phase 5** folded into the same pass: any `BoxSet` in the batch triggers `get_boxset_items` to reconcile `movie_collections` (upsert current members, drop stale ones for that box set). **Phase 2 turned out to already be free**: `fetch_home_data` was already called unconditionally on every debounce cycle and already re-fetches Recently Added/Favorites/Continue Watching/Not Watched/Recently Played Albums/Playlists correctly sorted from the server — a bespoke client-side insert-by-date path would just be overwritten by it moments later, so no code was needed beyond making sure that fetch actually runs when it should (see Phase 3). **Phase 3**: `UserDataChanged` previously never triggered any home-row refresh at all — favoriting/resuming something from another client left Favorites/Continue Watching stale until the next unrelated `LibraryChanged` event or restart. Now: immediate, fetch-free removal (`remove_from_dynamic_rows` on played/position=0, new `remove_from_favorites` on unfavorite) plus a transition-gated trigger (`row_has_id` checks the item isn't already in the target row) that invokes the same shared debounced refresh — avoids re-triggering a full `fetch_home_data` on every playback-position tick, only on a genuine first transition. **Phase 4**: an added/updated episode's parent series doesn't necessarily appear in the same WS report, so its unplayed-count badge could go stale — episodes are bucketed separately and any series not already in the batch is fetched explicitly by `series_id`; Next Up itself needed no bespoke logic (same free-via-`fetch_home_data` reasoning as Phase 2). **Phase 6**: episodes in the batch upsert (re-sorted by `index_number`) into any already-cached `series_episode_cache[season_id]` entry, and — new `sync_open_episodes` — into the live `series-episode-cards` model + `FjordState.series_episode_items` if that exact series+season is the one currently on screen (series screen episode row or the season detail overlay, which share the same model), re-anchoring `series-focused-ep`/`season-focused-ep` by id. Scope note: the plan's suggested "reload the whole season screen" fallback for a focused-item-removed case was implemented as a clamp instead (matching the library-grid/home-row precedent) rather than threading `tokio::runtime::Handle` through `remove_item_from_all_models`'s two call sites just for that one path — the dangerous "wrong episode silently selected" failure mode this whole phase targets is fully closed either way; a real network re-fetch-and-reset is a smaller follow-up if still wanted. Verified via `cargo check`/`clippy` (clean) and running the built binary against the live Jellyfin server (stable, WS connects, no panics); full interactive live-editing verification (add/rename/favorite an item from another client while the affected screen is open) needs either the user's own test pass or explicit sign-off to mutate real server data from this session, so it's flagged as a follow-up rather than done unilaterally. |
| 90 — Fix: WS UserDataChanged removal condition backwards for Not Watched rows (2026-07-09) | User reported live-testing Phase 89: favoriting 2 items from the Jellyfin web UI made the dashboard flash — appeared favorited, briefly not, then favorited again. Added temporary diagnostic logging first (per-item WS payload, transition decisions, debounce gate acquire/skip, `fetch_home_data` landing counts — all `info!` so they land in `fjord.log` without `RUST_LOG`) rather than guessing at a fix. While re-deriving the removal condition from first principles to investigate a specific angle, found a confirmed logic bug independent of needing a repro to prove: `remove_from_dynamic_rows`'s trigger in `ws.rs` was `played || pos_ticks == 0`, applied uniformly to both Continue-Watching-style rows (correct: leaves when finished OR reset to 0) and Not-Watched-style rows, which have the OPPOSITE membership rule (untouched = position 0) — so `pos_ticks == 0` alone fires on *any* `UserDataChanged` for a never-watched item, including a pure favorite toggle (position is naturally 0 regardless of what changed), spuriously stripping it out of Not Watched/`unwatched_collections` precisely when it's in the state that belongs there. New `remove_from_continue_watching` (`context_menu.rs`) filters only Next Up/Continue Watching rows, not Not Watched; `ws.rs` now calls `remove_from_dynamic_rows` (all rows) only when `played == true`, and the narrower function when only `pos_ticks == 0`. Existing call sites (mark-played in `context_menu.rs`/`main.rs`) were unaffected — they only ever fire on `played == true`, where both functions behave identically; the bug was specific to the new WS-triggered position-based branch. This closes a real, confirmed bug either way; whether it's *the* mechanism behind the reported favorite-flash (vs. the still-suspected overlapping-debounced-refresh race — the gate's `pending.store(false)` fires before the fetch it guards actually completes) remains to be confirmed by the user's next repro with the new logging in place. |
| 91 — Fix: WS delta refresh flashed every poster in a home row (2026-07-09) | Follow-up to Phase 90's live test on a debug build (`PKGBUILD` temporarily switched to `cargo build`/`target/debug` for faster local iteration, not committed): favoriting still visibly cleared and reloaded posters, just slower/more visible than a release build. Real fjord.log data showed the underlying WS behavior was actually correct — the same item genuinely toggled favorite false→true→false→true across ~50s (confirmed via `favorite_movies` counts on each landed refresh), likely repeated clicks in the Jellyfin web UI, not a Fjord bug. But each triggered refresh called `push_home_data`, which wholesale-replaces every row (Continue Watching/Next Up/Recently Added/Not Watched/Favorites/Recently Played/Playlists) with fresh `CardItem`s that have no poster data at all — posters only reappear once `spawn_poster_loading` decodes them afterward. Phase 1 already solved this exact problem for the six flat library lists (`upsert_cards_in_model` preserves posters for cards that already exist) but it was never applied to home rows. New `refresh_row_preserving_posters` (`home.rs`) rebuilds a row strictly from the fresh server data (so removed rows still disappear, unlike a plain upsert) but carries over the old row's decoded poster for any id that persists across the refresh; new `push_home_data_preserving_posters` applies it to all 16 home-row setters and replaces the `push_home_data` call in `ws.rs`'s delta-sync task. `push_home_data` itself is untouched — cold-start/login/retry call sites have no prior on-screen poster state worth preserving. Now a refresh only needs to decode posters for genuinely new rows, not the whole dashboard. |
| 92 — Screen-change + poster fade-in animations (2026-07-09) | User asked for smoother navigation. New `FadeInTrigger` (`widgets.slint`): a non-visual helper instantiated as `fade := FadeInTrigger {}` alongside `opacity: fade.fade-opacity;` + `animate opacity {...}` on the host element, fading it from 0→1 shortly after creation via a 16ms one-shot `Timer` (not `init =>` — the write needs to land on a genuinely later event-loop tick than the initial `opacity: 0` binding, or there's nothing for `animate` to transition from). Applied to: `MediaCard`'s poster `Image` (200ms — covers both first load and, per the same architecture, any card whose poster wasn't preservable across a WS-triggered row replace, Phase 91) and, in `main.slint`, every `if`-gated screen/overlay (150ms) — dashboards, library grid, browse, settings, detail/series/season/collection/album/artist/person, Now Playing, context menu, playlist picker, queue panel, lyrics. Deliberately excluded: `PlayerScreen` (video start latency is too perception-sensitive for an artificial opacity ramp) and the two docked bars, `MiniPlayerBar`/`MusicPlayerBar` (they share `total-bar-h` with the content area's height calculation, so fading their opacity alone without also delaying the resulting layout shift would look like a gap, not a fade). **Bug found and fixed during the same pass** (via an actual screenshot showing the whole dashboard stuck solid black): `FadeInTrigger`'s first version wrote `parent.opacity = 1` directly inside its own `Timer.triggered`, but `parent` from the Timer's perspective is `FadeInTrigger` itself (one level up), not the host screen two levels up — the write silently no-oped and left every screen stuck at `opacity: 0` forever, with no compile error since `FadeInTrigger` has its own built-in `opacity` property that type-checks fine. Fixed by exposing `out property <float> fade-opacity` instead and having each host bind to it by name, which works regardless of nesting depth. Also hit and fixed along the way: reusing the same `fade := FadeInTrigger {}` id text across multiple sibling `if` blocks is a compile error (ids aren't scoped per-conditional in Slint) — each of the 26 usages in `main.slint` needed a unique id (`fade1`..`fade26`). Verified via a real screenshot (window activated through a small KWin/qdbus script, since no `wmctrl`/`xdotool` was available) showing the Home dashboard rendering correctly post-fix. |
| 93 — Fix: dashboard/TV posters flashed on every cold start, even with nothing changed (2026-07-09) | User asked why posters still flashed on first launch with a warm cache and no server-side changes at all. Root cause, found by reading `spawn_auto_login`: cached data paints instantly (`push_cached_data`, correct), but then — unconditionally, on every single login, by design (the WS can't retroactively report changes made while Fjord was closed, per JELLYFIN.md's own don't-rely-solely-on-WS guidance) — `fetch_home_data()` + `client.get_all_series()` re-fetch everything, and applying that result still went through the old, non-poster-preserving `push_home_data` / a bare `items_to_model`, wholesale-replacing every row with poster-less cards before `spawn_poster_loading` redecoded them — the exact same flash mechanism as Phase 91, just from the startup fetch instead of a WS event. `refresh_row_preserving_posters` made `pub(crate)` (`home.rs`) and reused directly for `all_series`; `spawn_auto_login`'s home-row apply switched from `push_home_data` to `push_home_data_preserving_posters`. Movies/Collections/Artists/Albums/Playlists are unaffected — those are lazy per-session (`*_fetched`-flag-guarded), only refetched on first grid open, not at pure startup. |
| 94 — Fix: library-grid posters still flashed on first open per session (2026-07-09) | User reported posters still flashed on first entering the Home dashboard and library grids even after Phase 93. Found four more unfixed call sites, all in `spawn_library_fetch` (the lazy "first grid open this session" background refresh gated by `*_fetched` flags): Movies, Collections, Artists, Albums, and Playlists all applied their network result via a plain `items_to_model` — the same wholesale poster-less replace as Phases 91/93, just triggered by the first-open fetch instead of the WS/startup one. TV (`nav==1`) was already unaffected — it does no separate network re-fetch here at all, relying entirely on the startup refresh Phase 93 already covers. All five switched to `refresh_row_preserving_posters`. Home dashboard itself has no code path left that isn't already covered by Phase 91 (WS delta-sync) or Phase 93 (startup) — if it's still flashing after this fix, that's a distinct, not-yet-found path and needs a fresh report rather than another guess. |
| 95 — Fix: refreshing a row rebuilt the whole model, re-triggering every card's fade-in (2026-07-09) | User reported posters still flashed ~3-5s after launch even on a warm cache with nothing server-side changed. Added timing logs to `spawn_poster_loading` first rather than guess again — they showed poster decode itself completes in ~70-80ms both at cache-push and at the network-refresh landing (confirmed on a real release build), ruling out slow/duplicated decode as the cause. The actual mechanism: `refresh_row_preserving_posters` (Phase 93/94) and `upsert_cards_in_model` (Phase 1) both always returned a **brand new** `ModelRc`, even when every row's data (poster included) was correctly carried over — but swapping a Repeater's underlying model instance makes Slint destroy and recreate every delegate element, including each card's poster `Image`, which re-triggers Phase 92's `FadeInTrigger` fade-in from scratch regardless of whether the poster itself changed. So the poster-preservation fix genuinely worked at the data level, but Phase 92's fade-in (added in the same session) turned "rebuild an identical model" into a visible flash again. Fixed both functions with a fast path: if the new data has the exact same ids in the exact same order as the old model (the overwhelmingly common "nothing changed" case, confirmed via a new `same_shape` log — 13 of 16 home rows and the full 184-item `all_series` list took it on a real run), mutate the **existing** model row-by-row via `set_row_data` instead of returning a new one, so the Repeater never destroys the elements and the fade never re-fires. Only a genuine membership/order change (a real content difference between cached and fresh data) falls back to a full rebuild — which is correct, since that row's content actually did change and a fade there is expected. `sync_open_episodes` (Phase 6) has the same underlying pattern but wasn't touched — much lower trigger frequency (only fires when an episode changes AND that exact season is on screen), flagged as a known follow-up rather than silently left unmentioned. |
| 96 — Consolidate card-model apply into one shared primitive (2026-07-09) | Follow-up to Phase 95: user reported the flash was still happening, seemingly twice for some rows, and pushed back on scoping the review down to "just the poster flash" — pointed out that opening a detail screen shows a loading spinner and flashes every time, even the second time, suggesting a broader "every fetch-and-show path was written independently" pattern (see `[[plan groovy-stirring-phoenix]]`, Part 1). Direct code reading found the actual remaining cause: three more functions — `poster.rs::push_decoded_section` (home rows), `poster.rs::push_decoded_series` (`all_series`/TV), `movies.rs::push_library_cards` (Movies/Collections/Artists/Albums/Playlists) — build a fresh `Vec<CardItem>` once poster bytes finish decoding and always call `ModelRc::new(VecModel::from(items))` unconditionally, with no same-shape check. All three run **twice** at startup (once from the cache-based decode, again from the redundant network-triggered re-decode of the same posters), and it's the second pass that flashes: even though Phase 95's metadata-merge fix already correctly preserved the poster data, this poster-decode layer still swapped the model wholesale, destroying/recreating every card element and re-firing `FadeInTrigger`. New shared `apply_cards_preserving_identity` (`main.rs`, next to `item_to_card_item`/`items_to_model`) centralizes the same-shape/mutate-in-place logic Phase 95 introduced; `push_decoded_section` (via new `get_section_model` read-side counterpart to `push_section_model`), `push_decoded_series`, `push_library_cards` (via new `LibraryKind::get_all`), `home.rs::refresh_row_preserving_posters` and `refresh_favorites`, `context_menu.rs::upsert_cards_in_model`, and `ws.rs::sync_open_episodes` all switched over — five near-duplicate "build cards, rebuild model" implementations now share one apply step, each keeping only its own domain-specific merge logic. Verified on a real release build: `all_series` (184 rows), `all_movies` (607 rows), and every home section now hit `same_shape=true` on **both** the cache-decode and network-refresh passes — no second rebuild, no second fade. Part 2 of the same plan (in-memory per-item cache for Detail/Series/Season/Collection/Album/Artist/Person, so reopening an already-viewed item skips the loading spinner) follows as a separate, larger increment. |
| 97 — Fix: library-grid still flashed — library-display rebuilt independently of all_X (2026-07-09) | User retested Phase 96 with targeted logging: Home/TV confirmed fixed, but Movies flashed twice, Collections once, Music twice. New logs (bumped `apply_cards_preserving_identity`'s diagnostic from `debug` to `info` — the installed package doesn't run with `RUST_LOG=debug`, so Phase 96's own logging was invisible in the shared log the whole time) showed every single library apply hitting `same_shape=true` — the Phase 96 fix was working correctly for `all_movies`/`all_collections`/etc. The actual remaining cause: `browse::refresh_library_display` builds `library-display` — the model the grid *actually renders*, separate from the `all_X` backing lists — via a plain `ModelRc::new(VecModel::from(items))` on every call, with zero identity preservation, and it's called unconditionally from three places: grid-open, the network-fetch-landing handler, and the poster-decode-completion handler. So even with `all_movies` itself perfectly preserved underneath, `library-display` still got wholesale-rebuilt up to 3 times per grid session (TV has one fewer call site, hence the milder symptom; Collections' single observed flash vs. Movies'/Music's two is down to exactly which of those three call sites landed while the grid was actually visible). Fixed the same way as every other Phase-96 site: compute the final sorted+filtered `Vec<CardItem>` first, then apply it via `apply_cards_preserving_identity(&g.get_library_display(), final_items)` instead of an unconditional rebuild — genuinely different content (a new grid opened, or Shuffle's intentionally-random order) still rebuilds correctly; nothing else does. |
| 98 — Fix: poster-decode functions always built a brand-new Image even when unchanged (2026-07-09) | User confirmed Phase 97 reduced the flash to once per grid, but pushed back that it still wasn't the expected switch-tabs flash — reopening a settled Movies/Collections/Music grid showed everything correctly, then ~1s later flashed all posters anyway. Tracing the real log (all applies now correctly showing `same_shape=true`) surfaced a further, more subtle bug: `movies.rs::push_library_cards` and `poster.rs`'s `push_decoded_section`/`push_decoded_series` always build a **brand-new** `slint::Image::from_rgba8(...)` from freshly-decoded bytes, even for a card whose poster hadn't changed at all. `apply_cards_preserving_identity` only avoids destroying/recreating card *elements* when ids/order match — it says nothing about whether the `Image` *value* bound to `source:` is the same object. Swapping every visible card's texture for a *different-but-pixel-identical* `Image` handle, all in one synchronous batch (607 cards for Movies), is itself visibly disruptive even with zero element recreation — `refresh_row_preserving_posters`/`refresh_favorites` already avoided this (prefer the existing poster over a fresh redecode) but the three lower-level poster-decode functions never got the same treatment, since they're the ones that *produce* the decoded image in the first place rather than merging one. Fixed all three the same way: look up the row's existing `CardItem` by id in the model being applied against, and if it already has a poster, reuse that `Image` handle directly instead of decoding a new one — a fresh decode is now only used for a genuinely new poster (nothing existing to reuse). Per a later user report, only Movies/Collections/Music (routed through `movies.rs`) were visibly affected; TV (`poster.rs::push_decoded_series`) got the same fix anyway for consistency, since it's the identical class of bug. |
| 99 — Fix: push_library_cards set library-display to raw unsorted order, bypassing the sort (2026-07-09) | User reported the flash persisted after Phase 98 and asked directly what TV does differently, since it alone never flashed. Rather than keep guessing, added `#[track_caller]` to `browse::refresh_library_display` (logging the exact call-site file:line of whichever of its ~15 callers fired) and to `update_library_filter`, then had the user retest and read the resulting log. Every mismatched rebuild for Movies/Collections/Music traced to `update_library_filter("")` — the `library-search-clear` callback, which fires whenever a `NavItem` is double-clicked to open a grid or the grid is backed out of — landing a few seconds after the grid had already settled, with `library-display`'s current content ("old") disagreeing with a fresh sort of the same underlying `all_X` data ("fresh") despite identical length. Root cause: `movies.rs::push_library_cards` (Movies/Collections/Artists/Albums/Playlists) sets `library-display` **directly** to the just-applied `model` — which is in whatever raw order the network/decode pipeline produced, never passed through the sort/filter/query pipeline at all — whereas `poster.rs::push_decoded_series` (TV's equivalent landing point) has always correctly called `browse::refresh_library_display(&w)` instead. So TV's grid was sorted from the very first paint; the other five briefly showed Jellyfin's own raw order, then visibly reshuffled into alphabetical order the next time anything (a nav double-click's search-clear, a sort-bar apply, a WS delta) called `refresh_library_display` — which for most items looks identical to raw order and so goes unnoticed, but for any title where Jellyfin's own ordering and Rust's `.to_lowercase()` comparator disagree (articles, symbols, accents), the sorted position differs and the whole grid's stable-sort tie-breaking shifts, reordering far more than just that one item. Fixed by routing `push_library_cards` through `browse::refresh_library_display(&w)` exactly like TV does, instead of a direct `set_library_display`. The two `#[track_caller]` diagnostics are left in place — cheap, and exactly the kind of "which of many call sites actually fired" question that's come up repeatedly across Phases 96-99. |
| 100 — Fix: Collections/Artists/Albums unplayed-count badge flashed in then vanished (2026-07-09) | Follow-up report right after Phase 99 landed: on the Collections library grid, a card's "N unwatched" badge briefly appeared then disappeared. Traced to a threading gap, not a display bug: `main.rs::item_to_card_item` (used by `refresh_row_preserving_posters`, which `spawn_library_fetch`'s network-metadata-landed branch calls) correctly copies `unplayed_count` from `MediaItem.user_data.unplayed_item_count` — Jellyfin populates this for any folder-like item, BoxSets included, not just Series — so the badge legitimately appears once that merge lands. But `movies.rs`'s `meta`/`decoded` tuples (`spawn_library_poster_loading`/`push_library_cards`, the *other* landing point that fires moments later once posters finish decoding) never carried an unplayed-count field at all — `CardItem::default()` leaves it at 0 — so that pass's `apply_cards_preserving_identity` mutate-in-place `set_row_data` overwrote the just-correct badge back to 0, even though the row's id/order hadn't changed. `poster.rs::push_decoded_series` (TV) never had this gap; its `SeriesMeta` tuple already threads `unplayed_count` through end to end. Fixed by adding an `i32` field to both the `meta` and `decoded` tuple types in `movies.rs` (sourced from `i.user_data.unplayed_item_count`) and setting `h.unplayed_count = upc` in `push_library_cards`'s `CardItem` construction, mirroring `push_decoded_series` exactly. Same underlying lesson as Phase 99: two code paths writing "the same" `CardItem` needed to actually carry every field through, not just the ones each author happened to think about — Artists/Albums (also folder-like, also carry a real `UnplayedItemCount`) get the same fix for consistency and may start showing a legitimate badge they were silently missing before, not just Collections. |
| 101 — Demote flash-hunt diagnostic logs to debug! + add a Settings log-level dropdown (2026-07-09) | Follow-up to confirming Phases 99-100 fixed: user asked whether the diagnostic logging added while chasing the library-grid flash bugs should be trimmed or demoted now that the bugs are found, and whether a Settings-exposed log-level control would help future troubleshooting the way it would have helped earlier this session (had to bump a log from `debug!` to `info!` mid-session because the installed package doesn't run with `RUST_LOG=debug`, and a shared log couldn't show debug detail without a source change + rebuild). Agreed on both. Demoted to `debug!`: `apply_cards_preserving_identity`'s two log lines, `spawn_poster_loading`'s three timing logs, `push_library_cards`'s apply log, `refresh_library_display`'s `#[track_caller]` log, `update_library_filter`'s `#[track_caller]` log, and `spawn_library_fetch`'s six per-type "network fetch landed"/"re-decoding" logs — all fire many times per session and were diagnostic-only; kept at `info!`: startup/login/WS-connect one-shot lines, warnings/errors, "settings saved". Added `Config.log_level: String` (`"error"`/`"warn"`/`"info"`/`"debug"`, default `"info"`) — `main()` now does a cheap early `load_config()` read (before `state`/`window` exist, well before the full config load that already happens later) to build both the console and file `tracing_subscriber::EnvFilter`s from it, `RUST_LOG` still overriding either when set; a value change takes effect on next launch, not live, since the subscriber is a one-time `init()`. New Settings → General row 2 ("Log level", between Video-in-background and the border/Sign-Out; `GEN_LOG_LEVEL`, `LOG_LEVEL_MODEL`) shifts `GEN_SIGN_OUT` from row 2 to row 3 — wired through `dropdown_model`/`current_value_str`/`apply_dropdown_selection`/`settings_row_action` and `apply_settings_to_window`/`read_settings_from_window` exactly like every other dropdown setting. |
| 102 — Part 2: in-memory screen-open caches for Detail/Series/Season/Collection/Album/Artist/Person (2026-07-09) | User confirmed ready to resume Part 2 of `[[plan groovy-stirring-phoenix]]`, paused earlier this session after Part 1 (the card-model-apply consolidation, Phases 96-101) shipped and was verified. Original complaint: every detail-style screen shows a loading spinner and flashes on every open, even the second time reopening an item just viewed — no caching existed anywhere for the `get_item_detail`/similar/collection-items/artist-albums/filmography/track-list fetches that gate these screens. Re-scoped the plan's design during implementation after re-reading all 7 screens' actual fetch code: rather than caching Slint-ready state (impossible — `slint::Image`/`CardItem`/`CastMember` are `!Send`, and `FjordState` must stay `Send` since `Arc<Mutex<FjordState>>` already crosses `tokio::spawn` boundaries everywhere), cache the **raw API response types** (`MediaItem`/`Vec<MediaItem>`, both plain `Clone + Send` structs) and skip only the network round-trip on a hit — the downstream poster/backdrop/cast-portrait fetch-and-decode stays exactly as it was (already disk-cached from a prior view, so fast in practice), just no longer gated behind a real HTTP round-trip. New `config.rs::BoundedCache<V>` (FIFO cap 40 + 5-minute TTL) backs six typed `FjordState` fields, each shared across every screen that makes the matching call: `item_detail_cache` (`get_item_detail`, all 7 screens), `similar_items_cache` (`get_similar_items`, detail+series), `boxset_items_cache` (`get_boxset_items`, detail+collection), `artist_albums_cache`, `person_filmography_cache`, `container_tracks_cache` (`get_album_tracks`/`get_playlist_items`, shared since both return the same `Vec<MediaItem>` shape). TTL (not precise per-mutation invalidation) is the deliberate correctness net — the plan's original design called for invalidating on every played/favorite mutation across a large surface of call sites, but this session's Phases 96-101 flash-bug hunt kept re-learning the same lesson about "refresh in the background" paths growing subtle bugs; a stale hit here just self-heals within 5 minutes instead. Per-screen: each `open_*` function now does one synchronous cache peek (before spawning any async work) and only sets `app-content-loading=true` on a miss — a hit skips the loading-spinner overlay entirely, since the remaining work is fast enough to feel instant against whatever screen the user was already on. Each async fetch task wraps its cacheable call in `async { if let Some(v) = cached { return Ok(v); } client.get_x(&id).await }` so `tokio::join!` still runs everything concurrently; a successful fetch (cached or fresh) is written back to the cache, refreshing its TTL. `detail.rs`/`series.rs`/`collection.rs`/`artist.rs`/`person.rs`'s card-model rows (similar/collection/albums/filmography) also switched from a blind `ModelRc::new(VecModel::from(...))` rebuild to `apply_cards_preserving_identity` while in the area — free consistency with Phases 96-99's own lesson, applied to the one family of screens that hadn't gotten it yet. Two screens got a smaller slice than the full pattern, both deliberate: `season.rs` only caches its own `get_item_detail` (episodes are already covered by the pre-existing `series_episode_cache`, unrelated to this work); `series.rs`'s `SeriesCtx::spawn_main` also caches only `get_item_detail` — its seasons list + first-season-episodes fetch were left always-fresh since they interact with the CR10-20 stale-fetch guards (`series_open_id`, `series_episode_cache` cleared on every open) and caching them risked reintroducing exactly the class of subtle bug those guards exist to prevent; `spawn_next_up` (inherently dynamic — must reflect current watched state) was never a candidate for caching at all. `person.rs::open_person_screen`'s signature changed from taking `client` directly to taking `state` (needed for cache access), updated at its one call site in `main.rs`. Verified: `cargo build --workspace`/`test --workspace` clean, `cargo clippy` shows the same 54 pre-existing warnings as before this change (nothing new in any of the 9 touched files), and a real release-build smoke test. |
| 103 — Persist the six screen-open caches to disk + route WS-driven invalidation through them (2026-07-09) | Follow-up to Phase 102 (Part 2): the in-memory-only caches meant every fresh launch started cold, so the first open of anything was still a real fetch + spinner. User asked why not persist to disk and guard against staleness instead of discarding every restart, then pushed further: Jellyfin's WebSocket already reports exactly which item IDs changed (`LibraryChanged`/`UserDataChanged`), and the app's existing WS delta-sync (`ws.rs`) already uses that to patch just the changed IDs into the flat lists rather than a full re-fetch — so why not reuse that instead of a TTL? Investigated before committing to the design: Jellyfin has no "what changed since I was last connected" API (confirmed via `JELLYFIN.md` — no sync token; the WS itself is documented there as unreliable, only reports events while connected), so there's no way to catch up on a gap after being closed except by asking again — but *while running*, the existing delta-sync plumbing is exactly the right mechanism to reuse. Implementation: `BoundedCache<V>` (Phase 102) dropped its TTL/`Instant` entirely in favor of this; gained `Serialize`/`Deserialize`/`Clone` derives. New `config.rs::ScreenCachesFile` wraps all six caches into one `~/.cache/fjord/screen_caches.json` (one file, not six, since they're always loaded/saved together) — loaded in `push_cached_data` alongside the other warm-start caches, saved every 60s by a new repeating `slint::Timer` (`wire_screen_cache_save_timer`, mirroring `wire_nw_timer`'s shape; always writes unconditionally rather than tracking a dirty flag, since the write is cheap). New `fjord-api::get_items_by_ids_detailed` (mirrors `get_items_by_ids`'s existing chunk-of-200 fan-out, but with the richer Fields list `get_item_detail` returns by default — `get_items_by_ids` stays untouched, so the WS delta-sync path that depends on its lighter payload is unaffected). New post-login `spawn_screen_cache_refresh` (fired once after every login, alongside the existing home-data/series/WS-start burst): refreshes `item_detail_cache` via one batched `get_items_by_ids_detailed` call (removes any key the response doesn't return — a deleted item); the 5 relationship caches trickle out under a shared `Semaphore(2)` since no batch endpoint exists for those, deliberately lower concurrency than a user-initiated action since this one is unprompted and must never compete with foreground use. WS-driven invalidation reuses the existing delta-sync machinery directly rather than inventing a new mechanism: `LibraryChanged`'s existing removed-ids purge block gained six more `.remove()` calls (one per cache); `maybe_spawn_delta_refresh`'s existing `get_items_by_ids(upsert_ids)` batch call gained a parallel `get_items_by_ids_detailed(upsert_ids)` piggybacked onto the same already-happening fetch, refreshing `item_detail_cache` for a genuine delta (only what actually changed) plus invalidating (not re-fetching) any of the 5 relationship caches keyed by an upsert id, since the event doesn't distinguish "metadata changed" from "membership changed" for a container; `UserDataChanged`'s existing per-item `update_item_user_state` loop gained one `item_detail_cache.remove(id)` (played/favorite state lives in the cached item too; cheap invalidate, no extra network call, self-heals on next open). Verified via a real release build with `Config.log_level` (Phase 101) set to `debug`: confirmed genuine `cache_hit=true` on reopening a series and a person across a full app restart — not just within one session, as Phase 102 alone provided. |
| 104 — Opt-in one-time library prewarm: metadata + backdrops/portraits, with two real cap bugs found and fixed via live testing (2026-07-09) | Follow-up to Phase 103: user asked why the screen-open caches only populate lazily rather than proactively, given a future concern about browsing feeling bad on a slow connection. Discussed and refined scope with the user before implementing (see `[[plan groovy-stirring-phoenix]]`): `item_detail_cache` is cheap to prewarm fully (one batched `get_items_by_ids_detailed` call per 200 items); the 5 relationship caches have no batch endpoint, so prewarming those means one request per boxset/artist/person/album/playlist — the user chose to include this anyway, understanding it's a real, not free, one-time cost. Extended further at the user's prompt: grid posters are *already* prewarmed today (pre-existing `spawn_movies_poster_loading`/etc behavior); the real remaining image gap is backdrops + cast portraits, both currently fetched only per-screen-open. Split into two independent, user-triggered actions per the user's request (separate toggles so metadata-only or images-only is possible): new `prewarm.rs` with `spawn_metadata_prewarm` (item_detail_cache for all top-level library items, batched; derives unique cast-member ids from the results and prewarms their own detail + `person_filmography_cache`; then `boxset_items_cache`/`artist_albums_cache`/`container_tracks_cache`/`similar_items_cache`, each rate-limited under a shared `Semaphore(6)` since no batch endpoint exists) and `spawn_image_prewarm` (backdrops + portraits for whatever's currently in `item_detail_cache`, independent of whether metadata prewarm has run — reuses the existing tag-revalidated/disk-check-first fetch functions, so a re-run is naturally incremental). Two new Settings → General rows ("Prewarm library metadata" / "Prewarm library images"), button-style like Sign Out, with a live status line driven by new `FjordState.prewarm_*` progress fields read by a 1s `AppState`-updating timer (decouples UI updates from the actual fetch rate). Added explicit **cost logging** per the user's request ("add logging for the cost so I can see what it is") — both actions track request counts by category and wall-clock elapsed time, logged as one `info!` summary line on completion, so the cost is observed fact, not a guess. Also captured a side quest during discussion: the user's Jellyfin server showed sustained 47-66% CPU across all 8 cores during the metadata prewarm, initially mistaken for ffmpeg (the `--ffmpeg=/path` in the process command line is just a startup flag, not evidence of ffmpeg running — htop was showing per-thread rows of the single multi-threaded Jellyfin server process); the real cause, confirmed from the cost log itself, is that 10,609 of 11,731 requests (90%) were `get_person_filmography` calls, one per unique credited person — a reverse lookup (person → items) that's a fundamentally more expensive query pattern than a direct `/Items/{id}` fetch regardless of Jellyfin's own metadata caching. **Two real bugs found via live testing before this ever got committed:** (1) `BoundedCache`'s FIFO `cap` (40, sized for "recently viewed items") was never raised for the prewarm sweep — confirmed via a real run (11,731 requests made, `person_filmography_cache` still showed exactly 40 entries afterward on disk) that nearly the entire sweep's results were evicted as fast as they were inserted. Fixed: `cap` changed from `#[serde(skip)]` to genuinely persisted, new `BoundedCache::set_cap` (raise-only) called by `spawn_metadata_prewarm` before inserting, sized to fit what's about to be fetched. (2) Even after fixing (1), the user asked "is this really one-time?" — it wasn't: Phase 103's *ambient* per-login background sweep (`spawn_screen_cache_refresh`) iterated *every* key in each cache with no limit, so once the cap-fix let a cache grow to fit the whole library, that same unprompted sweep would repeat the prewarm's full request volume on *every future login*, not once. Fixed: new `BoundedCache::recent_keys(n)` + `AMBIENT_REFRESH_LIMIT` (40) caps the ambient sweep to its original "recently used" scope regardless of actual cache size — the opt-in prewarm button is now the only path that touches the full library. Re-verified end-to-end after both fixes: ambient sweep on a fresh login processed exactly 40 keys and finished in ~27s (not minutes); re-running metadata prewarm again reproduced the identical 11,731-request breakdown; all six on-disk cache entry counts now correctly matched what was actually fetched (11,671 / 791 / 102 / 46 / 10,609 / 123, matching cap); image prewarm's coverage jumped from 7 backdrops + 120 portraits (before the fix, operating on a mostly-evicted cache) to 903 + 10,609 (after, operating on the fully-retained one) — end-to-end confirmation the whole pipeline now works as designed. Deliberately out of scope: seasons/episodes (consistent with Phase 103's own CR10-20-driven exclusion), a cancel/interrupt control, and auto-retriggering on new library content (WS invalidation + the button re-run cover that). |
| 105 — Universal press/click animation (mouse + keyboard) + fade/transition consistency pass (2026-07-09) | User asked for a "click animation" that works identically for mouse and keyboard, since Fjord is a 10-foot/remote-control-first UI. Investigated Slint 1.16's compiler source directly (`i-slint-compiler-1.16.1/typeregister.rs`/`builtins.slint`) to confirm reserved, always-available `transform-scale`/`transform-origin` properties exist on every element (same category as `opacity`), which the first implementation used for a scale-down "press" bounce (mouse via `TouchArea.pressed`, keyboard via a new shared `AppState.kb-activate-pulse` counter bumped once centrally in `keys::handle_key()` on every Enter press, consumed by a new `widgets.slint::PressPulse` non-visual helper — same one-shot-`Timer` idiom as the existing `FadeInTrigger`). Bundled in a `[[plan groovy-stirring-phoenix]]`-driven fade-consistency pass while auditing `main.slint`'s existing `FadeInTrigger` convention: `VolumeOverlay`/`ToastNotification` and player's skip-segment/skip-timed/Up-Next-banner overlays were the only instantiation sites without the same 150ms fade-in every other overlay already had; `NavItem`'s active-tab background gained `animate background` to match `ToggleSwitch`'s existing pattern; every focus-ring `border-width` across the widget library gained `animate border-width` (folded into the same edit pass already touching those components for the press-pulse work). **Three real bugs found via live user testing before this shipped, all traced to the same root cause — `NavItem`'s pre-existing `kbd-focused` prop is a coarse, GLOBAL "sidebar has keyboard focus" proxy (every sidebar instance binds it to the identical `AppState.focused-section < 0` in `layout.slint`), correct enough for a static background highlight but wrong once exposed through a reactive pulse:** (1) every sidebar item flashed simultaneously on Enter, not just the active one — fixed by gating the pulse on `active && kbd-focused` together (matching the exact condition the background already used to single out one row); (2) the active sidebar item (and the bespoke Quit row, which had the identical bug independently) kept pulsing on every Enter press anywhere in the app — including deep in Library/Browse/Settings/Detail/etc, where `focused-section` is simply stale/irrelevant — fixed by adding a new, narrowly-scoped `AppState.sidebar-kb-active` bool computed fresh in `keys.rs` from `active_mode() == AppMode::Dashboard && focused_section < 0` (the one authoritative condition under which the sidebar genuinely receives Enter), replacing `kbd-focused` as the pulse's gate while leaving the background's own (unrelated, unbroken) use of `kbd-focused` untouched; (3) user feedback that the scale-based press bounce made button/item **text** look like it was growing/brightening/bolding on release — since `transform-scale` scales an entire subtree including child `Text`, there was no way to "exclude the text" while keeping a shared scale transform, so the whole mechanism was reworked to be **border-only**: `PressPulse`'s consumer sites across all ~13 touched `.slint` files were converted from `transform-scale` to a `border-width` bump (widgets with an existing focus ring: 2px focus → 4px press; widgets with none: 0px → 3px; a handful of special cases — `ToggleSwitch`, `SettingsDropdown`, the skip-segment prompt, the alphabet-scrubber cells — sized proportionally), leaving text completely untouched on press; values were then bumped further after a second round of user feedback that the effect still felt too subtle. Elements with no border-capable container (`QueuePanel`'s "Clear All", `album.slint`'s ✕ remove button) got a small wrapping/underline treatment instead of touching their bare `Text`. Two more follow-ups from the same testing thread: (4) user asked why the transport/media glyphs (▶⏸⏭⏮ etc.) rendered as colorful yellow/orange emoji on the HTPC but plain monochrome on the dev machine — different systems' fontconfig fallback chains pick a color emoji font (e.g. Noto Color Emoji) for codepoints the primary UI font lacks a glyph for, and a color font always paints its own baked-in colors regardless of the requested `color:` property; user wanted source-level consistency rather than a per-machine font tweak, so every icon-style glyph across all `.slint` files (media transport controls, hearts, checkmarks, warning/info, volume) got a trailing U+FE0E (text-presentation selector) appended — this tells a compliant shaper to prefer a font further down the fallback chain that has a monochrome/outline glyph for that codepoint over jumping straight to a color-only font, which is honored by the standard fontconfig+HarfBuzz stack Linux desktops use. (5) User reported some focus/press borders appeared to render *behind* poster images ("posters or other things") — audited every widget touched by this phase for the specific risk pattern (a border on an outer `Rectangle` while an `Image` sits in a *separately nested* Rectangle flush to the same edges, with no inset gap) and found exactly one genuine instance: `MediaCard`, whose poster `Image` lives inside a separate `poster-rect` child Rectangle rather than the same Rectangle as the border — reused everywhere (dashboards, library grid, section rows), which is why it read as widespread. Every other widget touched either has no `Image` at all, or already uses the codebase's established known-good pattern (border + `clip` + `Image` all on the *same* Rectangle, as documented in `CastRow`'s own comment) or has enough padding/inset that the border and any image content can't spatially overlap regardless. Fixed by converting `MediaCard`'s border from a property on `root` to a dedicated, last-declared, transparent overlay `Rectangle` sized to `root`'s bounds — the same fix already established for `MiniPlayerBar`'s focus ring, which guarantees top-most paint order regardless of Slint's border-vs-nested-child compositing order. (6) User noticed the active sidebar tab was *still* showing fully lit while browsing a library grid — the (1)/(2) fix above had replaced the pulse's gate with `sidebar-kb-active`, but `NavItem`'s own background highlight (the actual visual the user was looking at) still read the old per-instance `kbd-focused` prop, so it hadn't been touched yet; switched it to `sidebar-kb-active` too and deleted the now-fully-unused `kbd-focused` prop entirely, including the identical `kbd-focused: AppState.focused-section < 0;` line at all 7 `layout.slint` `NavItem` call sites. First attempt kept `sidebar-kb-active` as a value Rust pushes only from inside `keys::handle_key()` (recomputed every keystroke, not just Enter, to make it safe for a continuously-rendered background) — but live testing then showed it went stale again after any *mouse-driven* screen change (e.g. clicking into the library grid never runs through `handle_key()` at all), so it kept reflecting whatever it was left at by the last keypress. Fixed properly by converting `sidebar-kb-active` from a Rust-set `in-out property` into a **pure Slint expression** (`out property`) mirroring `active_mode()`'s own condition chain directly off the same `AppState` flags (`show-context-menu`, `show-library`, `is-playing`, `focused-section`, etc.) — correct-by-construction for any input method, since those flags are set identically regardless of what triggered the change, at the cost of duplicating `active_mode()`'s condition list in two places (a new screen/overlay added to one needs the matching branch added to the other). Verified via `cargo build --workspace`/`cargo clippy` (54 pre-existing warnings, nothing new) and multiple real release-build sessions with live user testing between each fix. |
| 106 — MediaCard title-wrap fix, subtitle-clip fix, and a live "Recursion detected" crash found + fixed (2026-07-10) | Follow-up to Phase 105's poster-card border work. User reported poster titles were getting cut off instead of wrapping to a 2nd line — `MediaCard`'s title `Text` combines `overflow: elide` + `wrap: word-wrap` + a fixed `height: 30px`; confirmed via Slint's own text-layout source (`i-slint-core-1.16.1/textlayout/sharedparley.rs`) that Slint *does* support proper multi-line elide (renders as many full lines as fit within the given height, eliding only the last visible one) — the bug was simply that 30px wasn't tall enough to fit 2 real lines at this font-size with typical line-height metrics, so the wrapped 2nd line got squeezed out almost immediately. First attempt replaced the guessed constant with a self-measuring `title-line-sizer` probe (same `visible:false; height:0px` idiom as `LyricsView`'s `sizer`) computing `title-line-sizer.preferred-height * 2` — correct in principle, but user asked to keep it simple/non-dynamic instead, partly motivated by a bigger idea floated for later (dynamic *per-row* height shared across all cards in a row, so long titles could wrap to 2-3 lines without misaligning neighboring cards — deliberately deferred as a separate future feature, tied to a possible future poster-scale-slider setting; not implemented now). Settled on a plain fixed `height: 34px`. That surfaced a second bug: the subtitle line (year/date-range) started getting clipped at the card's bottom edge — `poster-rect`'s `root.height - 54px` budget was tuned for the old ~1-line title box and no longer left room for a real 2-line title *plus* the subtitle once the title box reliably grew to fit 2 lines; bumped to `root.height - 60px` (6px outer spacing + 34px title + 2px inner spacing + ~16px subtitle, with margin). Separately, user asked for the focus/press border (Phase 105) to be visually bigger since it was overlapping the poster edge and the title's first line; an attempt to outset the border ring 3px beyond `root`'s own bounds (rather than flush with them) was tried and **caused a live crash** — `thread 'main' panicked at .../i-slint-core-1.16.1/properties.rs:583:9: Recursion detected`, triggered during mouse-hover geometry evaluation — and independently got visibly clipped by the surrounding `Flickable`'s own viewport edge for edge-of-row cards, since scrollable containers don't reserve margin for content that intentionally overflows an item's own declared bounds. Reverted immediately back to the known-stable flush version (confirmed via live testing to no longer crash) — but the underlying overlap complaint was real and still needed fixing, so it came back once more ("now the hilight still cuts of the first letter in the title"). Fixed properly this time by inverting the approach entirely: instead of expanding the ring *outward* past `root`'s bounds, inset the poster+text *content* 3px inward within `root`'s existing (already-stable) bounds — the content `VerticalLayout` gained explicit `x:3px; y:3px; width:root.width-6px; height:root.height-6px`, and `poster-rect`'s height calc switched from `root.height - 60px` to `parent.height - 60px` to account for the now-smaller available height. The ring itself stays exactly flush with `root` (the configuration already confirmed crash-free), so this never reintroduces the recursion risk or the Flickable-clipping problem — the gutter comes from shrinking the content, not growing the ring. Final thread: user asked why Browse All, Settings, and Quit "look different" when navigating to them — traced to `active_mode()`'s priority chain: Home/TV/Movies/Collections/Music (nav 0-4) all share the same catch-all `AppMode::Dashboard` regardless of which is selected, so `NavItem`'s two-tier background (bright accent when `sidebar-kb-active`, muted otherwise, from Phase 105) stayed bright for any of those five merely by being cursor-highlighted; Browse All auto-opens `show-browse` the instant the sidebar cursor lands on it (pre-existing shortcut) and Settings is checked directly via `active-nav == 10`, so both flip `active_mode()` away from `Dashboard` immediately, dropping straight to the muted tier — a real, consistent behavior difference, not a bug, but one the user found confusing/inconsistent. Quit's distinct look is unrelated (intentional red tint, `#3a1a1a`, not built from `NavItem` at all). User preferred the simpler Browse/Settings/Quit look over the two-tier one; resolved by removing the two-tier distinction from `NavItem` entirely — `background` is now a single `active ? Theme.accent-muted : (hover ? Theme.surface-raised : transparent)` for every sidebar item, no longer reading `sidebar-kb-active` (which remains in use only for the press-pulse gate). Verified via `cargo build --workspace` and multiple live release-build sessions with user testing (including screenshots) between every step of this whole thread. |
| 107 — HTPC crash: MediaCard's content-inset geometry bindings caused a second "Recursion detected" panic; fixed with layout padding (2026-07-10) | User's first real HTPC session on r716.c8c6a5a crashed ~9 seconds after pressing Play Now on the Up Next banner — the shared `fjord.log` (repo root) shows the full chain: episode 3 started fine (first frame rendered), the WS `UserDataChanged` marked episode 2 resumable → `needs_refresh=true` → delta refresh fired 5s later → `fetch_home_data landed — continue_watching=3` (membership grew 2→3, triggering a repaint) → `PANIC: properties.rs:583: Recursion detected` on the very next line. Root cause: Phase 106's content-inset fix gave `MediaCard`'s inner content `VerticalLayout` explicit geometry bindings (`x:3px; y:3px; width: root.width - 6px; height: root.height - 6px;`) plus `poster-rect` reading `parent.height - 60px`. The fatal detail is the *instantiation site asymmetry*: `SectionRow` passes `height:` to `MediaCard` explicitly (a plain property — acyclic), but `LibraryGrid` passes only `width:`, so there `root.height` is assigned by the enclosing row `HorizontalLayout`'s **layout cache** — and an explicit-geometry binding on a child *layout element* that reads `root.height` feeds back into the very constraint computation that produces `root.height` → cycle → Slint's recursion detector panics. **Correction (caught in review, 2026-07-11):** this entry originally said the Continue Watching row's own model rebuild triggered the cycle — that doesn't hold up against the root cause two sentences later, since Continue Watching is a `SectionRow` and passes `height:` explicitly, exactly the "acyclic" case. The log only proves the `continue_watching` refresh was the last logged event before the panic, not that its `MediaCard`s were the ones that cycled; the far more likely trigger is a `LibraryGrid` screen left mounted-but-hidden behind the fullscreen player (its `MediaCard`s get only `width:`, never `height:`) whose layout got re-evaluated by the same repaint pass — not confirmed, since the log doesn't identify which mounted screen's `MediaCard` actually panicked. This is why the dev machine (where every live test of Phase 106 passed, including this exact scenario visually) never crashed and the HTPC did within hours — it needs the vulnerable instantiation (a `LibraryGrid`, not just any screen) plus a repaint-driven re-evaluation order. Same panic signature as Phase 106's outset-ring crash; the CLAUDE.md gotcha now covers both variants ("Part 2"), ending with the general rule: never put explicit `x/y/width/height` bindings derived from `root` geometry on a **layout element** inside a reusable component — some instantiation site will eventually size the component through a layout cache and close the cycle. Fix: the 3px inset now comes from `padding: 3px` on the `VerticalLayout` (a native layout property, resolved entirely inside the layout's own cache — no external geometry binding, the mechanism layouts are *designed* to inset with), and `poster-rect` returned to reading `root.height` directly (the month-stable pattern), with the constant adjusted 60px→66px so the rendered result is pixel-identical (text area still gets exactly 54px). Local release build clean; the real verification is the HTPC itself (the only machine that ever reproduced this), via makepkg + replaying the same flow: play an episode → Play Now on the Up Next banner → wait ~10s for the delta refresh to land. |
| 108 — Poster cache cleanup was deleting every prewarmed cast portrait (2026-07-10) | Spotted live while smoke-testing the Phase 107 fix: startup logged `poster cache cleanup: deleted 8346 orphaned file(s)` on the dev machine. `run_poster_cache_cleanup`'s known-ID set was built from the six flat library lists only (movies/series/collections/artists/albums/playlists) — but cast portraits are cached in `posters/` under **person** ids, which appear in none of those lists. So every 24h sweep classified all of them as orphans and deleted them — including the 10,609 portraits the opt-in image prewarm (Phase 104) had deliberately downloaded. The two features were silently fighting: prewarm pays the network cost once, cleanup throws the result away within a day, next prewarm run pays it again. (This was technically pre-existing, documented behavior — "portrait files outside the known set are re-fetched on next access" — but it only became a real cost once Phase 104 made portraits a deliberate 10k-file investment.) Fix: the cleanup spawn site in `main.rs` now also gathers `detail_ids` = every `item_detail_cache` key (person detail entries are keyed by person id after a prewarm) + every cached item's credited `people` ids (covers portraits cached by ordinary cast-row browsing even without prewarm), passed as a 7th parameter and chained into the known set. The empty-set skip guard deliberately still checks only the six flat lists — `item_detail_cache` is loaded from the persisted `screen_caches.json` and can be non-empty even when the library fetch failed, so counting it toward the guard would defeat the network-error protection. Season/episode thumbnails still fall outside the known set by design (re-fetched on next access, never a bulk investment). Recovery for already-wiped machines: re-run Settings → General → Prewarm library images. |
| 109 — Auto mark-played at the credits trigger, with a rewind-revert safety net (2026-07-10) | User asked why episodes advanced via the Up Next banner/Play Now showed up in Continue Watching instead of being marked watched — traced the code: `start_playback` tears down the old player and reports its raw position via `report_playback_stopped`, and Jellyfin applies its *own* percentage-complete heuristic to decide played-vs-resumable. Pressing Play Now right at `credits_start` lands at ~94-95% of runtime, which apparently didn't clear Jellyfin's own threshold in the observed case. User's proposed fix: explicitly mark the episode played the moment the credits trigger fires (same condition that already fires the Up Next banner/auto-advance), rather than relying on Jellyfin's position heuristic at all. Investigated first rather than implementing immediately: this exact behavior existed once before and was deliberately removed (`CR10-13`, comment still in `playback.rs`) because it stuck even when the user pressed Skip and kept watching from an earlier point — surfaced this history to the user before proceeding, since naively reimplementing it would reintroduce a known bug. User's refinement resolved the tension: mark played at the trigger as before, but also watch for a rewind back past that trigger point during the *same* episode and revert the mark if it happens — directly patches the exact scenario `CR10-13` was reverted for. Implemented via a new `VideoState.credits_auto_marked_played: bool` (reset alongside `next_ep_banner_shown` on every new episode) and a `credits_mark_played: Option<(String, Arc<JellyfinClient>, bool)>` local in `wire_mpv_timer`'s existing 16ms tick, captured inside the `VideoState` lock (alongside the existing credits-trigger block) and dispatched via `rt_handle.spawn` after the lock releases — the same deferred pattern already used for `banner_trigger`/`gapless_commit` in this function, since calling the Jellyfin client while still holding the lock would block the tick. `mark_played`/`mark_unplayed` (`fjord-api::client.rs`) already existed, previously only reachable via the context menu's manual "Mark Played" action. Scoped identically to the banner/auto-advance itself (`skip_credits_mode != never-skip` — `never-skip` mode gets neither the banner nor the auto-mark, consistent with its own "no interference" philosophy). Verified: clean `cargo build --workspace`/`clippy` (zero new warnings, confirmed via `git stash` diffing the warning count before/after — the count had already drifted from the documented 54 baseline to 57 in an earlier commit, unrelated to this change), and a release-build smoke test confirming clean startup. **Not yet verified against a real Jellyfin server** — the actual API round-trip (mark-played firing at credits, mark-unplayed firing on rewind) needs live testing by the user, since it's the one part of this change that touches real, visible server-side watched-state. |
| 110 — Manual single-threaded code review of Phases 105-109 (session budget exhausted the usual 10-agent parallel review), found and fixed 3 issues (2026-07-11) | User asked for a code review of everything since Phase 105 (the press/click animation work through the Phase 109 mark-played feature). The usual xhigh-effort 10-parallel-finder-agent review hit the session's API rate/usage limit twice in a row (once mid-session, once again on retry after the first reset) — rather than keep waiting, did a single-threaded manual review of `git diff 9be9c24...HEAD` instead, at lower recall than the full parallel pass but still catching a real bug. **Found and fixed:** (1) *[correctness, high severity]* Phase 109's rewind-revert check recomputed its threshold as `credits_start.unwrap_or(duration - 30s)`, always preferring `credits_start` when present — but the trigger itself is `credits_fire || fallback_fire`, and for any episode whose end-credits run under ~29s, `fallback_fire` (the `duration - 30s` check) crosses first while `credits_start` is later still in the future. That made the revert-check fire on the *same tick* as the mark, before the lock protecting both ever released — the `mark_played` POST was silently overwritten by a `mark_unplayed` DELETE in the same local variable before either was ever dispatched, so the whole Phase 109 feature quietly no-op'd (or, on a rewatch, spuriously un-marked it) for every episode with short end-credits. Fixed by adding `VideoState.credits_mark_threshold: Option<f64>`, which records the position that *actually* fired the trigger (the `min` of whichever of `credits_fire`/`fallback_fire` was true) at the moment it fires, and having the revert-check read that back instead of re-deriving a threshold that can disagree with what actually happened. (2) *[efficiency, minor]* the Phase 108 `detail_ids` construction (`main.rs`) walked `BoundedCache::keys()` then called `.get(&k)` per key — a redundant second HashMap lookup plus a full `MediaItem` clone per entry, done once per login and scaling with prewarm's library-wide cache size. `BoundedCache` had no borrowing iterator at all, so this was the only API available — added `BoundedCache::iter() -> impl Iterator<Item=(&str, &V)>` (`config.rs`) and switched `main.rs` to a single pass over it. (3) *[docs]* the Phase 107 entry's own narrative named the Continue Watching row's model rebuild as the crash trigger, but two sentences later stated the actual root cause only affects `MediaCard` instances with no explicit `height:` — and Continue Watching is a `SectionRow`, which passes `height:` explicitly (confirmed by reading `home.slint`), exactly the case the same entry calls safe. Corrected the entry in place rather than leaving the self-contradiction for a future debugging session to trip over; this had already been surfaced mid-session once with an offer to fix it that went unanswered. Verified all three fixes: clean `cargo build --workspace` + `--release`, `cargo clippy --workspace` unchanged at 54 warnings (the pre-existing baseline — the 57-count seen during Phase 109 turned out to not persist), and a release-build smoke test confirming clean startup (no crash, no cleanup errors). The Phase 109 mark-played/rewind-revert round-trip still needs a live Jellyfin test by the user, same as before — this fix makes the *logic* correct but doesn't substitute for that. |
| 111 — Fix: tear_down_player's stop-report re-added a resume position moments after the credits-trigger mark, undoing it on every episode (2026-07-11) | User provided a real HTPC `fjord.log` (r719.9d6d26b, i.e. Phase 109 + the Phase 110 threshold fix, but before this fix) and asked why watched episodes were still showing up in Continue Watching. Traced through the WS trace directly rather than guessing: for episode `090761...`, `UserDataChanged` at 23:01:49 confirms the credits-trigger mark succeeded (`played=true position_ticks=0`); the user then watched 5 more seconds and pressed Stop; `UserDataChanged` at 23:01:55 (right after the stop) shows `played=true position_ticks=29170389999` (2917.04s — nonzero again). Same pattern for episode `0f110590...` on its "replaced" transition (Play Now switching to the next episode) a few lines earlier in the same log. Root cause: all 4 teardown paths (`do_stop_playback`, the "replaced" path when a new episode starts while one is active, natural end-of-file, and `quit_cleanup`) funnel through one shared `tear_down_player` helper, which unconditionally reports the *raw* mpv position to `report_playback_stopped` — with zero awareness that the credits-trigger had just explicitly told Jellyfin this item is played with position 0. The ordinary stop-report re-adds a nonzero `PlaybackPositionTicks` on the server the instant it lands, which is what makes Jellyfin's own `IsResumable` filter (queried by `get_continue_watching()`) match again, regardless of the `Played` flag — undoing the mark within seconds of it succeeding, on every episode that doesn't literally reach mpv EOF before the player tears down (i.e. nearly always, since Play Now / manual Stop / natural end after credits all stop *some* time after the credits trigger, not at the literal last frame). This is a different bug from Phase 110's threshold fix — that one was about the mark never landing in the first place for short-credits episodes; this one is about a correctly-landed mark being silently overwritten moments later by pre-existing, unmodified reporting code that had no idea the new feature existed. Fixed at the same single choke point: `tear_down_player` now reports `ticks=0` instead of the raw position whenever `VideoState.credits_auto_marked_played` is still true, and clears both `credits_auto_marked_played`/`credits_mark_threshold` itself (removing the need for each of the 4 call sites to do it individually). Composes correctly with the existing rewind-revert logic: a genuine rewind past the trigger point already flips `credits_auto_marked_played` back to `false` via the Phase 109 mechanism before any of this runs, so a real abandoned-rewatch scenario still reports its true resumable position through this same path, unchanged. Verified: clean `cargo build --workspace` + `--release`, `clippy` unchanged at the 54-warning baseline, release smoke test confirms clean startup. This is now directly informed by a real captured log rather than only local reasoning — the mark_played half of Phase 109 was confirmed working correctly by the same log; only the post-mark stop-report was ever wrong. |
| 112 — Fix: last episode of a series auto-restarted itself at natural end (2026-07-11) | User report: no Up Next overlay shows for a series' last episode (correct, expected), but letting it play to the literal end causes it to immediately restart from the beginning instead of just stopping. Traced by reading `/Shows/NextUp`'s two call sites in `wire_mpv_timer` side by side. `/Shows/NextUp` (`get_next_up_for_series`) is unreliable right at an episode boundary in two distinct ways, and the codebase only ever guarded against one: (1) shortly before Jellyfin has processed a stop/played report for the current episode, it still returns the CURRENT episode as "next up" — the banner-trigger path already had a same-id check for this (CR10-13), re-deriving the true next episode from the series' own ordered episode list when triggered. (2) Once a series is FULLY watched — which, after Phase 109/111's credits-trigger auto-mark, can now happen well before natural EOF for the very last episode — NextUp can fall back to a "start over"/rewatch suggestion instead of correctly reporting "no next episode"; the natural-end-of-file fallback path (fires when EOF arrives before the banner-trigger's own background fetch has set `next_ep_pending` — exactly the last-episode case, since no banner ever shows so no such fetch ever runs) called `get_next_up_for_series` with **zero** guard of any kind, so this fallback suggestion sailed straight through and restarted the episode. Fixed by extracting a single shared `resolve_true_next_episode(cli, series_id, current_id)` helper (`playback.rs`, near `playlist_next`) that never calls `/Shows/NextUp` at all — fetches the series' ordered episode list directly and only accepts a genuine current-position+1 match as "next"; anything else (same episode, an earlier one, any other suggestion) means there is no next episode, full stop. Both the banner-trigger path and the natural-end fallback (which needed a `finished_item_id` captured before `tear_down_player`'s returned `item_id` gets moved/consumed a few lines later) now share this one resolution instead of one path having an ad-hoc guard and the other having none — a general fix rather than a second special case bolted onto the first. Verified: clean `cargo build --workspace` + `--release`, `clippy` confirmed unchanged (57 warnings with or without this change, via `git stash` diff — the baseline itself had already drifted from 54 in an earlier, unrelated commit), release smoke test confirms clean startup. Root-caused entirely from first principles by reading the two NextUp call sites against each other — no log was available for this specific report, unlike Phase 111. |
| 113 — Clippy cleanup: 57 → 0 warnings (2026-07-11) | User asked what the 57 clippy warnings tracked across recent phase entries actually were and whether they were worth fixing — none were bugs, all were `clippy::style`/`complexity`/`perf` idiom suggestions (a baseline that had drifted from an older documented 54, unrelated to any of this session's fixes). `cargo clippy --fix --workspace` mechanically resolved 40: `.map_or(false, \|x\| ...)` → `.is_some_and`/`.is_none_or`, manual `RangeInclusive::contains` reimplementations, `x % n == 0` → `.is_multiple_of(n)`, redundant closures/derefs/casts, no-op `.into()` conversions, an unneeded `match` collapsed to `if let`, and one genuine (verified) needless `return` — confirmed the auto-fix correctly left a superficially-identical sibling `return` untouched in `home.rs::wire_nw_timer` where removing it *would* have changed control flow (falling through into an unrelated `if due_tv` block), only touching the one where it's truly the last statement in its enclosing block. Remaining 17 needed manual judgment: `unnecessary_filter_map` (`mpv.rs::get_tracks` — closure always returned `Some`, so `map` was correct); `unnecessary_sort_by` (`browse.rs` — converted to `sort_by_key(\|b\| Reverse(...))` per clippy's own suggested rewrite); 4× `field_reassign_with_default` (`artist.rs`/`context_menu.rs`/`detail.rs`/`main.rs::item_to_card_item` — converted `CardItem::default()` + N field writes to a single struct literal with `..Default::default()`); `too_many_arguments` on `start_playback` (11 params, call from ~30 sites — bundling into a struct would touch every call site for zero behavior change, so `#[allow(clippy::too_many_arguments)]` with a comment explaining why, rather than a forced refactor); 8× `type_complexity` — these were all instances of the project's own established "meta"/"decoded" tuple convention (already named in `CLAUDE.md`'s module table) repeated inline instead of aliased: `poster.rs` already had `SectionMeta`/`SeriesMeta` type aliases defined but two call sites weren't using them; extended with matching `DecodedSeriesCard`; `movies.rs` got new `CardMeta`/`DecodedCard` aliases (the exact same 9-tuple decode shape was duplicated verbatim in 3 places, including two byte-for-byte identical closures at the normal-completion and panic-flush paths); `artist.rs` got a locally-scoped `DecodedAlbumCard` (its own distinct `String`-based variant of the same shape, used once). Verified: `cargo build --workspace` + `test --workspace` + `--release` all clean, `cargo clippy --workspace` now reports zero warnings across the whole workspace, release smoke test confirms clean startup. Pure internal refactor — no behavior, UI, or API change; not something the HTPC build needs re-testing for beyond the usual smoke check. |
| 114 — Code review of everything since CR11 (45 commits, ~4500 lines); found and fixed 6 more issues, 2 of them real mark-played bugs (2026-07-11) | User asked for a full review of every change since the CR11 review wrapped up (commit b3785cb). Launched 8 parallel finder agents (scaled down from the usual 10 given the diff size); 3 completed before hitting the session's usage limit again, but returned enough concrete findings to act on — verified and fixed each by hand rather than waiting for a retry. **Two real continuations of the Phase 110/111 mark-played bug hunt, both variations on the same root cause (pre-existing code that reports the raw playback position with no awareness `credits_auto_marked_played` exists):** (1) `report_playback_progress` — the routine ~10s Jellyfin progress report that runs throughout ALL playback, not just at teardown — reported the live, non-zero position completely unconditionally. Even with Phase 111's `tear_down_player` fix working correctly, the very next progress tick after the credits trigger (which can land anywhere from 0-10s later, since `pos_tick`'s modulo-600 cycle isn't reset by the trigger) re-added a resume point and undid the mark long before the user ever stopped or reached EOF — meaning the bug Phase 111 was supposed to have fixed could still reproduce, just via a different, more frequently-firing call site that was missed entirely. Fixed with the identical `&& !vs.credits_auto_marked_played` guard used at the trigger site. (2) The rewind-revert block correctly reset `credits_auto_marked_played`/`credits_mark_threshold` on a genuine rewind, but never reset `next_ep_banner_shown` — since the outer trigger block is gated on `!next_ep_banner_shown`, once it fires once for an episode it can never fire again, even after a legitimate rewind-and-rewatch. A user who rewound past credits to re-watch a scene, then watched forward through the same point a second time and stopped before literal mpv EOF, would end up with a genuinely-watched episode never re-marked played. Fixed by resetting `next_ep_banner_shown = false` alongside the other two fields; re-showing the Up Next banner on a second pass through credits is correct behavior (the "once per episode" comment predates rewind-tracking entirely), not a regression. **Four smaller fixes found in the same pass:** `layout.slint`'s Quit sidebar row had its press-pulse migrated to `sidebar-kb-active` (Phase 105) but its `background` binding was missed, left reading the stale `active-nav==11 && focused-section<0` check — arrowing to Quit then opening any overlay via mouse left the row lit red the whole time the overlay was open. Sign-out never cleared the six Phase 102/103 screen-open caches, which hold per-user played/favorite state keyed only by item id with no user/server scoping — a second account on the same install would silently inherit the first account's cached watched-state on anything viewed before sign-out; added `BoundedCache::clear()`, called for all six on sign-out. `widgets.slint::PressPulse`'s `reset-timer.running = true` was a no-op when already running (Slint's `Timer.running` is dirty-checked like any property, confirmed against `i-slint-core`'s actual source rather than assumed), so a second `kb-activate-pulse` bump within 110ms of the first didn't restart the countdown, cutting a rapid second press's flash short — fixed by forcing a genuine `false` then `true` write. `prewarm.rs::spawn_image_prewarm` still had a `keys()`+`get()` double-lookup that the same-day `BoundedCache::iter()` clippy cleanup (Phase 113) missed in this one file — switched over, which made `keys()` itself genuinely dead code everywhere and it was deleted. **Findings surfaced but deliberately left open** (design tradeoffs, not clear-cut bugs — reported to the user rather than acted on unilaterally): `resolve_true_next_episode` (Phase 112) fetches the full series episode list on every advance rather than caching it, and bypasses `/Shows/NextUp` entirely rather than using it as a hint with the ordered-list check only as a validator, which discards NextUp's server-side watched-state awareness for scenarios not yet hit in testing (e.g. cross-client out-of-order watching); the opt-in library prewarm (Phase 104) has no session/sign-out guard on its multi-minute background sweep, unlike `ws.rs`'s documented `Arc::ptr_eq` pattern for the same class of risk; `load_screen_caches()` does a blocking synchronous file read+parse on the UI thread during login (can be ~1.3MB after a prewarm). Verified all fixes: clean `cargo build --workspace` + `test --workspace` + `--release`, `cargo clippy --workspace` still zero warnings, release smoke test confirms clean startup. |
| 115 — Acted on the 3 design-tradeoff findings left open in Phase 114 (2026-07-11) | User said "yes" to acting on all three. `resolve_true_next_episode` (Phase 112) changed from dropping `/Shows/NextUp` entirely to a validated-hint form: still fetches the ordered episode list (needed for the current episode's position either way), but now also calls NextUp and accepts its answer whenever it resolves to a position strictly greater than the current episode's, falling back to strict position+1 only when NextUp's answer fails that check. Both original failure modes (same-id response, post-mark rewatch suggestion) are still caught by the same position test; the skip-ahead case NextUp used to handle (e.g. an episode already watched from another client) is preserved instead of being discarded. `prewarm.rs::spawn_metadata_prewarm` (Phase 104) gained a `session_current()` guard mirroring `ws.rs`'s `Arc::ptr_eq` pattern (CR11-2) — checked before every write that inserts a `MediaItem` (embeds per-user `UserData`) into a shared cache, across the two batched detail loops and all five relationship-cache JoinSet closures; deliberately not added to `spawn_image_prewarm`, whose writes are just raw image bytes keyed by item/person id, not per-user. `push_cached_data` (`main.rs`) ran entirely on the Slint UI thread including `load_screen_caches()`'s synchronous read+parse of `screen_caches.json` (~1.3MB after a prewarm, the one `load_X_cache()` call in that function actually flagged, by far the largest on disk) — moved to run via `tokio::task::spawn_blocking` before `invoke_from_event_loop` is entered, with the parsed result threaded into `push_cached_data` as a new parameter instead of the function loading it itself; the function's several other, much smaller `load_X_cache()` calls were left as-is, not flagged and not worth the larger restructuring. Verified: clean `cargo build --workspace` + `test --workspace` + `--release`, `cargo clippy --workspace` still zero warnings, release smoke test confirms clean startup and correct screen-cache population through the new code path. |
| 116 — Relaunched the 5 finder agents from Phase 114 that hit the session limit; found and fixed 8 more issues (2026-07-11) | User asked whether the 5 agents that never ran in Phase 114 should be retried now that time had passed. Confirmed the session limit had reset and relaunched all 5 on the same `b3785cb..HEAD` range; all 5 completed this time. **Two more real playback bugs, both continuing the credits-trigger mark-played saga:** (1) the rewind-past-credits revert reset `credits_auto_marked_played`/`credits_mark_threshold`/`next_ep_banner_shown` but never touched the in-flight Up Next banner — `next_ep_pending` stayed `Some`, the banner stayed visible, and its already-spawned countdown task kept ticking down untouched, meaning a user who rewound to re-watch a scene while the countdown was running could still get auto-advanced to the next episode on the ORIGINAL schedule regardless of the revert. Fixed by clearing `next_ep_pending` (the countdown task's own per-second loop already bails within ~1s when that's `None` — reusing the exact mechanism the "Skip" button's `on_cancel_auto_advance` already relied on) and hiding the banner via the same deferred-dispatch pattern used elsewhere in the function. (2) Suppressing `report_playback_progress` entirely (not just zeroing it) for the whole `credits_auto_marked_played` window left the server's position at 0 for as long as that window lasted; when a revert then fired `mark_unplayed` (which, mirroring `mark_played`, also resets position to 0 server-side), the resulting `UserDataChanged` event read as `played=false, position_ticks=0` — which `ws.rs` treats as "untouched," briefly dropping the item from Continue Watching during an active rewatch. Self-healing within ~10s, but fixed properly: the revert dispatch now sends one immediate `report_playback_progress` call with the real position right after `mark_unplayed` succeeds. **Six smaller fixes:** `spawn_screen_cache_refresh` (`main.rs`) — the AMBIENT per-login refresh of the six screen-open caches, not the opt-in prewarm — had no session guard at all, unlike `prewarm.rs`'s Phase 115 fix; promoted `session_current()` to `main.rs` (shared by both files now) and added it to all 6 of this function's write sites, since this one runs automatically on every login, making it a wider exposure than the opt-in sweep it was originally fixed for. `spawn_metadata_prewarm`'s own finalization block wasn't session-guarded either — a stale sweep's completion landing after a new session's sweep had already started would reset the new sweep's reentrancy guard and overwrite its summary text. `OfflineScreen`'s three buttons and `PlaylistPicker`'s rows both return early in `keys.rs::handle_key()`, before the centralized `kb-activate-pulse` bump on Enter — added the same bump to both branches, since their `PressPulse`-driven widgets were keyboard-dead (mouse-only) as a result. Two U+FE0E glyph ternaries from the Phase 105 consistency pass only had the variation selector on one branch (context_menu.slint's Mark Played/Unplayed icon, widgets.slint's StorylineSection chevron) — fixed both to match. `container_tracks_cache` wasn't invalidated by either playlist-mutation path that should have cleared it — removing a track left it visible on reopening the same playlist, and `refresh_playlists`'s own CR11-7 "reopen so the tracklist reflects the change" fix was silently defeated by the same cache serving the pre-mutation list — fixed both by invalidating the cache entry before the reopen/re-render. Also re-ran the Phase 86 table-reorder script — Phase 115 had been inserted above 114 again (the exact same habit that caused the original disorder), now corrected. **Findings noted but not acted on** (narrower, lower-confidence, or requiring a bigger redesign): `BoundedCache::recent_keys` is documented as "most recently used" but `insert()` only appends to the insertion-order list on a key's first-ever insertion, never on a refresh/re-insert or a `get()` hit — so it's actually pure insertion order, meaning the ambient refresh's "40 most recent" can drift permanently toward whatever a prewarm inserted first rather than what's actually being browsed; fixing this properly needs `get()` to become `&mut self` everywhere it's called, a much larger change. `credits_mark_played`'s POST/DELETE dispatches are two independent spawned tasks with no ordering guarantee — a rewind-then-immediately-forward-past-threshold sequence faster than either HTTP call completes could theoretically land them out of order; the trigger window is extremely narrow (full playback-direction reversal twice within network-latency time) and a proper fix needs a generation/sequence guard, judged not worth the complexity for how rarely it could actually trigger. Several reuse/simplification and CLAUDE.md-header-doc-sync findings (PressPulse's `trigger:` binding repeated identically 51 times with no default; the credits-mark state now spread across 5 sites with no shared accessor; `prewarm.rs`'s 6 near-identical JoinSet loops; 3 independently-named copies of the same tuple shape from the Phase 113 clippy cleanup; a few files' headers missing newer symbols) were left as pure cleanup, no user-visible impact. Verified: clean `cargo build --workspace` + `test --workspace` + `--release`, `cargo clippy --workspace` still zero warnings, release smoke test confirms clean startup. |
| 117 — MediaCard title/subtitle: closed the title-year gap, made long titles/episode names wrap fully instead of truncating, and worked around a real Slint 1.16.1 vertical-alignment bug found via a standalone repro (2026-07-11) | User reported a gap between a card's title and its year/subtitle whenever the title was short enough to fit on one line — the title `Text` had a fixed 34px (2-line) box, top-aligned by default, so a 1-line title left blank space at the box's own bottom before the subtitle started. First fix: `vertical-alignment: bottom` on the title `Text`, which closed the gap for 1-line titles. User then asked for the subtitle (episode names on some cards) to wrap instead of eliding to one line — added `wrap: word-wrap` + a 28px 2-line box to the subtitle `Text`, and grew `poster-rect`'s reserved-space constant from 66px to 76px to match. User then reported the SAME gap reappearing, this time only on SOME 2-line-wrapped titles. Root-caused with a disposable standalone Slint crate outside the repo (pinned to the exact `slint = "=1.16.1"` from this project's `Cargo.lock` — the bug does not reproduce on 1.17.1) reproducing the two real offending titles pulled straight from `~/.cache/fjord/*.json` via `grep` (`Chillin' in Another World With Level 2 Super Cheat Powers`, `Your Friendly Neighborhood Spider-Man`) at the real card width, screenshotted with `spectacle -b -n -a -o <path>` (KDE's screenshot CLI, since no Wayland screenshot tool was preinstalled) — confirmed `vertical-alignment: bottom` combined with `overflow: elide` + `wrap: word-wrap` genuinely mis-measures text height in this Slint version as soon as the title wraps to 2+ lines, elided or not, while `vertical-alignment: top` never exhibits it (hardcoded zero baseline, no text-height computation involved). Asked the user whether to also fix the missing elide ellipsis while in there; user's actual answer reframed the whole approach — don't cap the title at all, always show it in full. **Final design:** removed the fixed-height/elide/bottom-alignment approach entirely — both title and subtitle `Text` elements are now plain `wrap: word-wrap` with no explicit height, growing to however many lines they need, and `poster-rect`'s height is now `max(0px, root.height - 12px - text-col.preferred-height)`, reading the sibling text block's actual preferred height so the poster shrinks (down to fully hidden in a pathological case) rather than the text ever overflowing the card's own bounds. Re-verified the LibraryGrid `root.height`-in-a-layout-cache recursion-crash risk (see the matching CLAUDE.md gotcha) doesn't reappear, using the same disposable-crate technique: instantiated the equivalent structure both with an explicit height (`SectionRow`-style) and inside a `HorizontalLayout` row with no explicit height (`LibraryGrid`-style, the historically vulnerable case) plus a hover-triggered focus ring (the exact mechanism of the earlier real crash) — no panic in either case. Also verified end-to-end against the real running app with real server data (auto-login, live screenshot via `spectacle`, clean `fjord.log` with no panics). `cargo build`/`clippy --workspace` clean throughout. Follow-up ideas from the user, captured under Deferred/future rather than built now: a user-facing poster-size setting so larger posters naturally have more text budget before this becomes visible, and/or rendering the poster full-bleed behind the title with a dark semi-transparent scrim instead of a separate text block below it. |
| 118 — Settings batch: Fjord client version display, configurable seek step, subtitle appearance (scale/position/ASS-styling toggle/color/background), and per-series session-only remembered audio/subtitle track (2026-07-12) | Planned via a formal plan-mode session after the user asked to plan four settings gaps identified in an earlier settings review together. **1) Client version**: `AppState.client-version`, set once at startup from the existing `env!("FJORD_BUILD_ID")` (previously only logged), shown in the Settings sidebar below the Jellyfin server name/version block — added specifically because the project's own HTPC testing workflow already involves manually checking this string in a shared log before trusting it against current `HEAD`. **2) Seek step**: `Config.seek_step_secs`/`seek_step_long_secs` (default 10s/30s, matching the previous hardcoded amounts), new Player → Seeking rows appended at the end of the section (avoiding a renumber, per the established append-only convention for rows with no natural "home" section) — replaces 8 hardcoded seek-amount literals split across two independent code paths (`keys.rs` keyboard dispatch, `controls.rs`'s separate non-debounced on-screen skip buttons). **3) Subtitle appearance** — the most involved of the four, and the one that changed shape twice during planning after deeper investigation: initially planned as font scale/color/position with a naive assumption that `sub-color` would just work; verified against the actual installed mpv manual (`man mpv`, mpv 0.41.0, not memory) instead, which turned up that mpv's subtitle style overrides are gated by `--sub-ass-override` (defaulting to `"scale"`, not `"yes"` as first assumed) — `sub-scale`/`sub-pos` apply to ASS-styled subtitles unconditionally, but `sub-color` is documented as affecting only "unstyled" subtitles, doing nothing to ASS-styled ones (common for anime/fansub content) without `sub-ass-override=force`; separately, `sub-back-color` alone doesn't draw a background box at all under the default border style (it recolors the drop *shadow*), needing `sub-border-style=background-box` alongside it — both facts caught by re-deriving the design against the manual rather than assuming, avoiding a feature that silently did nothing for a large fraction of the user's own library. User's own follow-up question during plan review ("should the color setting be gated behind ASS or not?") reshaped the row layout a second time: landed on gating *only* the two settings that actually break/conflict with ASS styling (Color, Background — hidden until a "Respect subtitle's own styling (ASS)" toggle, default on, is switched off) while Scale/Position stay always-visible and always-active, never gated, since they never needed it. Implementation: 5 new `Config` fields, 5 new Player-section rows inserted after the existing Subtitle Type row (renumbering `PLY_CACHE_MB` through `PLY_CREDITS_SECS` up by 5 — done via a small Python script operating on a fixed line range rather than manual edits, same technique as the PLAN.md table reorder script), `PlayerConfig` + `Player::new` gain construction-time application, and a new `Player::set_sub_style()` applies the same fields *live* to an already-running player from the `on_settings_changed` handler (mirrors the existing sub-delay/audio-delay live-adjust UX — no restart needed to see a style change). `Config.sub_color` stores a display name ("White"/"Yellow"/...), translated to an mpv hex color by a new `config.rs::sub_color_hex()`, mirroring how `sub_lang` already stores a display name translated by `playback.rs::sub_lang_code()` — keeps `fjord-player` itself free of any UI-preset knowledge. **4) Remembered per-series track** — user chose per-series scope (not global) and session-only lifetime (not persisted to disk) when asked directly via `AskUserQuestion`, matching `series_episode_cache`'s existing lifetime class. No new Settings row at all (always-on behavior, not a preference). Write side: `controls.rs`'s `commit_panel_selection` (S/A panel) now looks up the committed track's `lang` via the already-existing `Player::get_tracks()` (no new mpv plumbing) and stores it per-`series_id` in a new `FjordState.remembered_tracks: HashMap<series_id, RememberedTracks>` — required threading a new `state` parameter into `wire_controls`'s signature. Read side: `playback.rs`'s track auto-select block (`wire_mpv_timer`) checks the remembered language for the playing series first, ahead of `Config.sub_lang`/`sub_lang2`/`audio_lang`, reusing the entire existing language-matching/type-priority logic unchanged — only the source of the preferred-language string changes; a remembered language with no matching track in a given episode still falls through to the global preference rather than leaving mpv's default unchanged. The remembered value is deliberately a raw mpv lang code (copied from a real `TrackInfo.lang`), not a display name, since numeric track ids aren't meaningful across different episodes/files. Cleared on sign-out alongside the six screen-open caches, same cross-account-contamination reasoning. Verified end-to-end: `cargo build`/`clippy --workspace` clean throughout implementation (built incrementally after each of the ~15 edit steps, not just once at the end), full workspace build/clippy/test/release pass, plus a live smoke test against the real server session covering all four features individually. |
| 119 — Global UI animation speed setting: Scroll speed + Animation speed, scaling all 121 real `animate` transitions plus LoadingSpinner's cycle Timer, new Settings → UI section (2026-07-12) | User asked whether UI settings like animation speed should be added, prompting a full plan-mode design pass with several rounds of back-and-forth before landing on the final shape. Confirmed via a standalone throwaway Slint crate (pinned to the project's exact `slint = "1.16.1"`) that `duration: 300ms * speed;` compiles and works correctly — at `speed = 0.0` every `animate` block's duration becomes `0ms`, an instant jump, while intermediate speeds (50%, 150%) remain available, something a binary reduce-motion toggle can't offer. Investigated actual scope: `grep -rn "animate "` finds 119 real blocks across 17 `.slint` files (a few more grep hits are comment-only mentions); a separate sweep for non-`animate` `Timer {}` blocks (which the `animate`-only grep would miss entirely) found 5 total — 4 are "hold this state, then a separate animate eases it away" patterns (toast auto-dismiss, pause/play flash hold, press-pulse hold, fade-in kick-off tick) correctly left alone, but `LoadingSpinner`'s dot-cycle `Timer { interval: 260ms; }` directly choreographs the spinner's own rhythm alongside its dots' `animate opacity`, so it was folded into the same multiplier — floored at `max(16ms, 260ms * speed)` rather than a literal 0ms, since unlike an `animate` duration, a Timer interval of 0ms would re-fire continuously and spin the CPU (a real finding made during implementation, not anticipated in the original plan). **Design journey (kept in CLAUDE.md since it explains the final call):** started as a single blanket multiplier, then split into Scroll speed (the 22 `viewport-y`/`viewport-x` Flickable sites — a throughput property, always kept separate) + Animation speed (everything else) per user request, then further expanded to a 4-category "Customize" toggle (Press feedback / Fade / Background & color / Geometry, opt-in and hidden by default) for real per-category control when the user asked for more granularity twice in a row. Then the user raised their own concern — with 4+ independent dials, a later "why does this look different" becomes undebuggable since nothing tracks what was changed — and the design was deliberately collapsed back to just the 2 settings, the simplest version considered rather than the most powerful one. Implementation: `Config.scroll_speed_pct`/`animation_speed_pct` (both default 100), `AppState.settings-scroll-speed`/`settings-animation-speed` (derived floats, computed in `apply_settings_to_window` and inline in `settings.slint`'s mouse-click dropdown handlers), a new `SECTION_UI` appended *after* Key Bindings rather than inserted earlier (every row in `settings.slint` hardcodes a literal `section: N` with no symbolic const reference, so inserting a section anywhere but the end means renumbering every row of every later section by hand — same append-only principle as Phase 118's Seeking rows, one level up). Applied the multiplier to all 121 real sites (99 `animation-speed` + 22 `scroll-speed`) via a two-pass Python script: the first pass's regex anchored `animate` to the start of the line and missed 28 sites in `main.slint`'s `fadeN` pattern, where other content (`opacity: fadeN.fade-opacity;`) precedes `animate` on the same line — caught by re-running with the anchor removed and an idempotency guard (skip lines already carrying a multiplier) so the second pass only touched what the first missed. Verified end to end: `cargo build`/`clippy --workspace` clean after every step, a `grep` sanity count confirming 121 sites now reference one of the two speed properties with none left un-scaled. Live interactive testing deferred — the Jellyfin server was down for this whole session, so this is unverified against a real playback session and real keyboard-driven scrolling; flagged for the user to check once the server's back. |
| 120 — Widened the Scroll/Animation speed preset range and clarified the row subtitles (2026-07-12) | Same-day follow-up to Phase 119, from live testing feedback: the original `["0","50","75","100","150"]` preset only offered one slower-than-normal option (150%), and the user pointed out the percentage's direction is counter-intuitive — it's a *duration* multiplier, so a bigger number means a LONGER transition (slower), the opposite of what "speed" suggests at a glance. Widened `SPEED_PCT_MODEL` (`settings.rs`) to `["0","25","50","75","100","150","200","300"]` and added an explicit "lower % is faster, higher % is slower" clause to both row subtitles. `current_value_str`/`apply_dropdown_selection`/`settings_row_action` in `settings.rs` all parse the selected value generically (`val.parse()`), so the Rust side needed no changes beyond the const array itself; only `settings.slint`'s two dropdowns needed their `current-value`/`selected(v)` ternary chains extended, since the mouse-click path hardcodes the value set inline rather than referencing the Rust const. Also used this as a live opportunity to verify a same-day, not-yet-committed local dev-build speedup (`.cargo/config.toml`, `mold` linker + `sccache` compilation cache, both already installed by the user) actually helps: touch-and-rebuild dropped from ~20s+ to 13s, and a full `cargo clean` rebuild dropped from ~1m40s cold to 1m20s with sccache landing a 50% cache-hit rate on the dependency tree (the remaining misses are `fjord-app`/`fjord-api`/`fjord-player` themselves plus Slint's build-script-generated code, which don't cache as cleanly — a known sccache limitation, not a config problem). Not yet decided whether `.cargo/config.toml` should be committed (machine-specific tool paths) or left local-only. `cargo build`/`clippy --workspace`/`test --workspace`/`build --release` all clean throughout. |
| 121 — Live-testing follow-up: easing curve, Settings FJORD block styling, Up Next banner fade (2026-07-13) | Three small fixes from live-testing the Phase 117-120 work now that the animation-speed lever was in hand. (1) At 300% duration the user liked the slower feel but wanted the deceleration itself ("ease out") to feel more gradual — tried `ease-out-quart` first (`cubic-bezier(0.25,1,0.5,1)`), user reported no visible difference from plain `ease-out`, so stepped up to `ease-out-expo` (`cubic-bezier(0.16,1,0.3,1)`) across all 103 non-scroll `animate` sites (scroll's 14 `ease-in-out` sites left untouched — a separate lever). Confirmed the duration multiplier (`* AppState.settings-animation-speed` / `settings-scroll-speed`) is untouched by this — easing curve and duration scale are independent properties on the same `animate` block. (2) Settings left pane: the FJORD block (client version) had no bold name line matching the SERVER block's `jellyfin` / version two-line structure — added a static "Fjord" bold title line above the version string in `settings.slint`, mirroring `server-name`/`server-version`'s exact styling. (3) Audited every `if`-gated element in `main.slint` for a missing fade: confirmed `PlayerScreen`'s exemption (video-start latency) and `MiniPlayerBar`/`MusicPlayerBar`'s exemption (shared `total-bar-h` with the content area — fading one without delaying the other would look like a gap) are both already deliberate, documented decisions, not oversights — left alone. Found one genuine gap: the Up Next auto-advance banner (`main.slint`, floating card, no shared-geometry concern) had no fade at all; added the same `FadeInTrigger` + `animate opacity` idiom as the other 28 sites (now 29), updated the file's header comment to include it in the documented list. `cargo build -p fjord-app` clean after each step. |
| 122 — `FadeGate`: screen/overlay switches now fade out, not just in (2026-07-13) | User asked specifically why going Back from a Detail page felt instant despite the page fading in on open. Root cause was generic, not Detail-specific: all 29 `main.slint` fade sites (Phase 121) used `FadeInTrigger`, which only ever handles the fade **in** — the instant the driving `show-X` flag goes false, Slint's `if` conditional destroys the element immediately, so there's no element left for a fade-out `animate opacity` to act on. Fixed by adding `FadeGate` (`widgets.slint`, next to `FadeInTrigger`): it owns the mount/unmount decision itself (`in property show`, `out property mounted`/`fade-opacity`) instead of the raw condition — `gateN := FadeGate { show: <original condition>; }` declared as an always-present sibling (same pattern as this file's own `toast-timer`/`_toast-vis`), then `if gateN.mounted: Element { opacity: gateN.fade-opacity; animate opacity {...} }`. `mounted` stays true for one fade-out's worth of time (`max(16ms, 150ms * settings-animation-speed)`, matching the consumer's own `animate` duration) after `show` flips false, so the element survives long enough to actually play the fade-out before being unmounted. Hit one real compile error along the way: naming the out-property `opacity` collides with the built-in `opacity` every Slint element already has (`Cannot override property 'opacity'`) — renamed to `fade-opacity`, the same workaround `FadeInTrigger` already used for the identical reason. All 29 `main.slint` sites converted in one pass via a Python script operating on the file's exact known text (not a blind regex — the 29 sites have several different shapes: single-line, multi-line with a separate opacity line, condition split across an outer/inner `if`), verified by a full rebuild after. `player.slint`'s 3 `FadeInTrigger` sites (skip-segment overlays — dismissed by an explicit button/countdown, not a `show-X` flag) and `MediaCard`'s poster fade-in were deliberately left alone — a different, narrower shape of problem, out of scope here. `cargo build`/`clippy -p fjord-app` clean. |
| 123 — Phase 122 regression: `FadeGate` broke every dashboard/library/settings switch; reverted 11 of the 29 sites (2026-07-13) | User reported "librarys don't behave as they should" after Phase 122, having tested the dashboard and settings pages. Root cause: `AppShell` (`layout.slint`) `inherits HorizontalLayout`, and its content-slot children — the 11 AppShell-hosted `main.slint` sites (BrowseScreen, HomeScreen, 2×DashboardScreen, 4×LibraryGrid, CollectionsDashboard, MusicDashboard, SettingsScreen) — each carry `horizontal-stretch: 1` in that one shared slot, which only ever worked because exactly one condition was true at a time under the old instant-unmount behavior. `FadeGate` keeps the OUTGOING screen mounted for its fade-out duration while the INCOMING one mounts immediately, so for that whole window two `horizontal-stretch: 1` siblings competed for the same slot and the `HorizontalLayout` split/squeezed space between them — a real, visible layout break on every nav/library switch, worse at higher `settings-animation-speed` (longer overlap window). The other 18 `FadeGate` sites (Connecting/Offline/Login, the 7 overlay screens, now-playing/context-menu/playlist-picker/queue-panel/lyrics/volume-overlay/toast/Up-Next-banner) are unaffected — all free-floating direct children of `MainWindow` with explicit/absolute positioning, no enclosing `Layout`, so a brief double-mount there is just a visual crossfade, not a layout hazard. Fixed by reverting exactly those 11 sites back to plain `FadeInTrigger` + instant unmount (their original Phase 117-120 behavior, entrance-only fade) via a second targeted Python script; kept `FadeGate` on the remaining 18. `main.slint`'s import line needed both `FadeGate` and `FadeInTrigger` again. **General lesson, written into CLAUDE.md's Slint gotchas territory**: a "delay unmount until the fade-out finishes" pattern is only safe for free-floating/absolutely-positioned elements — applying it to a child inside a shared `Layout` (`HorizontalLayout`/`VerticalLayout` with `stretch`/flex sizing) violates that layout's per-instant mutual-exclusivity assumption, and the failure (squeezed/split content) doesn't look like a fade-timing bug at first glance, making it easy to miss when applying the pattern broadly instead of site-by-site. `cargo build`/`clippy --workspace`/`test --workspace` all clean after the revert. |
| 124 — Widened Scroll/Animation speed presets to 500% (2026-07-13) | User asked to raise the ceiling from 300% to 500%. Added `"400"`/`"500"` to `SPEED_PCT_MODEL` (`settings.rs`) and both dropdowns' `model:`/`current-value`/`selected(v)` ternary chains in `settings.slint` (the mouse-click path hardcodes the value set inline, same as every prior widening of this range). Confirmed the Phase 123 layout regression can't reopen at the new ceiling: the 11 AppShell-hosted sites no longer do delayed-unmount at all (reverted to instant unmount in Phase 123), and the 18 `FadeGate` sites are all free-floating overlays where a longer overlap window is just a longer crossfade, not a layout hazard — so there's no speed value, however high, that reintroduces the squeezed-layout bug. `cargo build`/`clippy --workspace`/`test --workspace` clean. |
| 125 — Trimmed `Overview`/bio metadata: fixes an item-specific gap in expanded `StorylineSection` boxes (2026-07-13) | User reported one specific series' expanded overview left a visible gap before the season tabs below it, while every other item with an expandable overview looked correct — pointing at that item's data, not the shared component. Verified via a standalone throwaway Slint crate reproducing `StorylineSection` in isolation with the exact on-screen synopsis text: no gap, the box tightly wraps the text, so the component's own height computation (`max(txt.preferred-height, 66px)`) is correct. Root cause: `wrap: word-wrap` `Text` treats embedded `\n` as a real line break, so a trailing `\n`/`\n\n` left over in that item's `Overview` field (as scraped from TMDb/TVDB by Jellyfin) creates invisible blank line(s) at the end of the string that still count toward `txt.preferred-height` — nothing in Fjord trimmed this field before display. Fixed generally by adding `.trim()` at all 8 places `.overview`/bio text is read into `AppState`: `detail.rs`, `series.rs` (×2 — the `detail_overview` var and the `basic` item fallback), `season.rs`, `artist.rs`, `album.rs`, `collection.rs`, `person.rs` (bio) — sanitizing the data once at the boundary rather than patching the one affected screen. `cargo build`/`clippy --workspace`/`test --workspace` clean. |
| 126 — Now Playing "Up Next" strip: album art disappeared after the first track (2026-07-14) | User reported album posters in the Up Next strip vanish after the first track. Dispatched an Explore agent to trace it: `push_queue_display` (`main.rs`) rebuilds `queue-items` from scratch and is called from **~13 sites** — nearly every queue/playlist mutation, including both natural-advance paths in `playback.rs` — but only **one** of those (`on_refresh_queue_display`, wired to opening Now Playing/Queue Panel) pairs with `spawn_queue_poster_loading`, the function that actually fetches art. Every other call site defaulted every row to `has_poster:false` with no re-fetch, so it wasn't specifically a "second track" bug — it was general: *any* queue mutation after the initial art fetch wiped the strip. It only looked like a first-track-specific bug because opening Now Playing is what populates it correctly in the first place, and natural track-advance is the most common next mutation a listener hits. Fixed at the root: `push_queue_display` now snapshots the current model's known art keyed by `poster_id` before rebuilding, and carries it forward for any row whose `poster_id` matches — since every track on the same album shares one `poster_id` (`album_art_id`), a same-album advance is now free, with zero signature changes needed at any of the 13 call sites. For the remaining case (a genuinely new `poster_id` — the queue crossing into a different album), both natural-advance sites in `playback.rs` (gapless commit and the non-gapless playlist/queue-advance branch) now also call `spawn_queue_poster_loading` right after `push_queue_display`, matching what the one already-correct call site does. `cargo build`/`clippy --workspace`/`test --workspace` clean. |
| 127 — Bundled Noto symbol fonts: fixes missing icon glyphs on the HTPC (2026-07-14) | User reported several music-bar icons (Prev/Next/Queue/Vol-down/Vol-up) rendered as tofu boxes on the HTPC, though fine on the dev machine. Fjord never set any explicit `font-family` — it always relied entirely on whatever the host's fontconfig stack happened to cover, fine on a full desktop install, not guaranteed on a minimal HTPC. Root-caused precisely via `fc-query --format='%{charset}'` cmap inspection (not `fc-match`, which always returns *some* font even without real glyph coverage) against every one of the ~40 distinct non-ASCII symbol codepoints actually used across `crates/fjord-app/ui/*.slint` — found the dev machine's rendering "worked" via an inconsistent mix of `NotoSansSymbols2`, `NotoSansMath`, and even an incidental GNOME-dependency font (`AdwaitaSans`) covering different glyph subsets, none guaranteed present elsewhere; per explicit user preference, ruled out relying on `NotoColorEmoji` for the volume icons too — no color glyphs anywhere in the app. Confirmed two fonts together (`NotoSansSymbols2-Regular.ttf` + `NotoSansMath-Regular.ttf`, both Apache-2.0, both monochrome) cover every needed codepoint except two purely decorative ones in `PlaylistPicker` (fullwidth `＋`, one-eighth-block `▎`), which were swapped for plain ASCII (`+`, `|`) instead of justifying a third bundled font. Fixed by embedding both fonts as static assets (`assets/fonts/`, license text alongside) and adding `import "../../../assets/fonts/*.ttf";` to `main.slint` — Slint's own documented mechanism for exactly this: any font imported anywhere in the compiled project is registered as a glyph-coverage fallback for the whole app, automatically, no `font-family` needed on any consuming `Text`. Verified the font bytes are genuinely embedded (not silently dropped) by grepping the compiled binary for byte slices pulled directly from the source TTF files. Could not get a reliable live screenshot in the sandboxed dev environment (window-focus/compositor issues repeatedly captured unrelated desktop windows instead of the test app) — confidence rests on the documented Slint mechanism + direct cmap verification, not a rendered screenshot; flagged for an explicit visual check on the HTPC. `cargo build`/`clippy --workspace`/`test --workspace`/`build --release` clean. |
| 128 — Bundled Inter as the default text font + user-selectable Settings dropdown + hard-pinned Adwaita/Noto icon fonts (2026-07-14) | Follow-up to Phase 127, prompted by the user wanting to compare candidate primary UI fonts (built an HTML artifact rendering the same real Fjord content — title, tagline, meta line, buttons, storyline, music bar — once per candidate, using Fjord's exact theme colors). That comparison surfaced a real problem: several candidates (Adwaita Sans, Liberation Sans, Fantasque Sans Mono) turned out to have their *own* competing glyphs for common icons (♥ ✓ ▶ ★), confirmed via the same `fc-query` cmap technique as Phase 127 — a plain `primary-font, Noto-fallback` stack means icon appearance silently depends on which primary font wins the glyph race, the same inconsistency Phase 127 was meant to eliminate, just moved up a level. Settled through live iteration: ♥/✓ always render via bundled Adwaita Sans specifically (the user's preferred style, confirmed against the comparison artifact); every other icon always via the bundled Noto Sans Symbols2 (falling to Noto Sans Math automatically for what Symbols2 lacks); neither ever yields to whichever primary font is active. Went through `/plan` given the scope (7 files' worth of Rust wiring plus ~15 `.slint` files touched for the icon pins) — plan approved, then implemented in full: bundled `Inter-Regular/Bold.ttf` (titles use weight 700, so Regular-only would force a synthesized fake bold) + `AdwaitaSans-Regular.ttf` (both OFL-1.1, license verified via `pacman -Qi`/official GitHub release before bundling); `IconCircleButton` (the single shared component behind every ♥/✓ toggle *and* — via `now_playing.slint` — the whole transport row) computes its font-family from which glyph it's showing, one change routing all ~16 call sites correctly; `FjordButton` gained `icon-left`/`icon-right` properties kept separate from `text` specifically because Noto Sans Symbols2 has *zero* Latin-alphabet coverage (confirmed via cmap) — a combined string like `"▶︎ Play"` under one `font-family` override would've left the word falling through to an uncontrolled system fallback; every other standalone icon `Text` (context menu, browse, album, player, settings' passthrough warning, `MediaCard`'s badges) got the same treatment by hand, splitting icon+prose mixes into separate elements. Settings → UI gained a third row, "Text font" — a dynamic dropdown identical in shape to the pre-existing Audio output device row (`fetch_system_fonts()` mirrors `fetch_audio_devices()` exactly, shelling out to `fc-list : family` instead of `mpv --audio-device=help`, same startup-fetch/selected-callback/`settings.rs` special-casing throughout, traced via an Explore agent against the *real* `AUD_AUDIO_DEVICE` code paths before implementing rather than guessing). Bound via `MainWindow.default-font-family` — not `font-family`, which `Window` doesn't expose in Slint 1.16.1, only `WindowItem`'s `default-font-family` (a real build error caught this, not foresight) — set synchronously from `Config.ui_font_family` at launch so the app never renders with a placeholder font on the first frame regardless of how long the async `fc-list` enumeration takes. `cargo build`/`clippy --workspace`/`test --workspace`/`build --release` clean; confirmed both new fonts genuinely embedded (not silently dropped) via the same byte-slice-in-binary check as Phase 127. |
| 129 — Settings sidebar: FJORD block deduplicated, keeping the bold name over the muted label (2026-07-14) | User caught that the FJORD block in the Settings left pane printed "Fjord" twice — the muted `FJORD` section label, then a bold `Fjord` name line added the same session mirroring the `SERVER` block's label/name/version shape. First fix (wrong) dropped the bold name line and kept the muted label, leaving `FJORD` (small, muted) directly above the version with nothing bold anywhere in the block — user immediately caught this broke the visual parity with `SERVER`'s bold `jellyfin` line and asked "wasn't it the muted one that should've been removed?" Corrected: dropped the muted `FJORD` label instead, kept the bold `Fjord` name — it's self-identifying (literally the word "Fjord") the same way `SERVER`'s bold `jellyfin` line doesn't need its own extra label beyond the `SERVER` category header above it. Final shape: bold `Fjord` directly above the version string, matching `SERVER`'s bold-name-then-version weight exactly, just without a redundant category label since "Fjord" already *is* the category. Also confirmed and explained a separate, non-bug finding from the same conversation: "System default" doesn't resolve to the user's live KDE font (Fantasque Sans Mono Nerd Font) — `fc-match sans-serif` independently resolves to Noto Sans on this machine regardless, since Fantasque is set via KDE's own `kdeglobals` (Qt-toolkit-specific theming), which non-Qt toolkits like Slint don't read without dedicated portal integration; explicitly selecting Fantasque from the Text font dropdown (confirmed already working) is the correct way to get it, not "System default". `cargo build -p fjord-app` clean. |
| 130 — "FJORD" capitalized everywhere it appears as UI text (2026-07-14) | Follow-up to Phase 129's back-and-forth — user's underlying preference was broader than just the one Settings block: the product name should read as "FJORD" (uppercase) wherever it's shown as text in the app, not just "Fjord". Found all 4 literal occurrences via `grep -rn '"Fjord"' crates/fjord-app/ui/*.slint`: the Settings sidebar name (`settings.slint`, from Phase 129), the sidebar header next to the logo icon (`layout.slint`), the login screen's own logo text (`login.slint`), and the OS-level window title (`main.slint`'s `title:` property). All 4 changed to `"FJORD"` — none had any layout dependency on the string's length (same character count either way) so no follow-on sizing changes needed. The bundled logo SVGs (`fjord_01` etc.) already had "FJORD" baked into the vector art in uppercase from the start (per their own CLAUDE.md description), so this brings the plain-text occurrences in line with what the logo assets already did. `cargo build -p fjord-app` clean. |
| 131 — Fixed LoginScreen flashing to full opacity right as the window first appears (2026-07-14) | User noticed the login screen visibly flashed before the app finished opening. Dispatched an Explore agent to trace the exact startup sequence (`MainWindow::new()` → `apply_settings_to_window()` → saved-session check → `window.run()` in `main.rs`) against `FadeGate`'s own timer logic rather than guessing from the symptom. Root cause: `AppState.show-login` defaults to `true` before any config/auth-check runs, so LoginScreen's `FadeGate` (`gate3`) mounts and arms its 16ms `kick-timer` the instant `MainWindow::new()` runs; startup logic then flips to `show-connecting` synchronously if a saved session exists, all *before* `window.run()` starts the event loop. `FadeGate`'s `changed show` false-branch set `fade-opacity = 0` and started `unmount-timer`, but never cancelled the already-armed `kick-timer` — so once the event loop finally started, that stale timer fired anyway and unconditionally forced `fade-opacity` back to `1`, flashing LoginScreen to full opacity on top of ConnectingScreen for roughly one fade cycle. A genuinely general `FadeGate` bug (any instance can hit the same race if `show` flips true→false within the same 16ms window, not just at startup), not anything Login-specific, confirmed the font-bundling/`default-font-family` work from the same session was NOT implicated (both complete synchronously before `window.run()`, the sole show-trigger). Fixed by having the false-branch also set `kick-timer.running = false`. `cargo build`/`clippy --workspace`/`test --workspace` clean. |
| 132 — Seerr integration: Discover screen + media requests, all 4 auth methods, secrets encrypted at rest (2026-07-15) | Fjord's stated long-term direction is a general media client, not just Jellyfin — Overseerr/Jellyseerr integration was the next roadmap item, and research turned up that the two projects have since merged into a unified successor, **Seerr** (docs.seerr.dev, native Jellyfin/Plex/Emby support), matching the user's own live instance. Researched directly against Seerr's real OpenAPI spec (not memory) before designing anything; findings written to `SEERR.md` as a permanent reference alongside `JELLYFIN.md`. Went through `/plan` given the scope — the design pivoted twice during planning itself, both on direct user pushback rather than being decided upfront: (1) a first draft used a single static API key; the user asked for parity with every non-Plex sign-in method Seerr offers, so v1 ended up supporting all four (API key, Jellyfin username/password, Jellyfin Quick Connect — passwordless PIN pairing, and local Seerr accounts), via a new dedicated `ConnectSeerrScreen` rather than inline Settings fields, since Settings had zero free-text-entry precedent anywhere else; (2) the plan's first secrets design just matched the existing (plaintext) precedent for the Jellyfin token — the user rejected that, asked "must we store them in plaintext?", then asked whether the *existing* Jellyfin token should get upgraded too rather than leaving it weaker than the new credential beside it — yes: `secrets.rs` now encrypts `token`/`seerr_api_key`/`seerr_session_cookie` with AES-256-GCM (key derived via HKDF-SHA256 from the existing per-install `device_id` + a compiled-in salt, deliberately not an OS keyring — see CLAUDE.md's "Secrets at rest" note for the full reasoning and its honest threat-model limits), with a plaintext-fallback migration path so existing installs don't get silently signed out on upgrade, plus `config.json` now chmods to `0600` on every save. New `fjord-seerr` crate (mirrors `fjord-api`'s pure-async-HTTP shape). New Discover sidebar tab (`active-nav==6`, hidden unless enabled) reuses `CardItem`/`MediaCard` with one additive `availability` badge field rather than a parallel card type; posters come from TMDB's CDN directly (`~/.cache/fjord/discover_posters/`) — the first non-Jellyfin image source in the app. New `RequestDetailScreen` (movie/TV detail + a full per-season checkbox picker for TV, not "all seasons only" — another explicit user choice over the simpler default) reuses the existing `BackdropHero`/`PosterBlock`/`MetaLine` atoms. Session-expiry (401) on cookie-auth resets the connection and toasts "reconnect in Settings" rather than failing silently; API-key 401 (keys don't expire) just surfaces the error. One real bug caught during implementation, not foresight: `CardItem` always carries a `slint::Image` field and is therefore `!Send` regardless of whether that field holds real data, so search results have to be built as plain Send-safe metadata off-thread and only turned into `CardItem`s inside `invoke_from_event_loop` — a compile error caught this immediately. A second near-miss caught via the documented Slint gotcha rather than live-testing it into a crash: the season checklist's scroll-centering math initially read `self.height` inside a `changed`-tracked property on a *conditionally*-mounted child of a `VerticalLayout` — exactly the "Recursion detected" pattern already written up in CLAUDE.md — fixed by mounting that Rectangle unconditionally instead (it already collapses to 0px/zero rows when there's nothing to show). `cargo build`/`clippy --workspace`/`test --workspace`/`build --release` all clean. **Not yet tested against the user's live Seerr instance** — needs the real URL + credentials for each of the 4 auth methods to verify end-to-end (see PLAN.md Pending). |
| 133 — Three real Seerr bugs found live on first use, all fixed same day (2026-07-15) | User connected to their real Seerr instance (Phase 132's first live test) and immediately hit issues. (1) Keyboard navigation went completely dead everywhere in the app right after a successful connect — not just on the Discover screen, Settings too. Root cause: `ConnectSeerrScreen`'s `LineEdit`s hold genuine Slint keyboard focus while typing, and closing the screen after a successful connect never returned focus to the app's own global `fs` FocusScope that `AppState.handle-key()` is wired to — the exact same class of bug as the documented post-login `grab-keyboard-focus` calls in `main.rs`, just newly reintroduced by this screen's native text fields. Fixed at all three close paths (the success path in `seerr_auth.rs::commit_connection`, the Escape handler in `keys.rs`, the ✕/backdrop click handlers in `connect_seerr.slint`). (2) No keyboard way out of the Discover tab — user reported "no items showing up" together with being stuck, which turned out to be the SAME bug: Discover starts with zero results (before any query is typed), and the original `discover::handle_key` only ever handled `Action::Up` in that state, so Back/Left did nothing. Every other dashboard-tier tab gets a working sidebar-vs-content keyboard split for free from `dispatch_dashboard` (via `AppState.focused-section`), but Discover has its own `AppMode` (flat poster grid, not `dispatch_dashboard`'s `SectionRow` model) so never got this. Fixed by having `discover::handle_key` replicate the same `focused-section` contract itself. While fixing this, also found `discover.rs` logged literally nothing anywhere — added `warn!`/`debug!` logging throughout plus a toast for the no-client case. (3) That new logging immediately caught the *real* reason search wasn't working at all: `error decoding response body: missing field 'media_type'`. Every `Deserialize` struct in `fjord-seerr/src/models.rs` used snake_case Rust field names with no `#[serde(rename_all = "camelCase")]`, but Seerr's JSON is camelCase throughout — the mismatch was never caught pre-launch since the feature was never tested against a live server until now. Required multi-word fields failed loudly (`SearchResult.media_type`); `Option<...>` fields with `#[serde(default)]` elsewhere (`MovieDetails.poster_path` etc.) would have failed silently, just staying `None` forever. Fixed by adding `#[serde(rename_all = "camelCase")]` to all 11 `Deserialize` structs in the crate. Also, per user request: added a "CLIENT" label above "FJORD" in the Settings sidebar (mirroring "SERVER"/`jellyfin`), added a "SEERR" block below showing the connection method when connected, and moved the whole SERVER/CLIENT/SEERR stack from the top of the left pane to the bottom (`vertical-stretch: 1` spacer after the section list) so a third conditional block doesn't shift the section list around. `cargo build`/`clippy --workspace`/`test --workspace` clean. |

| 134 — Discover landing rows, 4K request toggle, Seerr version + sidebar reorder, second Recursion-detected fix (2026-07-15) | Follow-up round after the user could finally browse Discover (Phase 133's fixes). Feedback, addressed in one pass: (1) "shuld we not have rows for trending, poular moves and series upcomming" — added Trending/Popular Movies/Popular TV/Upcoming Movies/Upcoming TV as 5 `SectionRow`s shown when `discover-query == ""`, replacing the old search-only placeholder text. Confirmed from the OpenAPI spec that all 5 `/discover/*` endpoints return the same shape as `/search`, so `SeerrClient` gained 5 thin methods with no new models; fetched once per session via a newly-wired `AppState.nav-selected` (declared from the start, never actually connected to a handler until now). (2) "what dose the request button do? 2k or 4k? hdr?" — answer was "neither, no options at all"; added a 4K toggle chip next to Request (`POST /request`'s `is4k` field, the only quality knob Seerr's API actually exposes — confirmed there's no discrete HDR flag, that's a Radarr/Sonarr profile concern). (3) "we shuld display the version of seerr... below jellyfin under server not belove Fjord under client" — fetched via `GET /status` (an existing but never-wired `check_status` probe, repurposed), Settings sidebar reordered to SERVER→SEERR→CLIENT with the Seerr version deliberately unmuted per explicit request. (4) The search-with-a-space report and the Fjord-vs-Seerr-web-UI result-mismatch report couldn't be reproduced or root-caused without a live session — traced the space-handling code path thoroughly (key event → is_printable → debounced search → url crate's query_pairs_mut, which auto-percent-encodes) and found no bug; the existing `debug!("seerr: searching for {query:?}")` log line (added in Phase 133) will show the exact query string on the next attempt, which is the fastest real next step rather than further guessing. Building the landing rows' own scroll container surfaced a second, pre-existing "Recursion detected" risk in the search-results grid container (`fl-container`) — conditionally mounted inside a `VerticalLayout` while its `kb-y` property read its own `self.height` in a `changed` handler, the exact crash pattern already documented in CLAUDE.md's Slint gotchas, just not yet triggered live; fixed both containers using the gotcha's own documented remedy (`root.height` instead of `self.height`) before it ever crashed rather than after a report. `cargo build`/`clippy --workspace`/`test --workspace` clean. |

| 135 — Radarr/Sonarr tag picker for requests (2026-07-15) | User asked "but in the seerr web interface you can add tags?" right after the 4K toggle shipped claiming to be "the full extent" of request options — a direct, specific challenge to that claim, so it was checked properly rather than answered from assumption. The published OpenAPI spec's `POST /request` body has no `tags` field at all, same as it originally had no `media_type`/`is4k` — but Seerr's actual TypeScript source (`MediaRequestBody`) confirms `tags?: number[]` is real, and that available tags come from `GET /service/{radarr\|sonarr}/{id}`'s `tags: Tag[]` field (also undocumented in the spec), for whichever server has `isDefault: true`. Implemented: `SeerrClient::available_tags(media_type)` (2 sequential calls: list servers to find the default, then fetch its tags), wired into `open_discover_item`'s existing parallel fetch as a third, best-effort branch (empty tags list on any failure — permissions, no default server configured — never blocks opening the item). UI reuses the season checklist's exact interaction pattern (same row height, same unconditional-mount-with-zero-height shape, same toggle-row layout) rather than building a second one from scratch. Keyboard flow extended from 3 zones to 4: Back → button row (Request/4K) → tags (new) → seasons, each zone's edge navigation updated to hand off to/from the new zone and skip it cleanly when a given item has no tags configured. `cargo build`/`clippy --workspace`/`test --workspace` clean. |

| 136 — RequestDetailScreen restyled to match the app's other detail screens; BackdropHero deleted (2026-07-16) | User asked "why dose it the detail for requests dont look like the other details?" — investigated by reading `detail.slint`/`season.slint`/`collection.slint` side by side with `request_detail.slint` rather than guessing. Found `RequestDetailScreen` was built against `BackdropHero` (`widgets.slint`), documented in the widget-list comment as a "shared detail-page atom" but, confirmed by grep, never actually used by any of the other three — they'd all independently converged on a different pattern: a full-bleed `Image` filling the entire header `Rectangle` with a dark gradient overlay and the poster/title/buttons laid directly on top, versus `BackdropHero`'s separate fixed 300px band with content starting below it on plain background. Two more differences fell out of the same divergence: the Back button was an inline `Rectangle` chip in the scrolling content instead of the universal floating `FjordButton` overlaid at the top-left corner; the Request button was a hand-rolled `Rectangle` instead of reusing `FjordButton` the way Play/Resume do on `DetailPage`. Rebuilt the header to match the other three screens exactly and deleted `BackdropHero` from `widgets.slint` — it had zero remaining callers once this screen stopped using it. Left the 4K toggle as a custom `Rectangle` (not `IconCircleButton`): that component hard-pins non-heart/checkmark icons to Noto Sans Symbols2, which has zero Latin coverage, so routing "4K" through it would render as tofu. `cargo build`/`clippy --workspace` clean. |

| 137 — RequestDetailScreen full content redesign: rating badge, collapsible overview, Cast & Crew row, card/chip picker strips (2026-07-16) | Same-day follow-up: "i think we culd plan a better design for the descover detail page." Presented 3 mockup options (richer content only / picker redesign only / both); user picked "both." Went through `/plan` given the scope. Researched the real OpenAPI spec again and found `MovieDetails`/`TvDetails` both carry `voteAverage` and `credits: {cast, crew}` that `fjord-seerr` never deserialized, plus `Season.posterPath` (documented in `SEERR.md` from the original integration but never actually read by the code) — added `Cast`/`Crew`/`Credits` structs and the three missing fields, all `#[serde(rename_all = "camelCase")]` like everything else in the crate. Three content additions, all reusing existing shared atoms: `MetaLine.rating-label` gets a `"★ 7.9"` badge from `voteAverage`; the plain overview `Text` became a collapsible `StorylineSection` (same `expanded <=>` binding Detail/Season use); a new Cast & Crew row reuses `CastRow` verbatim, fed from a capped/deduped cast+crew list built with the *exact same* director/writer/actor take(2)/take(3)/take(12) precedence `detail.rs`'s Jellyfin cast already uses — `item-selected` deliberately left unbound (no TMDB-person detail screen exists) but the row stays keyboard-focusable for consistency. The season/tag checkbox *lists* became horizontal card/chip `Flickable` strips instead — the mockup showed a wrapping grid, but Slint has no flex-wrap layout and this codebase has no precedent for one, so both reuse the same `kb-x` scroll idiom `CastRow` already established; tag chips are fixed-width with elided text rather than size-to-content, since a variable-width chip would break the fixed-stride scroll math every scrollable row in this app relies on. Replaced the two-bool `in-tags`/`in-seasons` keyboard model with a single `request-detail-zone: int` (0=buttons,1=storyline,2=cast,3=tags,4=seasons) plus an `existing_zones()` helper in `discover.rs` — the old two-bool model was already flagged in its own code comment as "a smaller diff than refactoring," which stopped being true once two more optional zones were added. One real "Recursion detected" risk reasoned through and avoided before it ever ran (not caught live): the new strips' `kb-tag-x`/`kb-season-x` properties read a sibling Flickable's `.width`, matching `CastRow`'s own proven-safe pattern — but unlike `CastRow` (whose `fl` lives inside `CastRow`'s own always-stable component scope), the new strips' `tag-fl`/`season-fl` are declared directly inside the *same* conditionally-mounted block as the property reading them, with no explicit width to anchor to; fixed with the same `root.width`-derived remedy `discover.slint`'s own `fl-container`/`landing-container` fix established, rather than assuming "sibling read, not self read" made it automatically safe. Also had to route `SeasonItem`'s new `poster: image` field through the same Send-safety two-phase pattern (`CardItem`, `CastMember`) already used elsewhere — a plain `SeasonRow` tuple crosses the async task, the real `slint::Image` is only constructed inside `invoke_from_event_loop`; caught by the compiler (`*mut () cannot be sent between threads`), not foresight. `cargo build`/`clippy --workspace`/`test --workspace`/`build --release` all clean. |

| 138 — Request Options modal: 4K/tags/seasons moved off the page into an overlay opened by the Request button (2026-07-16) | Same-day follow-up: "can we have the request button open a overlay with the settings for the request istead of having them all over the page?" Moved the 4K toggle, tag-chip strip, and season-card strip (all just added in Phase 137) out of `RequestDetailScreen` entirely into a new `RequestOptionsOverlay` component, opened by the Request button instead of submitting directly. Reused the established small-modal shape already in the app (`PlaylistPicker`, `context_menu.slint`) rather than inventing a new one: dim `#00000088` backdrop (click closes), centered `surface-raised` box with drop-shadow, a Cancel/Request confirm row at the bottom. `RequestDetailScreen` itself shrank back to a single Request button — `request-detail-btn-focused` (the old 2-button Request/4K row) was deleted outright since there's only one button left; `request-detail-want-4k` stayed (now toggled from the modal). Rather than fold the modal into the page's own `request-detail-zone` flow, it got its own `AppMode::RequestOptions` (checked ahead of `RequestDetail` in `active_mode()` — the modal can only ever be open while already on that screen, so no ordering conflict) and its own independent zone numbering (`request-options-zone`: 0=Quality (2K/4K), 1=tags, 2=seasons, 3=confirm — `existing_option_zones()`/`handle_key_request_options` in `discover.rs`), deliberately separate from the page's `existing_zones()` rather than continuing its numbers, since 0-4 already meant something else there before this change. The tag-chip/season-card Flickable strips are unchanged from Phase 137 apart from their width anchor: `root.width - 96px` (the full page width) became `root.dialog-w - 64px` (a new fixed 460px property on the modal's own root) — same "read a stable, non-conditional value" remedy for the Recursion-detected gotcha, just anchored to the modal's own fixed dialog width instead of the page's `AppShell`-derived one. `cargo build`/`clippy --workspace`/`test --workspace` clean. |

| 139 — Request Options visual polish: Quality (2K/4K) pair, pill tag chips, secondary Cancel button (2026-07-16) | Same-day follow-up, prompted by a screenshot: "not the best desing can you make it look better? and now 2k option?", then "and quality?" confirming the right shape was a labeled Quality section with 2K/4K as a pair, not a single ambiguous on/off 4K toggle. Three fixes: (1) the single 4K toggle became two side-by-side buttons under a "Quality" header — exactly one always visibly selected (accent fill vs muted `surface-overlay` + thin border); Left/Right pick the value directly (no separate keyboard cursor to track, since the selected button already is the keyboard position — unlike the Cancel/Request row, this isn't an action that needs a Confirm step on top of the L/R move). (2) Tag chips changed from `Theme.radius` corners with no border when idle (read as flat gray blobs) to a true pill (`border-radius: self.height / 2`) with an always-visible 1px border, 2px when focused. (3) Cancel was a plain `FjordButton` — always filled accent blue regardless of caller — so it looked just as "primary" as Request with no visual cue for which is the safe/reversible choice; replaced with a hand-rolled outlined ("ghost") button (transparent fill, border, muted text), the standard primary-filled/secondary-outlined confirm-dialog convention, without adding a new variant to the shared `FjordButton` widget itself (only site in the app that's needed one so far). Also widened the dialog 460→480px, bumped internal spacing 16→20px, and added a divider above the confirm row matching the section-divider convention used elsewhere on the page. `cargo build`/`clippy --workspace`/`test --workspace` clean. |

| 140 — Quality profile picker (WEB-DL/Remux/etc.) in the Request Options modal (2026-07-16) | Same-day follow-up: "no other quality options? like web-dl ,remux etc?" The 2K/4K pair only picks which Radarr/Sonarr *server instance* handles a request (Seerr's `is4k` routes to a separate configured 4K instance, if one exists) — it says nothing about which *quality profile* on that instance is used, which is where source/codec preferences like WEB-DL vs Remux actually live. Confirmed `POST /request` already accepts `profileId` (present in the OpenAPI spec's request schema, unlike `is4k`/`tags` which are missing outright) and that `GET /service/{radarr\|sonarr}/{id}` — the same endpoint already used for tags — also returns a `profiles` array; the spec shows it as a single object with no array wrapper, but Seerr's TypeScript source confirms `QualityProfile[]`, the same class of spec-imprecision this project keeps re-verifying against source. `fjord_seerr::SeerrClient::available_tags` was renamed `available_request_options(media_type, is_4k)`, now returning both tags and profiles from one fetch; `default_service_server` gained an `is_4k` parameter and now prefers a server whose own `is4k` flag matches the requested tier (falling back to any default server, so single-instance setups are unaffected) — an admin can configure a dedicated 4K instance alongside the regular one, each independently `isDefault`. Deliberately does **not** re-fetch tags/profiles when Quality is toggled to 4K in the modal — a documented scope cut (see `SEERR.md`), since that would need its own loading state for a fairly advanced dual-instance setup the user didn't actually ask about. New "Profile" chip strip in `RequestOptionsOverlay`, between Quality and Tags: radio-select (exactly one highlighted, clicking replaces rather than toggles) via a new `ProfileItem {id, name}` struct and one `request-detail-selected-profile-id` int, unlike `TagItem`'s per-row-bool multi-select; row 0 is always a synthetic "Default" entry (`id: 0` — real profile ids start at 1) prepended in Rust so there's an explicit "no override" option; the whole picker hides when the server has zero real profiles configured. This is a new optional zone inserted between Quality and Tags, so `existing_option_zones`/`handle_key_request_options` renumbered from (0=Quality,1=Tags,2=Seasons,3=Confirm) to (0=Quality,1=Profile,2=Tags,3=Seasons,4=Confirm) — every zone-number literal across `discover.rs` and `request_detail.slint` moved in lockstep (the Slint side via a careful highest-to-lowest-number `sed` pass, to avoid double-substitution collisions). `cargo build`/`clippy --workspace`/`test --workspace` clean. |

| 141 — Quality toggle now swaps in pre-fetched tags/profiles for the other tier, no re-fetch (2026-07-16) | User: "this shuld be fixed" — Phase 140's documented dual-instance limitation (toggling Quality to 4K didn't re-fetch tags/profiles for a genuinely separate 4K-tier Radarr/Sonarr instance) turned out worth fixing properly rather than leaving as scoped-out. Rather than a live re-fetch-on-toggle (its own loading state, and a real race if Quality is toggled rapidly before a fetch resolves), both tiers are now fetched **up front**: `fjord_seerr::SeerrClient::available_request_options_both_tiers(media_type)` replaces the single-tier `available_request_options`, resolving both the regular and 4K default server from one `/service/{kind}` list call (`pick_default_server`, pure/no I/O) then fetching server details — deduplicated to a single fetch when both tiers resolve to the same server (the common single-instance setup), or two fetches in parallel (`tokio::try_join!`) when a genuinely separate 4K instance exists. `AppState` gained a parallel "alt" set (`request-detail-tags-alt`/`-profiles-alt`/`-selected-profile-id-alt`) holding whichever tier isn't currently displayed. New `discover::set_quality(g, want_4k)` swaps the active and alt sets (only when the value actually changes) and resets both focus cursors — called from `handle_key_request_options`'s zone-0 Left/Right directly, and from a new `request-detail-set-quality(bool)` AppState callback the 2K/4K buttons' mouse-click handlers now go through instead of setting `want-4k` directly (keyboard and mouse both need the swap, but only the mouse path needed a new callback since the keyboard handler already runs in Rust). Because the swap moves the whole `TagItem`/`ProfileItem` *models* — which carry `selected`/the chosen id alongside each row — a user's picks on one tier survive toggling away and back, for free. `cargo build`/`clippy --workspace`/`test --workspace` clean. |

| 142 — Three live-testing fixes: search-with-space 400, a real click-handler zone-numbering bug, Request button sizing (2026-07-16) | (1) User hit a real 400 Bad Request searching anything with a space, with the actual failing URL in the toast (`query=why+`) — the exact bug Phase 134 tried and failed to reproduce. Root cause confirmed against Seerr's real `/search` route source (not assumed): the route reads `req.query.query` and passes it straight to TMDB with no validation and no `+`-to-space decoding anywhere in that path, but `fjord_seerr::SeerrClient::search` built its URL via `url`'s `query_pairs_mut()`, which follows the WHATWG form-urlencoded serializer and always encodes space as `+` — a literal `+` character survived all the way to TMDB, which rejected it. Fixed by percent-encoding the query by hand (`percent_encoding::utf8_percent_encode` with `NON_ALPHANUMERIC`, new direct dependency) and setting the query string via `Url::set_query` instead — `%20` is unambiguous under RFC 3986 regardless of which layer handles it. (2) User: "no differenses in tags or profiles when i switch between 2k and 4k" — re-verified Phase 141's swap logic and tier-matching line by line and found both sound (identical lists across tiers on a single-Radarr-instance Seerr setup, the common case, is the *correct* result of `pick_default_server`'s fallback, not a bug — not independently confirmed against this user's actual `Settings → Services` config, which needs their own Seerr admin access). But did find a real bug while re-checking: Phase 140's `sed` renumbering pass (which shifted every `request-options-zone == N` *comparison* to the new 5-zone numbering after Profile was inserted) matched `==` only, silently missing the `request-options-zone = N;` *assignments* inside each row's mouse `clicked` handler — a different substring, same underlying value. Clicking a Tag chip set focus to zone 1 (now Profile, was Tags pre-renumbering) instead of 2, a Season card set zone 2 (now Tags) instead of 3, Cancel/Request set zone 3 (now Seasons) instead of 4 — keyboard focus landing on the wrong row after a mouse click, very plausibly what prompted "this is still weird." Fixed all 4 by hand, found via `grep -n "request-options-zone = [0-9]"` rather than trusting the earlier `sed` pass had caught everything. (3) "the request button on detail is a bit small for the text" — confirmed `FjordButton` has zero internal horizontal padding, so an instance with no explicit `width:` sizes to exactly the text's bounds; the main-page Request button (not the modal's, which already had `width: 100px`) never got one. Fixed with a scoped `width: 130px` on that one instance (fits "Requesting…" too, so no resize when a request starts) rather than adding padding to `FjordButton` itself, which has 20+ call sites across the app, several tuned assuming zero internal padding — a global change risked clipping/regressing all of them without re-auditing every site. `cargo build`/`clippy --workspace`/`test --workspace` clean. **Fix (1), the `%20` search encoding, live-confirmed working 2026-07-18.** |

| 143 — pick_default_server fallback fixed: a lone per-tier server without its own isDefault flag was masked by the wrong tier's default (2026-07-17) | Phase 142's "identical lists across 2K/4K is probably correct, single-instance setup" conclusion was wrong — user pushed back with real Seerr admin screenshots ("they have different profiles and tags") showing genuinely different quality-profile and tag lists for a movie's regular vs 4K request in Seerr's own web UI. Re-verified the data model directly against Seerr's real source rather than re-guessing: `server/lib/settings/index.ts`'s `DVRSettings` interface confirms `is4k`/`isDefault` are independent per-server-entry booleans, `server/routes/service.ts`'s list-endpoint handler confirms both are included in the response Fjord parses, and the by-id endpoint confirms it makes a **live** call to the actual Radarr/Sonarr instance per server id — so the underlying API fully supports what the screenshots show. The actual bug: `pick_default_server`'s fallback only had two steps (tier+isDefault match, then any isDefault regardless of tier) — a dedicated 4K (or regular) instance that isn't itself marked "Default" (a legitimate config; nothing requires marking a tier's only instance as its own default) fails the first step and falls straight through to the second, which doesn't check tier at all and can return the *other* tier's server. Fixed with a three-step cascade: tier+isDefault match, then **any server matching the tier regardless of isDefault** (new), then any isDefault server regardless of tier (the original single-combined-instance fallback, demoted to last resort). Since this couldn't be fully confirmed without the user's exact `Settings → Services` config, also added a `tracing::debug!` line in `available_request_options_both_tiers` logging every resolved server's `(id, isDefault, is4k)` plus which id was picked per tier, visible in `fjord.log` once Log level is set to Debug — so the next report is confirmed from real data rather than guessed a third time. `cargo build`/`clippy --workspace`/`test --workspace` clean. |

| 144 — Discover "Requested" landing row + search results already in the library open the real item (2026-07-17) | User: "can we add a row to the descover dashbord with requested but not yet avilible and if you search thing that you have gets the in library witch is nice but culd it not open the item in library instead of discover detail?" Two features. (1) **Requested row**: `GET /request` (confirmed via the real spec — `filter`/`sort`/`mediaType` params) only carries a `tmdbId` per item, no title/poster — `SeerrClient::requested_not_available(take_per_type)` calls it twice (once per mediaType, since `MediaRequest` has no type field to infer from) and filters client-side (excludes `status==3` DECLINED and `MediaInfo.status` 5/6 AVAILABLE/DELETED) rather than depending on Seerr's own blended `filter` enum. `discover::fetch_requested_row` resolves each kept request's title/poster via its own `/movie\|tv/{id}` detail fetch (bounded `Semaphore(6)`, same shape as the Cast & Crew fetch), capped at 20 newest-first, then folds into the *same* `metas_per_row`/`poster_jobs` machinery the other 5 rows already use — the rest of `ensure_discover_landing` needed zero changes despite this row's completely different data-fetch shape. Landing rows went 5→6: found and fixed a hardcoded `fs + 1 < 5` literal in `handle_key_landing`'s Down handler (now derives from `lens.len()`) while extending. (2) **Open the real library item when already available**: confirmed via Jellyfin's own `ItemsController` source there's no server-side "find by provider id" query (`hasTmdbId` etc. are presence-only booleans) — documented in `JELLYFIN.md`'s Non-obvious behaviors so this isn't re-discovered later. `MediaItem` gained `provider_ids: HashMap<String,String>`, `get_all_movies`/`get_all_series` both request `ProviderIds` now; `discover::find_local_item` scans the cached `all_movies`/`all_series` for a `provider_ids["Tmdb"]` match, checked first in `open_discover_item` before the Seerr client is ever touched — redirects straight to `detail::open_detail` when found, covering all 8 call sites that can open a Discover item since they all funnel through that one function. Real gap caught while implementing: `ws.rs`'s delta-sync upsert path writes into the same `all_movies`/`all_series` but wasn't touched — without `ProviderIds` added there too, a `LibraryChanged` refresh of an already-cached item would silently wipe its `provider_ids`, the same "field dropped on one of two landing points" class of bug as Phase 100's unplayed-count note. `cargo build`/`clippy --workspace`/`test --workspace` clean. |

| 145 — Live-verified mismatched 2K/4K profiles/tags are real server config, not a Fjord bug; Request Options strips now scale to short lists (2026-07-17) | User, live-testing the Request Options modal: "why not all the profilece and why no tags for 2k," "why default and ultra-hd only i hade any here too," "wh is the overlay not scaling to the items." Rather than guess a fourth time, queried the user's live Seerr instance directly (`/service/radarr`, `/service/radarr/{id}`, `/service/sonarr/{id}`, authenticated with the session cookie already saved in `~/.config/fjord/config.json`, decrypted locally the same way `secrets.rs` does for this one diagnostic pass — nothing persisted). Confirmed conclusively: the 2K Radarr instance genuinely has exactly 3 profiles (Any/HD-720p/HD-1080p) and zero tags configured server-side; the 4K Radarr instance genuinely has exactly 1 profile (Ultra-HD, no "Any") and 3 tags (dv/hdr/sdr) — Fjord renders the complete, accurate list in both cases, nothing dropped client-side. (4K Sonarr differs again — it has both Ultra-HD *and* "any" — so a "why does 4K have 'any' sometimes" impression likely came from mixing a movie result with a TV one; they're independently configured Radarr/Sonarr pairs.) This is the expected, correct behavior of Phase 143's `pick_default_server` fix — divergent per-tier config is exactly what that fix restored visibility into; any change to make the lists match is a Radarr/Sonarr admin config change, out of Fjord's scope. The one genuine bug in the same report: `RequestOptionsOverlay`'s three `Flickable` strips (profile/tag/season, `request_detail.slint`) had a bare fixed `width: root.dialog-w - 64px`, so the live 4K-Radarr case above (a single profile chip) still rendered a full-width, mostly-empty strip. Fixed by widening each to `min(root.dialog-w - 64px, *-hbox.preferred-width)` — safe against the self.width-in-conditional "Recursion detected" trap since every chip/card has a fixed pixel width, making `*-hbox.preferred-width` deterministic and independent of the Flickable's own size (same reasoning the pre-existing `viewport-width: *-hbox.preferred-width` binding already relied on). `cargo build`/`clippy --workspace` clean. |

| 146 — "Open in library" now works for movies too; MediaStatus's real numbering fixed (was off-by-one past Available) (2026-07-17) | User: "the in library open detail in library instead of descover works for series but not movies. and the requested row shows items that have been requested but are availible why?" Two bugs, both found by live-querying the user's Seerr instance rather than guessing. (1) `all_series` is refreshed unconditionally on every login (`spawn_auto_login`'s startup fetch), but `all_movies` is lazy-fetched only once the Movies library grid is opened this session (`spawn_library_fetch`'s nav==2 branch) — deliberate, to avoid an eager full-library poster sweep on every login, but it meant a session that goes straight to Discover without ever opening Movies keeps whatever stale `movies.json` is on disk, which (for any install that hasn't opened Movies since Phase 144 added `ProviderIds`) has it empty on every row — `find_local_item` then silently never matches a movie. Fixed by factoring the movies-list fetch out of `spawn_library_fetch` into `spawn_movies_list_fetch(state, ww, rt, with_posters: bool)`, called from `discover::wire_discover`'s `on_nav_selected` (`with_posters: false`, since a poster sweep for the whole movie library just because Discover was opened would be real unnecessary cost) alongside `ensure_discover_landing`, sharing the same `movies_fetched` guard `spawn_library_fetch`'s own nav==2 branch (`with_posters: true`) uses — whichever caller runs first wins. (2) Queried the user's live Seerr `GET /request` directly and found requests with a raw `media.status` of 7, a code `fjord_seerr::MediaStatus` didn't recognize (`from_code` → `None`). Checked Seerr's real source (`server/constants/media.ts`): the actual enum is `Unknown=1, Pending=2, Processing=3, PartiallyAvailable=4, Available=5, Blocklisted=6, Deleted=7` — Fjord's had `Deleted=6` with no `Blocklisted` at all, off by one past `Available` since the enum was first written. `requested_not_available`'s exclusion filter (`Some(Available \| Deleted)`) never matched a real `Deleted` (7) request, so deleted-but-still-request-tracked items stayed listed as "not yet available." Fixed the enum to the real 1-7 numbering; added `Blocklisted` to `discover::availability_tag`'s no-badge arm too. `cargo build`/`clippy --workspace`/`test --workspace` clean. |

| 147 — Discover search now paginates past page 1 (2026-07-17) | User: "now searching for 'why' gives me 15 results on fjord but 1000 results on seerr webinterface." Root cause: `spawn_discover_search` called `client.search(&query, 1)` and stopped — never fetched a second page, so any search with more than one page of TMDB results (common words routinely have hundreds) was silently capped far below what Seerr's own web UI shows via its own infinite scroll over the same endpoint. Fixed with on-demand pagination rather than eagerly fetching everything up front: `FjordState` gained `discover_search_page`/`discover_search_total_pages`/`discover_search_loading_more`; new `discover::spawn_discover_search_more` fetches the next page and appends (vs. `spawn_discover_search`'s replace) via a new `AppState.discover-load-more()` callback, triggered from `discover::handle_key`'s existing Down-at-last-row branch (previously a bare `false`). `keys.rs::handle_key`'s per-mode arms don't hold `state`/`rt`, so the fetch is dispatched from wherever the callback is registered (`wire_discover`) instead — the same pattern several other keyboard-triggered async actions in this codebase already use. Extracted the bounded-concurrency TMDB poster fetch into a shared `fetch_and_patch_posters` helper used by both search functions (the append case pre-offsets each poster job's row index by the grid's row count, read synchronously on the UI thread before the fetch starts — can't be recomputed after, since `discover-results` can't be touched off-thread). Staleness reuses the existing `discover_gen` counter (a fresh search bumps it before its own debounce sleep even starts) rather than a separate query-string guard. `cargo build`/`clippy --workspace`/`test --workspace` clean. |

| 148 — Search auto-fills the grid; request-detail metadata panel (Status/dates/language/countries/network/Currently Streaming On) (2026-07-17) | User: "search shuld fill the screen so you not need to go to the end of a row to get new items." Added `discover::maybe_autofill_grid`: after every search commit (page 1 or an appended page), if the grid has fewer than `DISCOVER_AUTOFILL_ROWS(6) * library-cols` rows it fires the same `discover-load-more()` callback the Down-triggered path (Phase 147) already uses — chains automatically until the grid is full or the search runs out of pages, no new geometry plumbing needed. Separately, user shared a screenshot from another app showing a richer detail panel (Status/First-Air-Date/Next-Air-Date/Original-Language/Production-Countries/Network/Currently-Streaming-On/rating badges) and asked to add "some of this info" — asked which subset given the real scope difference (metadata rows need no new API calls; RT scores need a new `/ratingscombined` call per item; streaming providers need region handling + a new logo-fetch pipeline); user picked metadata rows + streaming providers, skipped RT scores. Verified every field against Seerr's real source (`server/models/{Movie,Tv,common}.ts`, not the OpenAPI spec) before implementing, then live against the user's own `/movie/{id}`/`/tv/{id}`/`/settings/public` responses after — `fjord-seerr::MovieDetails`/`TvDetails` gained `status` (TMDB's own production-status string, kept as a separate field/property from the existing Seerr-availability `MediaStatus` to avoid conflating them), `original_language`, `production_countries`, `watch_providers`, plus TV-only `next_episode_to_air`/`networks`; new `PublicSettings.streaming_region` (`GET /settings/public`, genuinely unauthenticated) resolves which `watch_providers` region to show, cached per-connection with a `"US"` fallback when unconfigured (confirmed live: this user's instance has it unset). New `RequestDetailScreen` "Details" section, purely read-only (no keyboard zone — reached by scrolling like `MetaLine`'s genre text), gated on `has-details` so a sparse item shows nothing extra. `cargo build`/`clippy --workspace`/`test --workspace` clean. |

| 149 — Streaming Region became a real Settings row — and Phase 148's "needs Seerr admin" claim was wrong (2026-07-17) | User: "could you not set it in Fjord and send it to Seerr?" then, after being told the server-wide default at `/settings/main` needs `Permission.ADMIN`: "why would you need to be seer admin to chang a user setting like region?" — a fair challenge that turned out to be right. Re-checked Seerr's real source instead of defending the first answer: `server/routes/user/usersettings.ts`'s `GET`/`POST /user/{id}/settings/main` is gated by `isOwnProfileOrAdmin()`, not `Permission.ADMIN` — any user can read/write their OWN `streamingRegion` with no elevated permission, confirmed live against a real owner-level account (`GET /auth/me` → `id: 1`; the POST handler's special-case for `user.id === 1` only blocks a *different* user editing the owner, not the owner editing themselves). `discover::resolve_streaming_region` (Phase 148) was rewritten to use this per-user path — also more correct on its own terms, since it matches what Seerr's own frontend actually prioritizes (`streamingRegion: data?.streamingRegion \|\| 'US'`, found in `SettingsMain/index.tsx`, reading the USER settings response, not the admin default). `fjord_seerr::PublicSettings`/`get_public_settings` removed as dead code once nothing called them. New Settings → Integrations → Streaming Region row (`INT_STREAMING_REGION`, only rendered while connected), same dynamic-dropdown shape as `UI_FONT_FAMILY` but with a genuine network write: Seerr's `POST /user/{id}/settings/main` replaces the whole object with no partial-patch semantics (confirmed from source — `user.username = req.body.username` unconditionally), so `fjord_seerr::UserGeneralSettings` always round-trips GET → mutate one field → POST the whole struct back, verified live that a real account's `username: null` round-trips unchanged. New `main.rs::spawn_streaming_region_fetch` (`pub(crate)`) populates the dropdown at startup (if already connected) and after every fresh connect (`seerr_auth.rs::commit_connection`, all 4 auth flows) — the latter needed a `tokio::runtime::Handle::current()` captured from inside each flow's own running async task (the outer closure's `rt` capture doesn't reach the nested `invoke_from_event_loop` closure `commit_connection` runs from) threaded through all 4 call sites. `cargo build`/`clippy --workspace`/`test --workspace` clean; `/auth/me`, `/watchproviders/regions` (139 entries), `/user/{id}/settings/main` all re-verified live. |

| 150 — Watch Trailer (Discover / RequestDetailScreen), via yt-dlp + mpv's bundled ytdl_hook (2026-07-17) | User: "can you go to plan mode and inwestiget if watch trailer culd be intigrated?" Investigated and confirmed Seerr's `/movie/{id}`/`/tv/{id}` responses (already fetched by `open_discover_item`) carry `relatedVideos: {site, key, name, size, type, url}[]` with a fully-formed YouTube watch-page URL (confirmed from Seerr's real source, `server/models/common.ts`'s `mapVideos`/`siteUrlCreator`) — but a YouTube webpage isn't something mpv can demux directly; only mpv's bundled `ytdl_hook` (which shells out to `yt-dlp` on `PATH`) can turn it into a real stream. Flagged this to the user, who initially assumed the URL was already stream-shaped ("dont we just get an url to play in mpv?"). Three scope decisions from planning: (1) Discover only, not also already-owned Jellyfin items via `RemoteTrailers` — fully served by data already fetched, doubling the work for no benefit; (2) `yt-dlp` optional, two layers — `optdepends` in `PKGBUILD`, plus proactive detection (`main.rs::detect_yt_dlp`, shells out to `yt-dlp --version` once at startup off the UI thread, same shape as `fetch_audio_devices`/`fetch_system_fonts`) hides the Trailer button entirely when absent, with the existing reactive toast (`"Couldn't play trailer — is yt-dlp installed?"`) as a second layer for the rarer case where detection succeeds but a specific video still fails to resolve; (3) trailer quality is a new Settings → Integrations → Trailer Quality dropdown (Best/1080p/720p/480p, `Config.trailer_quality`, plain static dropdown unlike Streaming Region's dynamic-fetch one), not hardcoded — user: "make it configurble." `fjord_seerr::Video{kind, url}` added (only `type`/`url` modeled, same "only what's consumed" style as `NextEpisode`); `discover::find_trailer_url` prefers `Trailer`, falls back to `Teaser`. New `playback::play_trailer` (not routed through `start_playback`, which is deeply Jellyfin-coupled — intro/credits fetch, series next-up, auto-mark-played) tears down any currently-playing item first (reporting its real stop position to Jellyfin, same as any other playback replacement) then calls `Player::new` with `vs.client`/`vs.playing_series_id` left `None` — the existing mechanism `report_playback_progress` and the series-advance block already key off to skip themselves for a client-less session, reused rather than adding a new `is_trailer` flag. `start_playback`'s own ~25-field `VideoState` reset block was extracted into a shared `reset_video_state_for_playback` (called by both `start_playback` and `play_trailer`) rather than hand-duplicated, to avoid the "many small fields, easy to miss one" class of bug this project has hit before; confirmed `play_start` must be set for the existing generic buffering-overlay logic (`wire_mpv_timer`'s `initial_stall`, driven purely by live mpv properties + an `Instant`, no Jellyfin coupling) to show "Loading…" during yt-dlp's resolution wait, and confirmed via every existing Play button's call shape that `Player::new` is already fire-and-forget from the Rust side (the actual load happens on mpv's own thread) so no new async wrapper was needed. New `PlayerConfig.ytdl_format: Option<String>` (only set by the trailer call site) maps the quality setting to an mpv `ytdl-format` selector string. `RequestDetailScreen` gained a `▶︎ Trailer` button as a third sibling in `req-row` (independent of request status — visible whether or not the item's already requested/available), reachable via `request-detail-btn-focused` (0=Request,1=Trailer) in zone 0; both buttons' `clicked` handlers now explicitly set this focus state, the exact mouse/keyboard focus-desync bug class already documented and fixed once on this same screen (Phase 142's zone-numbering fix) — skipping it here would have reintroduced it the moment a second focusable element existed in the row. `cargo build`/`clippy --workspace`/`test --workspace` clean; not yet live-tested (needs `yt-dlp` installed and a real Discover trailer) — see Pending. **Live-confirmed on the dev machine (2026-07-18):** trailer playback ("trailer watching is working good") and Trailer Quality actually capping resolution ("the qualityu setting is working"), both user-confirmed. Still unverified: the NVIDIA HTPC target specifically (dev machine is AMD/Vulkan) — see Pending. |

| 151 — Streaming Region write path 500'd on first real use — NOT NULL constraint on an unset `locale`, plus SEERR sidebar wording/color (2026-07-17) | User, testing the settings sidebar screenshot from Phase 149-150: "the streaming region setting do not work" — toast: `"Couldn't update streaming region: HTTP status server error (500 Internal Server Error)"`. Phase 149's claim that the GET→mutate→POST round-trip was "confirmed safe live" turned out to be wrong (corrected in `SEERR.md`) — only the read paths had actually been POST-tested; the write itself had never been exercised. `update_user_settings` called `error_for_status()` before reading the response body, so Fjord's own toast showed nothing but the bare status. Diagnosed by hand-crafting the identical POST directly against the user's live instance (decrypting the saved session cookie locally the same way `secrets.rs` does, one-off, nothing persisted — same technique used earlier this session for the Radarr/Sonarr profile investigation): the real error was `{"message":"SQLITE_CONSTRAINT: NOT NULL constraint failed: user_settings.locale"}`. Root cause: this account had never saved anything under Seerr's own Settings → General web UI (`GET /auth/me` → `"settings": null`, an unremarkable, common state) — for such an account `GET .../settings/main` **omits** `locale` entirely instead of returning `null` (confirmed live), so `UserGeneralSettings.locale: Option<String>` deserializes it correctly as `None` — but plain `Option` serialization re-emits `None` as JSON `null` on the way back out, and `user_settings.locale` is a `NOT NULL` column server-side, so the explicit `null` reaches the SQL layer unchanged and the whole write fails. Isolated field-by-field with hand-crafted POSTs against the same instance: omitting the key succeeds (Seerr falls back to its own column default, implicitly creating the settings row), sending `"locale": null` explicitly 500s every time. **Fix:** every field on `UserGeneralSettings` now carries `#[serde(skip_serializing_if = "Option::is_none")]` — applied uniformly to all eight fields, not just `locale`, since the same mismatch could exist on any column on a different Seerr version; `update_user_settings` also now reads the response body on failure instead of discarding it, so a future 500 surfaces Seerr's real message through the toast. Live-verified against the real instance: omitted-key POST succeeds, explicit-null POST still reproduces the exact same 500 (confirming the diagnosis), and the account's `streamingRegion` is now genuinely `"SE"` server-side (set during diagnosis — matches what the user would have picked anyway). Same report, separate fix: the Settings sidebar's SEERR block showed `"Connected via Jellyfin login"` — user: "whow you loged in to seerr is not relevant and then seerr shuld not be muted." The auth-method detail is still shown where it's actually actionable (the Integrations Connect/Disconnect row's subtitle); the sidebar block was simplified to a plain `"Connected"`, recolored `Theme.accent` (was `Theme.text`) to read as a deliberately prominent status indicator, consistent with the seerr-version line immediately below it already being deliberately non-muted. `cargo build`/`clippy --workspace` clean. |

| 152 — SEERR sidebar "Connected" line dropped entirely; Display Language + Discover Language settings; seerr-enabled toggle actually turns Seerr off now (2026-07-17) | User, on seeing Phase 151's "Connected" fix: "why cant you see how i want it? there is no seerr if you are not connenected so why even. dont assume ask." Right — that block only ever renders `if seerr-connected`, so restating "Connected" under it is pure redundancy. Asked (not guessed) which of 3 options fit; answer: drop the line, `SEERR` header + version is the whole block now, matching `SERVER`/`CLIENT`'s 2-line shape. Same conversation, much bigger ask: "now mabey add all these settings to the seerr settings? and all seerr settings shuld be gated if seer is on and connected" — then "dont assume ask" again before a structured question could even be answered. Two real unknowns resolved by reading Seerr's actual source (`seerr-team/seerr`, `develop`) rather than guessing: (1) Does Display Language (`locale`) do anything inside Fjord, which never renders Seerr's UI text? Yes — confirmed from `server/middleware/auth.ts`/`movie.ts`/`discover.ts`: Seerr defaults every TMDB `language` param to the connected user's `locale` when Fjord's client doesn't pass one (it never does), so this setting controls the language of every Discover title/overview. Live-verified beyond doubt: POSTing `locale=sv` then re-fetching `GET /movie/550` returned a Swedish overview immediately — reverted to `""` right after. (2) Is Discover Region worth mirroring? No — confirmed genuinely dead in Seerr itself: `discover.ts::createTmdbWithRegionLanguage` builds its TMDB `discoverRegion` param from `user.settings.streamingRegion`, never `user.settings.discoverRegion`, despite the matching-sounding name; the latter is only ever read/written by the settings route itself, never consumed by anything that filters Discover results — a latent no-op in Seerr's own web UI too, not a Fjord gap. Skipped. New `fjord_seerr::Language`/`get_languages()` (`GET /languages`, TMDB's full ~187-entry list, deliberately NOT Seerr's own separate ~40-entry UI-translation locale set — Fjord doesn't need that compatibility) backs both new Settings → Integrations rows, Display Language and Discover Language; `main.rs::spawn_streaming_region_fetch` renamed to `spawn_seerr_settings_fetch` and extended to fetch regions+languages in parallel and resolve all three settings from one shared `get_current_user`+`get_user_settings` call instead of three. "Default" selections write `""` (locale) but the literal `"all"` (originalLanguage, not empty — confirmed from Seerr's own ternary that empty falls through to the *admin's* default instead of "no filter"). The gating half surfaced 3 real, live-confirmed bugs in the *existing* toggle, not just scope for 2 new rows — user: "?? what do the seerr toggle do now? for me it shuld turn off seerr." `seerr_auth::push_seerr_status`'s `connected` computation already ANDs in `seerr_enabled`, so every `seerr-connected`-gated row was already *supposed* to hide when disabled — but `on_settings_changed` never called `push_seerr_status` after a toggle, so it stayed stale until restart; fixed by calling it (+ rebuilding `seerr_client`, which doubles as the live connection actually being torn down) inside that same handler. The Connect/Disconnect row itself had no gate at all — visible and clickable with the toggle off — fixed with `if settings-seerr-enabled` in `settings.slint`; `settings.rs`'s `max_row` became a 3-tier ternary to match. Corrected a wrong app_state.slint doc comment claiming enabled/connected were fully independent — true of the underlying `Config` credentials (never cleared by disabling, which is what makes instant reconnect-on-re-enable work) but never true of the derived `AppState.seerr-connected` the UI actually reads. `cargo build`/`clippy --workspace`/`test --workspace` clean; `GET /languages` (187 entries) and the full write round-trip re-verified live. **Live-confirmed through Fjord's actual UI (2026-07-18):** "the langue settings for seerr seams to be working and the gate works" — user-confirmed, no longer pending. |

| 153 — SEERR sidebar version line was still visually "muted" after Phase 152's color fix — size/weight, not color (2026-07-17) | User, immediately after Phase 152's fix: "why is seerr still muted?" Real miss: dropping the identity line left `seerr-version` as the block's only content line, but it kept *secondary*-line styling (11px, regular weight, matching `SERVER`'s/`CLIENT`'s own version lines below their bold 13px primary line) instead of being bumped to *primary*-line styling now that it's the sole line. Color was already correct (`Theme.text`) — the remaining "muted" read was pure size/weight. Fixed by matching `SERVER`'s/`CLIENT`'s primary line exactly (13px, weight 600). `cargo build` clean. |

| 154 — Seven-item issue batch: Discover search dead-end, Requested-row tier-blind filter (real bug, live-confirmed), 4K/2K badges, Discover loading spinner, RequestDetailScreen button/backdrop layout (2026-07-18) | User pasted 7 raw issues from testing and said "now we look at these, ask do not assume." Investigated each via an Explore agent before touching anything, then asked structured questions only where real design forks remained — 3 of 7 were unambiguous bugs/precedent-matches, fixed directly without asking. **(1) Discover search dead-end** — `handle_discover_search` (keys.rs) had no Up handler at all (unlike `handle_library_search`), so with an empty query Escape was the ONLY way out; typing then backspacing to empty is the identical state, not a second bug. Fixed: Up/Left on empty query now exit to sidebar (fs=-1), same destination Escape now also targets. **(2) Requested row still showing available items — root cause found via live data, not guessed.** Queried `GET /request` against the user's real instance: `MediaInfo.status`/`status4k` are tracked completely independently by Seerr (an item can be `status: Unknown` while genuinely `status4k: Available`), and `requested_not_available`'s filter checked `status` unconditionally regardless of which tier was actually requested — on an account where nearly every request is 4K, fulfilled requests kept showing because the (wrong) 2K status stayed Unknown. Fixed in `fjord_seerr`: `MediaInfo` gained `status4k`/`status4k()`, `MediaRequest` gained `is4k`; the filter now picks `status4k` when `r.is4k` else `status`. `discover.rs::fetch_requested_row` had the identical tier-blindness in its own badge text (`availability_tag(media.status())` unconditionally) — same fix applied there via a new `RequestEntry`/`request_entry()`. **(3) 4K/2K badges — "Full feature," user confirmed, once (2) supplied the real data.** `CardItem` gained `requested-4k`/`other-tier-available` (Discover "Requested" row only, false elsewhere — same zero-risk-elsewhere pattern as `availability`); `MediaCard` tier-prefixes the existing availability pill ("4K Requested") and adds a second bottom-right pill ("Also in 2K"/"Also in 4K") when the non-requested tier is already available. **(4) Discover loading spinner** — added a `LoadingSpinner` to the existing "Loading Seerr's trending picks…" text, matching the exact same pattern `home.slint`'s 4 dashboards already use for their own empty-loading state (`LoadingSpinner {}`, no width override needed — established precedent, not invented). **(5) Button row position — asked, user answered "fixed just below the poster," not either offered option.** Gave RequestDetailScreen's title column `height:240px` (matching PosterBlock exactly) + a `vertical-stretch:1` spacer before the button row, pinning Request/Trailer's Y to the poster's bottom edge regardless of title/meta length — a deliberate divergence from DetailPage, whose own button row DOES drift with its optional tagline/genre/studio lines (user didn't know this going in — "hmm did not now that"). **(6) Backdrop layout — "move the whole Details panel."** Moved Status/dates/language/countries/network/Currently Streaming On from below Cast & Crew into the backdrop's empty right-side space, as a third `HorizontalLayout` sibling next to `PosterBlock` and the title column. Fixed 420px width, not `horizontal-stretch` — the streaming-provider `Flickable` sits two levels deep inside this column, and a stretch-derived width is the same class of risk as the documented self.width-in-conditional "Recursion detected" crash (CLAUDE.md Slint gotchas); a plain fixed literal is the established safe alternative, matching `PosterBlock`'s own fixed 160px. **(7) Discover Region deliberately NOT built** — investigated via Seerr's real source (cloned `seerr-team/seerr` `develop` to a scratchpad tarball) and confirmed it's genuinely dead in Seerr itself: `discover.ts::createTmdbWithRegionLanguage` reads `user.settings.streamingRegion` for its `discoverRegion` TMDB param, never `user.settings.discoverRegion`, despite the matching name — a latent no-op in Seerr's own web UI too, not a Fjord gap; mirroring it would be a control that does nothing on either side. `cargo build`/`clippy --workspace`/`test --workspace` clean throughout. |

| 155 — Fixed button-row position extended to the other 4 detail-style screens (2026-07-18) | User, right after Phase 154's RequestDetailScreen button fix: "wont we need to update the other detail pages for the button position?" Investigated each of Detail/Season/Collection/Album/Artist via an Explore agent before assuming a straight copy-paste would work — asked user to confirm scope with the findings in hand (Album already had it; Detail/Collection are a clean copy; Season/Artist use non-`PosterBlock` art and would need reshaping, not a literal copy) — answer: all 4 remaining. `DetailPage` and `CollectionScreen`: same `height:240px` + `vertical-stretch:1` spacer fix as RequestDetailScreen, matching `PosterBlock`'s 160×240. `DetailPage` needed the fixed block nested one level in, since its "Ends HH:MM" text sits below the button row and must stay outside the pinned block as a normal sibling (nowhere to fit inside an already-bottom-pinned row otherwise). `CollectionScreen` had no spacer at all previously — a long wrapped overview could push its ♥︎/✓︎ row arbitrarily far down. `SeasonScreen`: `height:195px`, matching its own 130×195 poster (not `PosterBlock`). `ArtistScreen`: `height:160px` matching its circular portrait, PLUS switched `alignment:center` to `alignment:start` — the whole title+meta+button group previously floated as a vertically-centered unit (re-centering whenever the optional meta line appeared/disappeared), a different kind of drift than the other three screens' "content pushes buttons down from a fixed top." `AlbumScreen` needed no change — already had the identical `header-h`(240px)+spacer pattern, just hadn't been recognized as the precedent when RequestDetailScreen was first built. `cargo build`/`clippy --workspace`/`test --workspace` clean. |

| 156 — The button-position fix itself was wrong (real bug, live screenshots); SEERR sidebar needed a third correction (2026-07-18) | User posted 3 screenshots the same day: "episode detail is not right, not movie detail either, no one is right. did you not understand what i wanted?" Asked which specific symptom mattered rather than guess again — answer: "buttons themselves are misplaced," not (just) the spacing to whatever's below them. Root cause, reasoned through without live Slint rendering available in this sandboxed environment: a `VerticalLayout` with an explicit fixed `height` does NOT reliably force a `vertical-stretch:1` sibling to consume exactly "declared height minus other content" — when the non-stretch content's own natural height varies (optional line count differs per item), the button row's actual computed position varies with it, the literal bug. Fixed on all 5 screens (Detail/Season/Collection/Artist/RequestDetail) by replacing the flex/stretch mechanism with deterministic absolute positioning: each fixed-height block changed from a `VerticalLayout` to a plain `Rectangle` (opts out of Slint's automatic child distribution) holding two INDEPENDENTLY-positioned children — title/meta at `x:0;y:0` in its own inner `VerticalLayout` (sized to its own content, free to grow/shrink with zero effect on anything else), button row pinned via explicit `y: parent.height - 38px` (`FjordButton`/`IconCircleButton` both a hard-coded 38px in `widgets.slint`, confirmed by reading the source, not guessed). Neither child's position derives from the other's size, so there's no flex computation left to get subtly wrong. Same conversation, separate issue: "why is the version withe and bold it shuld be like the other versions and seerr shuld be like fjord and jellyfin" — Phase 153's bold/13px version-line fix was itself wrong; the real fix was restoring an actual primary NAME line (a fixed `"Seerr"` literal, mirroring CLIENT's fixed `"FJORD"`), with the version line reverted to plain `Theme.text-muted`/11px matching SERVER's/CLIENT's own version lines exactly — supersedes the older "deliberately NOT muted... don't fix it back to muted without checking first" note from a prior session. `cargo build`/`clippy --workspace`/`test --workspace` clean throughout. |

| 157 — Button-position fix, take 3: the plain-Rectangle switch broke text width; parent.width caused a real compile-time binding loop; root.width was the actual fix — CONFIRMED LIVE WORKING (2026-07-18) | Phase 156's `Rectangle`-with-absolute-`y` fix got the button Y right but immediately broke something new — user, next live test: "the buttons is in the right place but it did break the metadata" (Kung Fury's title wrapped "Kun/g/Fury" across 3 lines, meta/genres/studio rendering below "Ends" instead of above the buttons), then on a second screen: "and not title at all on the descovery detail." Root cause: switching `VerticalLayout` → plain `Rectangle` lost the VerticalLayout's automatic `preferred-width` computation — a bare `Rectangle` claims no space from its parent `HorizontalLayout` without an explicit width or stretch factor, so it collapsed to near-zero and every text line wrapped to nothing (the button row, using its already-correct absolute `y`, stayed fine throughout — why "buttons right, metadata broken" was the exact symptom). User pushed for real verification rather than more guessing ("build some mocup desing to see how it wuld look" → "why are you still using html and not just slint?" → "why do you need slint-viewer?" — each answered honestly, landing on: run the actual compiled app directly and have the user screenshot it, no synthetic input tools (`wtype`/`ydotool`/`xdotool`) available in this environment to drive navigation, so this needed to be a collaborative live-test loop, not something automatable end-to-end. Attempt 1, `horizontal-stretch: 1` on the Rectangle — live-tested, confirmed NOT to work (stretch doesn't apply to a plain Rectangle the way it does a Layout element; identical broken screenshot came back). Attempt 2, explicit `width: parent.width - 288px` (parent = the HorizontalLayout) — failed at COMPILE TIME with a genuine Slint error: `binding loop (layoutinfo-h -> layout-cache -> width -> width -> layoutinfo-h)` — a Layout's own width depends on its children's layoutinfo, so a child can't read that same in-progress width back; a stricter, previously-undocumented corollary of the existing self.width-in-conditional gotcha, now added to CLAUDE.md's Slint gotchas catalog in its own right. Attempt 3, `root.width` instead of `parent.width` — every one of these 5 screens wraps its body in a `Flickable { width: parent.width; viewport-width: self.width; }` near the component root, so `root.width` threads down unchanged and non-circularly; CONFIRMED LIVE WORKING via direct user testing on the real running app ("nicly done" / "nicely done"). Same-day follow-up: "Ends" text on DetailPage centered under the Play button (`width: 110px; horizontal-alignment: center;`, matching Play's own width) instead of left-aligned with the whole column. `cargo build`/`clippy --workspace`/`test --workspace` clean; this is the first of the button-position attempts actually confirmed working against the real app, not just build-clean. |

---

## Pending

- **Seerr live verification** (Phase 132-145 follow-up, multi-word search confirmed working 2026-07-18 — Phase 142's `%20` fix, see that phase entry — removed from this list): confirm mouse-clicking each row in the Request Options modal now correctly highlights that same row (Phase 142's zone-numbering fix); confirm the profile/tag/season strips visibly hug short lists instead of showing empty space (Phase 145's scaling fix, e.g. re-open the 4K movie Request Options modal, which has exactly one profile); confirm the Discover "Requested" row shows real pending requests with correct posters/titles, and that a search result already in the library opens the actual Jellyfin item (playable, correct watch/favorite state) rather than the Seerr page (Phase 144, both features); the new landing rows, 4K toggle, tag picker, the Phase 137 redesign (rating badge, cast/crew photos, season poster cards), the Phase 138 Request Options modal's Cancel/Request flow, the Phase 140 quality profile picker (real profile names, the Default option round-tripping as "no profileId sent," a profile-included request landing correctly in Seerr); whether `TvDetails`/`MediaInfo` nests any per-season availability beyond what the published OpenAPI spec shows (the season picker is pure selection either way); that a created request (4K + tags + profile included) actually shows up correctly in Seerr. Also: confirm the encrypted `config.json` round-trips correctly on this specific install and that no secret leaks into `fjord.log`. Confirm search auto-fills the grid without needing to reach the last row on a typical window size; confirm the Details panel shows correct Status/dates/language/countries/network for a few different movies and TV shows.

- **Watch Trailer** (Phase 150, dev-machine playback + Trailer Quality confirmed working 2026-07-18 — see phase entry): the specific "Loading…" buffering-overlay-during-resolution behavior; the exact controls-bar/mini-player-bar title text; no-Jellyfin-API-calls confirmation (grep `fjord.log`); interrupting a real Jellyfin video with a trailer still reporting that video's real stop position; the yt-dlp-absent case (button doesn't render); the reactive-toast case (yt-dlp present but video unresolvable); an item with no trailer data showing no button. **The one that matters most**: the NVIDIA HTPC target — dev machine is AMD/Vulkan, and YouTube commonly serves VP9/AV1, codecs this project has hit real NVIDIA-hwdec-specific bugs with before (see "Known platform issues" above); mpv should fall back to software decode automatically, but this is genuinely unverified against that exact hardware/codec pairing and is the harder target this project has always targeted first.

- **Phase 154 (7-item issue batch) live verification** — build/clippy/test clean and the Requested-row filter fix was confirmed via real live `GET /request` data (not just reasoning), but none of it has been exercised through Fjord's actual UI yet: confirm Up/Left genuinely exit an empty Discover search field (mouse and keyboard); confirm the Requested row no longer shows a fulfilled 4K request, and that a genuinely-still-pending one still does; confirm the new "4K Requested"-style tier-prefixed pill and the "Also in 2K"/"Also in 4K" cross-tier badge both render correctly and don't visually collide with anything else on the card; confirm the Discover landing-rows loading spinner actually shows during a real cold load (not just instantly-cached); confirm RequestDetailScreen's Request/Trailer buttons visibly sit at a fixed Y regardless of title length (compare a short-titled movie against a long-titled one), and that the Details panel reads cleanly in its new backdrop position (legible against the gradient, doesn't visually crowd the poster/title/buttons column) at a real window size. **Phases 155 and 156 both turned out wrong in turn (156 fixed the button Y but broke text width; Phase 157's `root.width` fix is the one actually confirmed live-working)** — DetailPage specifically, via the real Kung Fury item, is now confirmed correct end-to-end (title/tagline/meta/genres/studio all render normally, button row at a fixed Y, "Ends" centered under Play) — direct user confirmation, not just build-clean. Still not yet live-tested: the identical `root.width` fix on the other 4 screens (Season/Collection/Artist/RequestDetail) — same mechanism, same confidence, but not yet independently exercised through the UI on any of them. Also still worth checking: `ArtistScreen`'s new top-anchored layout vs. the old centered one (a real visual trade-off, not a pure improvement); the SEERR sidebar's corrected 3-line shape (header/"Seerr"/version) actually matches SERVER's and CLIENT's visual weight now.

---
## Issues
(all 7 items from the 2026-07-18 batch addressed — see Phase 154. Jot new ones here as they come up.)

## future additons


Enter plan mode to see if there any whay to integrate cinema location and avilibility for items that is in cinema.
and add filters for searches and mabey a row in the dashborde for in cinimas

mabye even a calender for upcomming episodes/realeses both sreaming with the platform and cinema and physical realese so you can plan how you wuld watch it cinema, streaming or buy it to add it to the collection.

mabey the calender shuld be opt in to only item you chose to add from the descovery think seerr have a watchlist


## Architecture notes

### mpv render API

mpv uses `vo=libmpv`. Two FBOs alternate each frame:

```
BeforeRendering:
  mpv_render_context_render(fbos[back])
  expose textures[back] as BorrowedOpenGLTexture → Slint draws it
  back = 1 - back

AfterRendering:
  if did_render: mpv_render_context_report_swap()   ← vsync feedback
```

`MpvRenderCtx` must be dropped before `Player`. Enforced in `VideoState` and the rendering teardown path.

### Disk cache

```
~/.cache/fjord/home.json         home row data    always refresh in background
~/.cache/fjord/movies.json       full movie list  refresh once per session on grid open
~/.cache/fjord/series.json       full series list refresh once per session on grid open
~/.cache/fjord/posters/<id>      poster bytes     permanent (never expire)
~/.cache/fjord/backdrops/<id>    backdrop bytes   permanent (never expire)
```

Warm start: all caches loaded synchronously before `window.run()` — window opens fully populated on the first frame.

### Poster loading pipeline

```
Tokio worker:
  fetch bytes (disk cache or HTTP, 8-connection semaphore)
  decode JPEG → SharedPixelBuffer<Rgba8Pixel>   ← Send
invoke_from_event_loop:
  Image::from_rgba8(buffer)                     ← !Send, must be on UI thread
  push HomeItem with poster into VecModel
```

### Thread model

```
main thread       Slint event loop + GL rendering notifier
tokio runtime     API calls, poster fetch/decode, home data refresh
16 ms timer       mpv event poll, position update, skip-segment (Intro/Recap/Preview/Commercial), credits auto-advance check, controls idle, progress report
```

---

## Deferred / future

- **Theming / layout customisation**: accent colour palette, dashboard row visibility toggles, row reordering — needs the full layout system in place first before it makes sense to build.
- **Vulkan rendering path** — second render backend alongside the current OpenGL path. Requires: Slint WGPU backend, `MpvRenderCtx` initialized with `MPV_RENDER_API_TYPE_VULKAN`, Vulkan FBO management replacing the current `gl::*` code. Enables true zero-copy decode on AMD (`hwdec=vulkan`, no CPU roundtrip). Legacy NVIDIA hardware needs OpenGL; selection persists in Config as `gpu_renderer: "opengl" | "vulkan"` and takes effect on next restart.
- Gamepad / remote control — d-pad maps to arrow keys; formal evdev/udev support deferred
- **Dashboard row reorder** — drag-to-reorder; part of the future theming/layout customisation update
- **Trickplay** — seek bar scrub thumbnail popup. Requires: fetch Jellyfin trickplay manifest (`GET /Videos/{id}/Trickplay/{width}/tiles`), parse tile sheet dimensions (tile size, columns, rows, interval), cache tile images per video, render a thumbnail above the seek bar while scrubbing (position computed from `seek-hover-pos`). Deferred because it's a separate subsystem from chapter nav and the API surface needs more investigation.
- **Multi-account / multi-server support** — currently Fjord stores one server URL + one user session in `config.json`. To support multiple accounts: `Config` would need a `Vec<ServerProfile>` (each holding server URL, device ID, username, token) with an `active_profile: usize` index; the login screen would gain a server-picker step; sign-out would become "switch profile" rather than "clear everything"; the `FjordState` runtime fields (`all_movies`, `all_series`, caches, etc.) would be cleared and repopulated whenever the active profile changes. CR6-1 (sign-out cleanup) is a prerequisite — it establishes the correct invariant that switching users produces a clean slate, which multi-account support then relies on.
- **Poster/card scaling setting** — from Phase 117: with titles/episode names now always shown in full (no truncation), a very long title on the smallest card breakpoint (115px wide) shrinks that card's poster noticeably to make room. User's proposed fix: a settings toggle to use larger posters in the library/dashboard (more text budget per card before this becomes visible) rather than reintroducing a truncation cap. Would likely hang off the existing `dash-card-w`/`dash-card-h` breakpoint functions in `main.slint`, e.g. as a user-chosen size multiplier or an extra breakpoint tier. Alternative approach floated in the same conversation, not mutually exclusive: render the poster image full-bleed behind the title/subtitle text with a dark semi-transparent scrim, instead of a separate text block below the poster — sidesteps the whole shrink-to-fit problem structurally rather than giving the text more room. Neither implemented yet; revisit when picked up.
