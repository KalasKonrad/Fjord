# Fjord — Development Plan

## Goal

A native Jellyfin frontend for Linux built with Rust and Slint. Uses the mpv render API so mpv renders directly into an OpenGL FBO, enabling `report_swap()` for vsync feedback — the approach that avoids choppy playback on NVIDIA legacy Wayland drivers.

---

## Completed

Full curated version history: [CHANGELOG.md](CHANGELOG.md) (git tags `v0.1.0`–`v0.4.2`). Full implementation detail per feature: `CLAUDE.md`.

---

## Pending

- **NVIDIA HTPC: HDR tone-mapping shader compile failure freezes the app (audio keeps playing) — live-diagnosed 2026-08-15 from a real `fjord.log`, workaround identified, not yet code-fixed.** Real 4K Dolby-Vision-tagged HEVC file, HDR passthrough off, `nvdec`: mpv's GL renderer failed to compile its HDR tone-mapping fragment shader 6 times (`error C7505: OpenGL does not allow swizzles on scalar expressions`, on a scalar `gamma.x` — mpv/libplacebo's `mobius` curve shape, though the exact active curve at crash time couldn't be pinned down from the log, since it was cycled extensively beforehand with no value-logging). The Slint GL/UI thread stalls for ~24s inside what's almost certainly the driver's own compile/link retry path (confirmed: an independent Tokio-driven WS keep-alive fired normally mid-freeze, so only the GL thread was stuck) — audio, a separate pipeline, keeps playing throughout, matching the exact reported symptom. No existing upstream mpv/libplacebo bug report found for this specific combination. **Fixed the same day**: the Tone Mapping row's subtitle now describes each of the 8 curves (was a static, now-demonstrably-not-always-true "auto is usually correct"), live-requested. See CLAUDE.md's new "NVIDIA HTPC: HDR tone-mapping shader compile failure" section for the full trace.
  - [ ] Confirm switching Settings → Video → Tone Mapping away from the current value (try `bt.2390` first) makes this exact file play without the freeze
  - [ ] If it recurs on a curve that isn't mobius-shaped, the diagnosis needs revisiting, not just retrying
  - [x] Tone Mapping row now describes each curve; stats overlay's COLOR row split into CLR IN/CLR OUT so the real post-tone-mapping output colorspace is checkable directly (`I` during playback) instead of guessed at — see CLAUDE.md. Not live-tested; the whole point of adding it is to check on the HTPC.
  - [ ] **Real complication, not yet investigated**: the user's own separate `media_display_sync` script apparently forces the display into HDR + wide-gamut mode whenever it detects 4K HDR content, independent of what Fjord/mpv actually output — this would undermine the "tone-map to SDR + turn KDE HDR off" recommendation entirely if the script re-forces HDR+wide-gamut anyway. Lives outside Fjord's repo; needs the script's own source shared before this can be investigated further. Likely fix is coordinating the script's decision with Fjord's actual output, not a Fjord-side change.
  - [x] **Researched whether real HDR passthrough is achievable on this exact GPU (GTX 1050 Ti) at all — resolved, driver-level capability confirmed present, real-world negotiation still needs a live check.** Includes a genuine self-correction (an initial AI-search-summary claim about an HDMI 2.1 requirement was quoted with too much confidence, then found NOT present in the actual primary source once fetched directly, and corrected explicitly once challenged). Once the user pointed out HDR passthrough already works on this exact hardware under Windows — real proof the chain is hardware-capable — confirmed from primary sources that NVIDIA's Linux driver 580.94.11 (2025) shipped the first-ever `HDR_OUTPUT_METADATA` support, Pascal/Maxwell/Volta were frozen onto that same 580/581 branch as their last full-feature driver (ending December 2025), and Arch's own current Pascal package (`nvidia-580xx-dkms`) is exactly that branch — no version gap between "driver with the feature" and "driver Arch gives this GPU." See CLAUDE.md's extended research-pass section for the full sourced trace.
  - [x] **Real Settings bug found and fixed from the same research: Tone Mapping was hidden whenever HDR passthrough was on, even though it's mpv's own documented fallback curve (`target-colorspace-hint-strict`, default on) when the passthrough hint isn't accepted.** Live-reported directly: "the tonemap setting shuld not be gateded byt the hdr hint setting." Confirmed `fjord-player`'s `Player::new` already sets `--tone-mapping`/`--target-colorspace-hint` as two fully independent mpv options — this was purely a Settings-visibility bug, not a deeper plumbing gap. Tone Mapping is now always visible in Settings → Video; HDR passthrough's own subtitle explains the fallback relationship directly. Build/clippy/test clean; not live-tested.
  - [x] **CLR OUT stats field fixed, 2026-08-17 — live-reported from real tone-mapping testing ("its hard to see any differeces on any of them besidese linear... but the thing i saw was that CLR out/in still stated hdr10 493 nits").** `video_out_primaries`/`video_out_gamma`/`video_out_sig_peak` (`fjord-player`) switched from `video-out-params` (post-`--vf`-filter only, per the real mpv manual — never reflects tone-mapping/passthrough negotiation, which happen in the VO renderer) to `video-target-params` (the manual's own documented "target properties that VO outputs to"). Build/clippy/test clean; not live-tested.
  - [ ] **WALKED BACK, 2026-08-17 — the "decisive" conclusion this checkbox originally recorded was reached on broken data, needs re-running.** Original claim: CLR IN and CLR OUT both read `bt.2020 · pq (493nits)` — mpv is doing zero conversion, genuinely attempting real passthrough — yet the TV never enters HDR mode; conclusively ruled out Fjord/mpv as the cause. Live tone-mapping testing the next day proved CLR OUT was reading the wrong mpv property the whole time (`video-out-params`, which the real mpv manual documents as post-`--vf`-filter only — never reflects tone-mapping or passthrough negotiation at all, both of which happen in the VO renderer) — fixed to `video-target-params`, the manual's own documented "target properties that VO outputs to." CLR IN and CLR OUT could **never** have shown a real difference before this fix, regardless of whether passthrough was actually succeeding. See CLAUDE.md's new section under the HDR tone-mapping investigation for the full trace. **Needs a fresh live test with the fix in place before trusting either conclusion (passthrough working / KWin-level break) again.**
  - [ ] User's own planned A/B test (script running normally vs. resolution/Hz already set before Fjord starts) still pending — see the `media_display_sync` complication item above, now with a sharper motivating hypothesis recorded (a live DRM mode-set racing mpv's own render-context/HDR-hint negotiation, the same shape as the already-fixed VO-init race documented elsewhere in this file).
  - [x] **Real bug found and fixed, 2026-08-16: turning HDR passthrough OFF didn't actually force tone-mapping — mpv's own "auto" default was still deciding on its own.** Live-reported: "i did test to turn off hdr in fjord and chose renhaird as tonemapping but CLR IN/OUT still show both bt.2020 · pq HDR (493nits)," confirmed changed before playback started, not mid-playback. `fjord-player::Player::new` only ever called `set_option("target-colorspace-hint", "yes")` when the toggle was ON — OFF meant never touching the option at all, leaving mpv at its own real default (`auto`, confirmed live via `mpv --list-options`), not `no`. Fixed by making the call unconditional and explicit (`"yes"`/`"no"`, never left at mpv's own auto). See CLAUDE.md's new section. Build/clippy/test clean; not live-tested — expected to make the exact reported scenario show a real CLR OUT conversion now.

- **Three real Bonfire-picker bugs, live-reported and traced from real logs, 2026-08-15 — all fixed.** See CLAUDE.md's new "Two real Bonfire-picker bugs" + "Log retention" sections for the full trace.
  - [x] Sign-out left orphaned Bonfire sub-profiles behind (blanked master entry in place, never removed) — both erroring on switch ("the master account for this profile isn't signed in on this device," confirmed directly in a live `fjord.log`) and corrupting the account picker's own display (a sub-profile's name/avatar visually took over the account tile, since `group_into_accounts` filtered the blanked master out of grouping entirely). Fixed by removing the signed-out profile's entry AND its sub-profiles entirely on sign-out (`AskUserQuestion` — user picked remove over keep-locked); re-populated automatically on next login as the same master.
  - [x] A genuinely separate corruption: switching to a Bonfire sub-profile (not signing out at all) could reparent that profile's own siblings under IT instead of the real master — `sync_bonfire_subprofiles` ran unconditionally after every session start and used whichever profile was currently active as the master_user_id to write onto Bonfire's `/list` response, with no check that the caller was actually the master. Fixed at the write source (skip entirely when the calling session is itself a sub-profile) plus a `load_config` self-heal for a `config.json` already corrupted by the old version.
  - [x] "no keybord navigation worked" on either picker at all, when reached via a cold-start startup gate — a genuine timing race, not a picker-logic bug: both pickers can be opened synchronously from `main()`, entirely before `window.run()` starts the Slint event loop, and a keyboard-focus grab issued that early doesn't reliably stick (same class of bug already found once for the FadeGate kick-timer race). Fixed with a shared `grab_focus_deferred` helper, queuing the grab via `invoke_from_event_loop` instead of calling it synchronously.
  - [x] **Re-tested, and a genuinely new bug surfaced: on the dev machine (not the HTPC), "Anso" — a real Bonfire sub-profile — still showed as its own account after upgrading.** Read the dev machine's own `config.json` directly (a real advantage of testing locally) and found a genuine CYCLE, not a simple chain: Anton (the true master) had been flipped to `is_bonfire: true, master_user_id: <Anso>`, while Anso pointed back at Anton — both look identical once corrupted, so the previous day's chain-walking self-heal's 5-hop cap just silently gave up on this shape rather than fixing it. Fixed with proper cycle detection: demotes only the node that's itself part of the cycle to a standalone account (not any sibling merely pointing into it from outside, which would risk guessing the wrong direction) — self-corrects fully once the real master does one more genuine password login. Verified directly against this machine's real corrupted data (temporary test, deleted; permanent synthetic-data regression test kept). See CLAUDE.md's new "Bonfire cycle corruption" section.
  - [ ] None of the four re-confirmed live yet via a real rebuild — worth checking, in priority order: keyboard nav works immediately on a cold-start picker; signing out of a master with active sub-profiles cleanly removes them everywhere; switching to a sub-profile and relaunching no longer shows it as its own account; the dev machine's own Anton/Anso cycle actually resolves to two clean standalone tiles after rebuilding, and re-adding Anton fresh fully re-derives the real household tree.

- **Log retention: multiple generations, moved to `~/.cache/fjord/logs/` — done, 2026-08-15, live-requested directly while diagnosing the bugs above.** A single `fjord.log`/`fjord.log.old` rotation genuinely wasn't enough during this exact session's own multi-restart testing — content needed to diagnose a live-reported bug got rotated away twice before it could be read. Now keeps 10 generations (`fjord.log.1`..`fjord.log.10`) in a dedicated `logs/` subdirectory; old flat files cleaned up best-effort on first run with the new scheme. Build/clippy/test clean; not live-tested.

- **Profile/account picker icon grown again, 120px → 180px — done, 2026-08-15, live-requested ("make the icon/logo even bigger").** At least the 5th documented size pass on this one element; not live-tested against this specific change.

- **Profile picker "← Back to Accounts" + Quit buttons made keyboard-navigable, sidebar "Switch Profile" split into "Switch Profile" + "Switch Account" — done, 2026-08-16.** Two live reports ("cant get to go back to accaunts with the keybord" / "quit it not also reacheble by keybord navigation") plus a design-feedback follow-up ("mabey if there are more than one accaunt you shuld get a switch accaunt too"). See CLAUDE.md's three dedicated sections. Build/clippy/test clean; not live-tested.

- **Full code review of the account/profile implementation — done, 2026-08-16, direct request ("now do a code reviewe of th accaunt and profile implimentation"), then "fix them all, dont guess or assume, ask."** 12 findings across two review passes (the code-review skill's own multi-angle pass, plus a targeted background sweep of `main.rs`/`keys.rs`), every one independently re-verified by direct code reading before being trusted, reported via `ReportFindings`, then all 12 fixed the same session — two genuine design forks resolved via `AskUserQuestion` before any code (see CLAUDE.md's dedicated section for the full per-finding writeup and reasoning).
  - [x] **Findings 8 & 9 (most severe) — two real gaps in the earlier Phase 1 step 8 async-guard audit**: `spawn_auto_login`'s post-login-join tail, and all 5 library-list fetchers (Movies/Collections/Artists/Albums/Playlists), had no `session_current()` check at all — a profile switch completing during either's in-flight network call could land the OUTGOING profile's series/plugins/library lists (and, worst case, its WebSocket abort handle) into the just-switched-to session. Fixed with the same `session_current()` primitive the original audit already established.
  - [x] **Finding 1 — `remember_login==false` was only enforced by the startup gate, not by a manual switch** via the sidebar's Switch Profile/Switch Account — directly contradicting `remember_login`'s own documented "regardless of what launch policy would otherwise decide" contract, and realistically the MOST likely way such an account is ever opened. Fixed with a shared `account_requires_login`/`require_login_for_account` gate checked before either picker selector's existing switch/PIN logic.
  - [x] **Finding 3 — the "Remember this login" checkbox never reset** — resolved via `AskUserQuestion`: RequireLogin now reflects the account's own already-known `false` value; both Add-Account entry points always reset it to `true`.
  - [x] **Finding 11 — Sign Out always showed a blank Login screen even with another valid account known** — resolved via `AskUserQuestion`: now routes to the account picker when 2+ accounts remain after sign-out, plain Login only when 0/1 remain.
  - [x] **Finding 10 — the 60s screen-cache save timer could overwrite the WRONG profile's `screen_caches.json`** — two sub-bugs: it read `config.active()` (which falls back to `profiles.first()` and can diverge from what's actually loaded, e.g. right after sign-out) instead of the live client's own user_id; and a profile switch never reloaded the switched-to profile's own cache file at all before the timer's next tick could stomp it. Fixed both — timer now keys off `client.user_id` (skipping entirely with no live client), and `finish_session_setup` warm-starts `screen_caches.json` the same way `spawn_auto_login` already does.
  - [x] **Finding 12 — Manage Profiles had ZERO keyboard navigation** — `manage-profiles-cursor` was declared from Phase 2 but never wired to anything; a D-pad/remote user could see the tile grid but never select/edit/delete/create a profile. Fixed with real `kbd-focused` wiring + Left/Right/Enter dispatch, mirroring `AccountPickerScreen`'s own already-correct tile pattern.
  - [x] **Finding 2 — Manage Profiles showed the master's own account as an editable/deletable tile** (Bonfire's `/list` includes it, same gap `sync_bonfire_subprofiles` already had to filter once before). Fixed with the identical self-exclusion filter.
  - [x] **Finding 4 — PIN entry buffers weren't cleared on a FAILED attempt**, only a successful one, in `switch_to_profile` and both `profile_edit.rs` save/delete paths — contradicting their own doc comments. Fixed in all 3 places.
  - [x] **Finding 5 — `bonfire_switch_profile`'s own returned `jellyfin_user_id` was deserialized and silently discarded**, never checked against the locally-assumed target id. Fixed with a `warn!` on mismatch (deliberately not a behavior change — this corner of the Bonfire API is still unverified against a real server).
  - [x] **Finding 6 — the sidebar's own "Profile" row double-flashed** whenever the profile quick-menu / Manage Profiles / Profile Edit were open (all three missing from `sidebar-kb-active`'s exclusion list). Fixed.
  - [x] **Finding 7 — `reset_session_state` cleared `show-account-picker` but never its sibling `show-profile-picker`**, despite its own doc comment claiming to cover both. Fixed (not confirmed reachable live, but a real inconsistency worth closing).
  - [ ] None of the 12 fixes have been live-tested yet — see CLAUDE.md's section for the full per-finding priority-ordered checklist of what's worth confirming first.

- **Bonfire/JellyProfiles integration — in progress, multi-session.** Full design approved and saved (plan file `groovy-stirring-phoenix.md`), documented in outline in this file's history and in full in CLAUDE.md as each phase lands.
  - **Phase 0 (Settings screen data-driven rewrite) — done and thoroughly reviewed, 2026-08-07/08.** Shipped, live-tested (keyboard + mouse), then given a full manual code review (6 parallel finder passes covering Rust dispatch, Slint ChangeTracker/layout gotchas, Rust↔Slint drift, the keybindings.json migration, cleanup/conventions, state lifecycle) per direct request ("do a code review on phase 0" → "we shuld fix everything"). 10 more real bugs found and fixed on top of the original live-testing round — most severe: mouse-driven navigation never cleared `keybinding-focused`, so leaving Key Bindings via the mouse silently hijacked every later keypress into the rebind dispatcher; the rebind-collision dialog didn't fire for several real actions (Queue Panel/Delete/Lyrics/Now Playing/digit-seek) with no Settings row; `AppState.nav-selected` was registered twice (Slint callbacks are single-handler, so `browse.rs`'s own registration was dead code). Also: 4 separate attempts at the Reset-to-Defaults button's scroll-into-view bug, the last one root-caused by reading `i-slint-core`'s actual `Flickable` source directly — it silently re-clamps any `viewport-y` assignment against `viewport-height`, the same safety net that stops a drag past the content edge. See CLAUDE.md's Settings section for the full account. Build/clippy/test clean throughout.
  - **Phase 1, step 1 (Config restructuring: DeviceConfig/ProfileSettings split) — done, 2026-08-08.** The isolated, zero-behavior-change commit the plan itself calls for first: `Config` is now `{ device: DeviceConfig, profiles: Vec<ProfileSettings>, active_profile_id }` instead of one flat struct — every ~90 call site across 7 files was updated to go through `Config::active()`/`active_mut()`, found and fixed via the compiler (moving a field out of a flat struct turns every remaining reference into an exact-file:line compile error). A `LegacyConfig`/`migrate_legacy_config` pair handles the one-time migration of an existing flat `config.json` forward, re-saved once so the fallback only ever runs on the first post-upgrade launch. Verified 3 ways: unit tests (a hand-written old-shape fixture, a new-shape round-trip check, and a check that an old-shape file genuinely fails to parse as the new `Config` so the migration fallback actually triggers), plus — per the plan's own explicit requirement — a one-off run against a scratchpad COPY of this dev machine's real, on-disk `config.json` (never the live file), confirmed working, then deleted (not left in the tree). v1 still only ever has exactly one profile — no picker, no switching UI, no Bonfire API calls yet; those are later, separate commits in the same phase. Build/clippy/test clean.
  - **Phase 1, step 2 (on-disk cache namespacing) — done, 2026-08-09.** The seven flat library/home caches + `screen_caches.json` are now scoped under `~/.cache/fjord/profiles/<user_id>/`, resolved via an explicit `user_id: &str` parameter (not an implicit "current active profile" lookup, which could race an in-flight fetch against a mid-flight profile switch) — ~20 call sites fixed via the compiler. A cheap, idempotent migration moves an existing flat install's cache files into the new location on first post-upgrade launch. Build/clippy/test clean.
  - **Phase 1, step 3 (`reset_session_state` extraction) — done, 2026-08-09.** Pulled the shared half of `on_sign_out`'s teardown (stop playback, abort WS, clear every in-memory list/cache, force-close every content-bearing screen) into a standalone fn, verified against the real current handler rather than the plan's own stale draft of it. Config auth-field blanking and "what shows next" stay caller-specific. Pure extraction — `on_sign_out`'s own behavior unchanged. Build/clippy/test clean.
  - **Phase 1, step 4 (shared plugin-availability registry) — done, 2026-08-09.** `FjordState.available_plugins: HashSet<String>`, fetched via a new `get_plugins()` (`GET /Plugins`, best-effort) alongside the existing home-data/series/system-info join in both login paths. Keyed by plugin name, not GUID (a deliberate deviation from the plan — exact GUIDs couldn't be verified without a live server). Not yet consumed by anything (Bonfire's gate, Intro Skipper detection are both later work). Build/clippy/test clean; not live-tested against a real `/Plugins` response.
  - **Phase 1, step 5 (Bonfire `fjord-api` module) — done, 2026-08-09.** New `bonfire.rs` (22 `impl JellyfinClient` methods) + `models/bonfire.rs` (13 structs), covering profiles/libraries/devices/the Bonfire group/preferences/admin. Every field name verified against the plugin's own real `docs/developer-api.md` (fetched live via `WebFetch`, two passes — the second specifically for nested/array shapes), not the plan's own vaguer earlier draft — which also surfaced one real gap the plan never knew about (a documented `preferences` endpoint), now included. Build/clippy/test clean; **not live-tested at all** — no method has been exercised against a real Bonfire-enabled server yet.
  - **Phase 1, step 6 (`ProfilePickerScreen` + full switch flow) — done, 2026-08-09.** User explicitly chose "full scope now" over a minimal local-only picker. New `widgets.slint::VirtualKeyboard` (numeric layout, D-pad navigable — the `full` alphanumeric layout is still Phase 3), `theme.slint::ProfileTile`, `crates/fjord-app/ui/profile_picker.slint` (avatar-tile row + "+Add Account" tile + PIN-entry sub-panel), a raw-key dispatch tier in `keys.rs` sitting alongside the existing `show-login` tier (no new `AppMode` needed — checked before `active_mode()` ever runs). `ProfileSettings` gained identity fields (`display_name`/`avatar_color`/`avatar_initial`/`is_bonfire`/`master_user_id`/`has_pin`). `do_login` gained an `append: bool` parameter — the picker's "+Add Account" tile signs into a brand-new profile alongside existing ones instead of overwriting the currently-active one; `finish_session_setup` was extracted verbatim from `do_login`'s old tail so both a fresh password login and a token-based profile switch share one setup path. New `crates/fjord-app/src/profile.rs`: `should_show_picker_at_startup` (0/1 profile → always false, so every existing single-profile install is unaffected; 2+ → policy-dependent), `switch_to_profile` (resolves a token — via `bonfire_switch_profile` on the target's own master client for a Bonfire sub-profile, or the stored token + `check_auth()` revalidation for a plain account — **before** ever calling `reset_session_state`, so a failed switch never tears down the still-active session), `sync_bonfire_subprofiles` (fire-and-forget, `Arc::ptr_eq`-session-guarded, add-only upsert into `Config.profiles` — no pruning of removed sub-profiles yet, deliberately deferred). The startup gate in `main.rs` now branches on `should_show_picker_at_startup` before the pre-existing auto-login flow, which is otherwise byte-for-byte unchanged. See CLAUDE.md's Bonfire section for the full design writeup. Build/clippy/test clean; **not live-tested** — no method in this step has been exercised against a real Bonfire-enabled server or a real second profile.
  - **Phase 1, step 7 (launch-policy Settings row) — done, 2026-08-09.** Two new Settings → Profiles rows: `profiles.launch_policy` (static 3-value dropdown, Always Ask/Remember Last/Default Profile) and the virtual `profiles.default_profile` (a genuinely *dynamic* dropdown — its option list is whichever profiles `Config.profiles` currently has, same shape as the audio-device/font-family rows — only shown when Launch behavior is Default Profile). New `profile::refresh_profile_settings_dropdown` builds that list, called from both `apply_settings_to_window` (startup) and `finish_session_setup` (every session-start path, so a just-added profile shows up without a restart). `on_default_profile_selected` is 100% local (no network write), following `on_font_family_selected`'s exact shape. Build/clippy/test clean; not live-tested.
  - **Phase 1, step 8 (async-result session-guard audit) — done, 2026-08-09. This was the last item of Phase 1's original scope.** An `Explore` agent audit across all 10 named files (Detail/Series/Season/Collection/Album/Artist/Person/Discover/poster-loading/prewarm) found real, concrete gaps, not just theoretical ones: `reset_session_state` cleared every screen's `show_X` flag but never the matching focused-id property (fixed — now clears all 7), and never closed 3 Discover overlay screens at all (fixed). Explicit `session_current(...)` guards added to every commit closure that can reopen a screen: `collection.rs`/`album.rs`/`artist.rs`/`person.rs` had ZERO session guard on their main open path before this; `series.rs`/`season.rs`/`detail.rs` had one only at their first fetch, not their final commit. A new `seerr_session_current` twin (Discover holds `Arc<SeerrClient>`, not `Arc<JellyfinClient>`) was added and applied to `ensure_discover_landing` (previously the single most guard-less site found — no guard of any kind) and `open_discover_item_ex`. The watchlist-row chain lost its `Arc` identity several hops before `push_watchlist_rows` (deref-coerced to `&SeerrClient` then value-cloned) — re-plumbing the whole chain was judged disproportionate, so it got a coarser but real "still connected at all" check instead, explicitly documented as not covering a switch to a DIFFERENT Seerr-connected profile. `poster.rs` was the highest-stakes fix (Home dashboard rows / `all-series` — genuinely Jellyfin-restricted content — had literally no guard) and the widest mechanical fan-out: 13 direct call sites + 5 more one level out via `home.rs::refresh_favorites`, all fixed via the same "compiler as checklist" technique step 1 used. `prewarm.rs::spawn_image_prewarm` got the same guard shape as its already-guarded sibling `spawn_metadata_prewarm`. Build/clippy/test clean; not live-tested (needs a real second profile + Bonfire server, unavailable in this sandboxed environment). **Two real gaps in this same audit's own scope found later, 2026-08-16 (code review) — `spawn_auto_login`'s post-join tail and the 5 library-list fetchers were never covered** (this audit's own file list was Detail/Series/Season/Collection/Album/Artist/Person/Discover/poster-loading/prewarm — `main.rs`'s login/library-fetch functions were never in scope at all). Both fixed; see the "Full code review of the account/profile implementation" entry below.
  - **Phase 1 — every item from the original planned scope is now done.**
  - **Phase 2 (native profile create/edit/delete) — done, 2026-08-10.** New `ManageProfilesScreen` (Settings → Profiles → "Manage Profiles", master accounts only) + `ProfileEditScreen` (create/edit one Bonfire sub-profile: name, avatar color, PIN, parental rating, enabled libraries, blocked/allowed tags, lockout minutes, LAN bypass, device whitelist, plus the household master's own confirmation PIN). Scoped via `AskUserQuestion` first: since Fjord's on-screen keyboard is numeric-only (full alphanumeric is still unstarted Phase 3), text fields use plain `LineEdit`s (physical keyboard required) — user picked this over pulling Phase 3's work forward early. That decision extended to the whole screen: `ProfileEditScreen` is mouse + physical-keyboard driven throughout (confirmed `SettingsDropdown`/`VirtualKeyboard` both work standalone via mouse with no dependency on Settings' own keyboard layer), not this app's usual D-pad zone system — a deliberate, documented scope cut (the two checklists and avatar-color picker aren't D-pad-navigable yet), not an oversight. Real gap found designing the pre-fill logic: `BonfireProfile` (the `/list` response) has no `max_parental_rating` field at all despite create/update both accepting one — Edit mode can't show the current rating, though leaving it untouched correctly omits it from the save request rather than clobbering it. Build/clippy/test clean; not live-tested (same standing limitation as the rest of this feature).
  - **Real bug found and fixed, 2026-08-11: `sync_bonfire_subprofiles` never ran on a normal auto-login launch.** User: "wierd i have bonfire and i have run it on the htpc as you can see in the log" — checked the actual HTPC log rather than defending the prior "not live-tested" claim, and found zero Bonfire-related lines across two separate real sessions despite `get_plugins()` succeeding both times. Root cause, confirmed by reading the code: `sync_bonfire_subprofiles` (the only mechanism that discovers additional profiles and can ever make `should_show_picker_at_startup` return true) was wired into `do_login`/`finish_session_setup` and `switch_to_profile`, but **never into `spawn_auto_login`** — the ordinary "resume an already-saved session" path every real launch after the very first login uses. On any install using auto-login (the norm), Bonfire sub-profile discovery had genuinely never run again since whichever session first signed in, independent of whether Bonfire is correctly installed and configured server-side. Fixed by adding the identical call to `spawn_auto_login`'s success path. Also added `debug!` logging throughout `sync_bonfire_subprofiles` itself (previously silent on every path except a genuine non-404 error) so the next log capture shows directly whether it ran and what it found, rather than needing another code-reading pass. Build/clippy/test clean; not yet live-tested — this is the fix that should make the next HTPC log actually show Bonfire activity, one way or the other.
  - **The actual root cause found the next day, 2026-08-12, and this one IS live-verified — done.** "and the bonfire dont work check the logs on the dev machine": the auto-login fix above made the call actually run, and its new logging immediately showed `missing field 'profileUserId'`. A one-off diagnostic query directly against the user's real, working Bonfire server (4 real profiles) confirmed the actual response is **PascalCase**, not the `camelCase` all 16 structs in `models/bonfire.rs` had assumed since Phase 1 step 5 (reasoning by analogy with Seerr, a different service) — Bonfire is a Jellyfin plugin on Jellyfin's own ASP.NET pipeline, so it was always going to match Jellyfin core's own PascalCase convention, not Seerr's. Fixed: all 16 `rename_all` attributes changed to `PascalCase`; `BonfireProfile.max_sub_profiles` also given `#[serde(default)]` (confirmed genuinely master-only from the same real response — absent on all 3 real sub-profile entries). Verified against the real `bonfire_list_profiles()` client method directly: went from the decode error to "4 profile(s) parsed ok." The other 16 Bonfire endpoints are still unverified beyond this one. Build/clippy/test clean.
  - **Sidebar row Enter-key lockup — live-reported 2026-08-14 while testing, fixed.** "if you are singed in and want to change user and go to the user in the sidbar and press enter nothing happens and you cant move with keybord until you press escape, mouse works as expected." Root cause: `active_mode()` has no branch for `active-nav==7`, falling through to `AppMode::Dashboard`; `dispatch_dashboard`'s `Confirm`/`Right` handlers had a blanket `nav < 10` fallback that tried to enter a content section that doesn't exist for the Profile row, leaving `focused_section` stuck non-negative so every later key routed through content-nav arms with nothing real to navigate — Back's own `focused_section >= 0 → reset to -1` is what "until you press escape" fixed. Mouse worked because its `clicked` handler bypassed all of this. Fixed by adding an explicit `nav==7` branch to both handlers, calling `invoke_open_sidebar_profile_menu()` directly, mirroring the mouse path. See CLAUDE.md's Sidebar profile row section. Build/clippy/test clean; bug itself live-confirmed, fix not yet re-tested.
  - **Settings silently kept showing the OLD profile after every switch — live-reported 2026-08-14 with a screenshot, fixed, a genuinely severe finding.** "the settings etc do not seams to be diffferent for different profiles." Confirmed correct and much worse than it looked: `apply_settings_to_window` — the ONLY thing that ever pushes any profile-scoped setting into the UI — was called exactly once in the whole app, at startup, and never again on login or switch. Not just a display bug: `wire_mpv_timer`'s own skip-mode dispatch reads the same stale `AppState` mirror, so actual playback behavior kept using the original profile's settings too. Two more instances of the identical pattern found while fixing it: `FjordState.seerr_client` (the object that actually performs every Discover API call) was ALSO only ever built once at startup — meaning Discover could keep silently using the OLD profile's live Seerr session after a switch, a real cross-profile data-isolation gap, not just cosmetic; `spawn_seerr_settings_fetch`/`spawn_refresh_seerr_version` (region/language dropdowns, admin permissions, Seerr version) had the same startup-only gap a third time. Fixed by hoisting `Config`/`client` to the top of `finish_session_setup`, rebuilding `seerr_client` right alongside them, and calling `apply_settings_to_window` (bumped to `pub(crate)`) from both of its commit closures. See CLAUDE.md's new "Settings silently kept showing the OLD profile" section. Build/clippy/test clean; not live-tested.
  - **PIN-protected profiles could be silently bypassed via "Remember Last"/"Default Profile" — live-reported 2026-08-14 via a direct question ("what if the las user had a pin? will it just show the pin screen?"), fixed.** Traced rather than guessed: the honest answer was no — both launch policies skipped the picker AND any PIN check entirely once a valid stored token existed, completely defeating the point of a PIN on that profile. This is exactly what the original Bonfire design doc called for handling, just never actually wired up. Fixed by replacing `should_show_picker_at_startup`'s plain bool with a 3-way `StartupGate` enum (`AutoLogin`/`ShowPicker`/`ShowPickerPin(user_id)`) — a PIN-protected resolved target now opens the picker pre-focused on that profile with the PIN modal already open (new `open_profile_picker_with_pin`) instead of silently resuming. Honest limitation: `has_pin` is the last-known local flag, not a live LAN-bypass-aware check — Fjord has no client yet at this point in startup to ask Bonfire directly. See CLAUDE.md's new "PIN-protected profiles could be silently bypassed" section. Build/clippy/test clean; not live-tested.
  - **Profile picker: FOURTH layout pass, live-reported 2026-08-14 with a screenshot ("the fjord loggo culd move to the center of the upper part... and we dont need to write fjord"), fixed.** The 3rd attempt's centering for the tile block was confirmed correct by this screenshot, but left the logo sitting at the bottom edge of a large empty region above it rather than centered within that region. Dropped the "FJORD" wordmark (icon only) and made the icon a fully standalone sibling of the tile block, centered independently in `[0, main-block.y]` — no coordination needed between the two anymore. Confirmed already-correct by construction, not a bug: the logo already is randomly-chosen-once-per-session and shared with the sidebar via the same `AppState.app-logo-idx`. Build/clippy/test clean; not live-tested against this specific change.

  - **2-tier account/profile redesign — done, 2026-08-14, directly requested ("then cleanup what you fixed and fix the issues that is not fixed") after the sidebar-lockup and settings-scoping bugs were fixed.** Live-testing surfaced a real design gap, not just bugs: the picker put every profile (a Bonfire sub-profile AND a fully independent plain login on a different server) into one flat tile row, so a plain account with no PIN concept was always an available bypass around a sibling PIN-protected household. Resolved via 2 rounds of `AskUserQuestion` — full design + reasoning in CLAUDE.md's new "Account/profile 2-tier redesign" section. New `AccountPickerScreen` sits above the (now always account-scoped) `ProfilePickerScreen`, shown only with 2+ known accounts (`profile::group_into_accounts`, grouped by `master_user_id` for a Bonfire sub-profile / own `user_id` otherwise); `should_show_picker_at_startup` grew from a 3-way to a 5-way `StartupGate` enum, resolving the account tier first via a new `DeviceConfig.account_launch_policy`/`default_account_id` pair (mirroring the existing profile-tier one), then gating on a new `ProfileSettings.remember_login` (default true — the actual security fix: false forces a full password re-entry, never a silent resume, before the profile tier is ever even consulted) before finally resolving the profile tier scoped to just that one account. "+ Add Account" moved to the account tier + a new always-available Settings → Profiles → Add Account row. Login screen keyboard focus got a narrow fix (initial native focus grab) — explicitly not the full on-screen-keyboard retrofit, still unstarted Phase 3. Deliberately did NOT add an in-picker "Add Profile" tile — user: "mabey it is cleaner to dont have this in the profile picker and the 'master' shuld do it in fjord or in jellyfin," so profile creation stays exclusively in Settings → Manage Profiles. Build/clippy/test clean. **Not live-tested** — see the live-test checklist below for the specific new items.

  - **Live-test checklist — Phase 1 + Phase 2, everything below is "not live-tested" or "not yet re-confirmed live" as of 2026-08-14.** All of the fixes/features this covers are already implemented and build/clippy/test-clean; this is purely the "confirm it actually works on the real HTPC" pass, consolidated from the scattered per-fix notes above and below into one thing to work through.
    - **Picker & switching**
      - [ ] Startup picker shows correctly with 2+ known profiles — avatar color/initial, real display name (not a GUID), lock icon on any profile with a PIN
            still takes some time to "login" — investigated and fixed twice since this note (see the two "Login speed" bullets below); worth re-checking against a fresh timing log
      - [x] Sidebar Enter-key lockup — fixed, see the dedicated bullet above (was: "if you are singed in and want to change user and go to the user in the sidbar and press enter nothing happens...")
      - [ ] Selecting a non-PIN profile switches cleanly: no stale/outgoing content briefly visible, no crossfade
      - [ ] Selecting a PIN-protected profile opens the numeric keypad; a wrong PIN shows an error and lets you retry; the correct PIN switches
      - [x] "Signing in…" spinner appears immediately on select, no dead pause before it shows
      - [x] Repeat-pressing Enter/select while a switch is already in flight doesn't fire a second concurrent switch
      - [x] **2-tier account/profile redesign — done, 2026-08-14, see CLAUDE.md's dedicated section.** Was: "it shuld be 2 layers? like accaunt then profiles?" / "mabey add a setting to remember login or something." A new `AccountPickerScreen` sits above the (now always account-scoped) profile picker, shown only with 2+ known ACCOUNTS (a plain login, or a whole Bonfire household grouped under its master); a new `ProfileSettings.remember_login` (default true) forces a fresh password re-entry at startup, never a silent resume, for any account with it turned off — the actual fix for "no PIN on a plain account is a bypass around a sibling household's PIN." "+ Add Account" moved to the account tier (plus an always-available Settings → Profiles → Add Account row, since the account tile only shows once 2+ accounts exist). New Settings → Profiles → Account launch behavior / Default Account rows mirror the existing per-profile pair one tier up. **Not yet live-verified** — needs:
        - [ ] A genuinely multi-account install (2+ accounts, at least one `remember_login` off) shows the account picker at startup instead of silently resuming
        - [ ] Unchecking "Remember this login" on a fresh login forces a real password re-entry on the next launch (not a PIN, not a silent resume) — and, fixed 2026-08-15 per direct follow-up ("it shuld remember server and username right"), the server AND username are pre-filled too, only the password needs retyping
        - [ ] Account picker's "+ Add Account" and Settings' own "Add Account" row both return Cancel/Back to their own correct origin
        - [ ] A single-profile account's own tile switches directly with no intermediate profile-picker flash
        - [ ] Account launch behavior / Default Account behave as labeled after a real restart
      - [x] Login screen keyboard nav — narrow fix done, 2026-08-14: the server-address field now grabs native focus the instant the screen appears, so typing works immediately with no mouse click first. **Not** the full on-screen-keyboard/Up-Down-between-fields retrofit (still unstarted Phase 3) — was: "login screen dont have keybord nav."
      - [x] "Add Profile" in the picker — deliberately NOT built. Asked directly (`AskUserQuestion`); user: "mabey it is cleaner to dont have this in the profile picker and the 'master' shuld do it in fjord or in jellyfin" — profile creation stays exclusively in Settings → Profiles → Manage Profiles (Bonfire Phase 2), which already gates its own "+" tile behind Bonfire's 5-sub-profile cap.

      - [ ] "Launch in fullscreen" is respected when the picker shows at startup, not just on the plain auto-login path 
       yes how dose it work is it from the last use so if one profile dont have fullscreen will it the next time not be in fullscreen?
       Answer: it's a DEVICE setting (`DeviceConfig.launch_fullscreen`, not `ProfileSettings`) — it's the same regardless of which profile/account launches, not per-profile and not "remembers the last profile's own value." Switching profiles never changes it.
      - [x] Picker tiles sit at true vertical center (3rd layout attempt, screenshot-confirmed working) and the icon-only logo (no more "FJORD" wordmark, 4th attempt) reads as genuinely centered in the empty region above "Who's watching?", not crowding it
      - [ ] A wrong PIN or any other failed switch leaves the outgoing session fully intact and still usable — nothing torn down
      - [x] **"← Back to Accounts" keyboard navigability — fixed 2026-08-16.** Was: "from the profile picker i cant get to go back to accaunts with the keybord" / "the button shows but i cant navigate to it with keybord and press enter." Up from the tile row now focuses the button (only when it exists, 2+ accounts), Down returns to the tiles, Enter/Escape/Backspace all activate it. See CLAUDE.md's dedicated section. **Not live-tested.**
      - [ ] Up from the tile row reaches the Back button (2+ accounts known); Down returns to the tiles; Enter/Escape/Backspace all activate it; a mouse click on a tile right after doesn't leave stale focus behind
      - [x] **Quit button keyboard navigability — fixed 2026-08-16.** Was: "quit it not also reacheble by keybord navigation," same day/direct follow-up to the Back-button fix. Down from the tile row now focuses Quit on both the profile picker AND the account picker, Up returns to the tiles, Enter quits, Escape/Backspace un-focuses without quitting. See CLAUDE.md's dedicated section. **Not live-tested.**
      - [ ] Down from the tile row reaches Quit on both picker screens; Up returns; Enter quits; Escape/Backspace back out without quitting
    - **Sidebar row & quick-menu**
      - [ ] Sidebar shows the current profile's avatar + real name right after a normal launch, not just after a fresh login/switch
      - [x] Up/Down reaches the Profile row (nav=7) correctly both with and without Discover enabled
      - [x] **"Switch Profile" split into "Switch Profile" + "Switch Account" — fixed 2026-08-16.** Was: "if i in a profile and click switch profile i get to the accaunt picker... the profile switch shuld take you to the profile switcher for the accaunt you are on" + "if you are on an accaunt that dont have any profiles you shuld not see switch profile only switch accaunt." Quick-menu's row set is now: Switch Profile (only when the CURRENT account has 2+ profiles, always opens that account's own profile picker) / Switch Account (always shown, opens the account picker) / Manage Profiles (master only) / Profile Settings / Sign Out. `open_switch_entry_point`'s old "smart" routing deleted. See CLAUDE.md's dedicated section. **Not live-tested.**
      - [ ] "Switch Profile" only appears (and switches within the current account only) when that account genuinely has 2+ profiles; "Switch Account" is always reachable, even with just 1 known account, and opens the account picker with its "+ Add Account" tile
      - [ ] "Profile Settings" lands directly on Settings → Profiles with the right row focused
    - **Native profile management (Phase 2)**
      - [ ] Settings → Profiles → Manage Profiles is reachable, master accounts only
      - [ ] Create a new sub-profile end to end — name, avatar color, PIN, parental rating, enabled libraries, blocked/allowed tags, lockout minutes, LAN bypass, device whitelist, plus the master's own confirmation PIN
      - [ ] Edit an existing sub-profile — every field pre-fills correctly except parental rating (known, accepted gap — Bonfire's `/list` response doesn't return it)
      - [ ] Delete a sub-profile
    - **Settings / launch policy**
      - [ ] Launch behavior (Always Ask / Remember Last / Default Profile) actually behaves as labeled after a real restart
      what if the las user had a pin? will it just show the pin screen?
      - [ ] Default Profile's dropdown lists every known profile and updates immediately after Add Account with no restart needed
    - **Recovery / edge cases**
      - [ ] `config.json` no longer picks up a self-referencing master-profile entry after a normal session (the corruption bug — worth a quick grep of the real file)
      - [ ] If a profile's stored token has gone stale (401 on switch), Add Account is confirmed as the actual way back in

- **Wrong/missing Discover posters, live-reported 2026-08-12 — fixed, live verification.** "Gran Hermano... have the mentalis's poster image" / "Law & order missing poster on popular tv shows row but have poster when you go in to the detail" — a genuine index-misalignment bug in 4 places (`ensure_discover_landing` rows 0-4, `spawn_discover_search`, `spawn_discover_search_more`, "New in Theaters"): each built the card list and its poster-fetch jobs from two independently-filtered views of the same raw results, and the two filters disagreed on blocklisted items — one blocklisted item anywhere in a row silently shifted every poster after it to the wrong title for the rest of that row. Fixed at all 4 sites by building the card list and its poster jobs together in one pass instead of two that can drift apart. See CLAUDE.md's Discover search-grid flash section. Build/clippy/test clean; not live-tested.

- **Two keyboard-focus bugs, live-reported 2026-08-12 — fixed, live verification.** "the browse all breacks haver you passt it once" — `browse-header-focused` was only ever reset via explicit in-screen exit paths, never on a general sidebar-tab switch (mouse or keyboard), so a stale `true` from a previous visit silently swallowed arrow-key navigation into search-typing mode on the next visit; fixed by resetting it in `browse::clear_browse_results`, the one hook every sidebar switch already funnels through. "the higlight for a item stays when you move to search and filter and sort on libray screens and som other screens" — `LibraryGrid`'s card focus ring never excluded the grid's own other zones (search/sort/back/scrubber), unlike every other analogous screen in the app (Artist/Collection/Album/Discover all already do this correctly); `browse.slint`'s list had the identical gap. Both fixed with the same exclusion pattern the other screens already established. See CLAUDE.md's Keyboard navigation section. Build/clippy/test clean; not live-tested.

- **Browse All: a second, more severe bug behind the first — live-reported 2026-08-14 ("the browse all bug is still present look at the devs log"), fixed.** Confirmed via a real dev-machine log (matching current `HEAD`) that the actual DATA went permanently empty after the first departure from Browse All, not just keyboard dispatch: `clear_browse_results` unconditionally wiped `media-items` to empty on every sidebar switch away, but the `browse_populated` guard (a deliberate one-build-per-session performance fix) meant nothing ever rebuilt it again. Fixed by no longer touching the list in the common case (it's already valid and meant to persist) — only rebuilt synchronously when the user left mid-search, so the grid doesn't show a stale filtered subset once the query resets. See CLAUDE.md's Context menu section (the paragraph right before it). Build/clippy/test clean; not yet re-confirmed live.

- **Bonfire: Launch Fullscreen wasn't applied on the profile-picker path — live-reported 2026-08-14, fixed.** `Config.device.launch_fullscreen` was only read/applied inside the auto-login branch of startup, not the picker branch. Hoisted above the `if show_picker {...} else {...}` split so both paths get it. Build/clippy/test clean; not live-tested.

- **Bonfire: "i cant select a profile" — one real bug fixed, one real blocker identified but not client-fixable, 2026-08-14.** Found and fixed a genuine data-corruption bug (`sync_bonfire_subprofiles` had no guard against Bonfire's `/list` response including the calling master's own profile, silently overwriting the master's correct entry into a self-referencing `is_bonfire: true, master_user_id: <itself>` state) — fixed at the source plus a self-healing repair in `load_config` for already-corrupted installs. But a one-off diagnostic against the real server showed this wasn't the actual cause of the report: even a `GET /list` call with the master's stored token now returns a bare 401 — the saved token has itself gone invalid server-side, which no client-side fix can repair. The picker's "+ Add Account" tile (fresh username/password login) is the real way out of this specific stuck state. See CLAUDE.md's Bonfire picker section. Build/clippy/test clean; not yet re-confirmed live.

- **Login speed: found and fixed the real cause via the new timing instrumentation — direct follow-up questions same day, fixed.** The dev-machine log immediately paid off: `not_watched_tv took 4.103s` against every other one of 17 concurrent login requests finishing in 1.0-2.7s (accounting for ~97% of the whole login's 4.23s total) — a Jellyfin-side `SortBy=Random` + recursive per-episode unplayed cost specific to the Series query (the Movie equivalent finished normally), not a client bug. Fixed by deferring both Not Watched rows (neither shown on the Home dashboard anyway) out of the login-blocking join entirely — `fetch_home_data` gained an `include_not_watched` param, `false` only for login, `true` unchanged for the other 4 call sites (WS refresh, playback-stop/end refresh, auto-login refresh) after auditing all 5 to avoid silently wiping the rows on a later refresh. See CLAUDE.md's "Login speed" section. Build/clippy/test clean; not yet re-confirmed live.

- **Profile picker: second layout pass against a real screenshot — same day, fixed.** The first 35%-anchor fix still left a large visible gap (confirmed via a screenshot). Re-anchored the block off the tile row's own vertical center instead of the whole block's top (`parent.height/2 - header.preferred-height - spacing - tile-row.preferred-height/2`, a safe sibling-preferred-height read, not self/ancestor). Icon grown 32px→64px per direct follow-up ("make it bigger"). Build/clippy/test clean; not live-tested against this specific change.

- **Sidebar profile row + quick-menu — live-reported 2026-08-14, scoped via `AskUserQuestion` (user picked a hybrid: quick-menu with an item linking to Settings → Profiles), implemented.** New sidebar entry (`active-nav==7`, before Settings) shows the current profile's avatar+name (`AppState.current-profile-tile`, pushed on every session start/switch). Pressing it opens a new `SidebarProfileMenu` quick-menu (Switch Profile [2+ profiles only] / Manage Profiles [master only] / Profile Settings / Sign Out). "Switch Profile" is the one genuinely new capability — `open_profile_picker` gained a `cancelable` flag so the picker can now be voluntarily reopened from a live session (previously startup-only, no Escape/Back handling existed for it). The underlying switch mechanism needed no changes — it already resolves a token before tearing down the old session. `reset_session_state` updated to clear the new menu/tile state. See CLAUDE.md's new "Sidebar profile row + quick-menu" section. Build/clippy/test clean; not live-tested.

- **Profile picker: layout polish — live-reported 2026-08-14, fixed.** Content block was dead-center vertically ("why are the profiles o low"), re-anchored to 35% of leftover space instead of 50%. Plain "FJORD" text replaced with the real app icon (same `app-logo-idx` ternary the sidebar uses) + wordmark, mirroring the sidebar's own pairing. Build/clippy/test clean; not live-tested.

- **Login/session-setup timing instrumentation — user question 2026-08-14 ("what makes login slow", "it takes longer to login to a profile than it took to open fjord before").** Confirmed the whole fetch pipeline (finish_session_setup's outer join + fetch_home_data's inner 14-way join) was already fully parallel — nothing sequential to fix. Added `main.rs::timed()` wrapping every branch of both joins so the next real login/switch shows exactly which single request dominates. Also reasoned through (not yet confirmed with real numbers) why a Bonfire switch is inherently slower than the old auto-login: it needs one extra, unavoidable, sequential round trip first (minting a fresh sub-profile token) before the same pipeline can even start. See CLAUDE.md. Build/clippy/test clean; pure instrumentation, not itself a speed fix.

- **Login: server address no longer requires an explicit http://\/https:// prefix — live-reported 2026-08-14, fixed.** `do_login` previously required an explicit scheme or failed outright with a raw URL-parse error. New `candidate_server_urls`/`authenticate_with_fallback`: an explicit scheme (any case) is used as-is and never retried; a bare host/IP tries HTTPS first, falling back to HTTP only on a genuine connectivity-class failure (never on a real server error like a wrong password, which would never be fixed by retrying under a different scheme). 6 unit tests. See CLAUDE.md's new "Login: server address scheme auto-detection" section. Build/clippy/test clean; not live-tested.

- **Bonfire picker: no loading feedback during a switch — live-reported 2026-08-14, fixed.** The dev-machine log confirmed a genuine ~12.5s gap with zero output between selecting a profile and landing in the app. New `profile-picker-loading` flag drives a spinner + "Signing in…" overlay, and guards against a repeat Enter press firing a second concurrent switch (a real risk, confirmed from the earlier stuck-token incident's own log). Also fixed 3 of `switch_to_profile`'s 4 failure paths that never cleared any UI state at all — would have left the picker permanently stuck looking busy on any of those specific failures. Timing logs added throughout (previously silent on success). See CLAUDE.md's Bonfire picker section. Build/clippy/test clean; not live-tested.

- **Missing Seasons row: upcoming/unaired season showed "0 episodes" — live-reported 2026-08-12, fixed.** Now shows "Upcoming · {date}" (or plain "Upcoming" with no known air date) when a season has 0 episodes and its `air_date` is absent or not yet in the past, using data already fetched but never used for this. Build/clippy/test clean; not live-tested.

- **HTML markup showing raw in person bios / Overview text — live-reported 2026-08-12, fixed.** Some (anime-focused) Jellyfin metadata providers return `Overview`/bio fields as real HTML; Fjord rendered it verbatim everywhere. New dependency-free `main.rs::strip_html_to_text` (converts `<br>`/block-closing tags to newlines, `<li>` to a bullet, drops other tags keeping inner text, decodes common entities, collapses blank-line runs), applied at all 11 real Overview/bio call sites across `album.rs`/`artist.rs`/`collection.rs`/`detail.rs`/`season.rs`/`series.rs`/`person.rs`. 4 unit tests added. Build/clippy/test clean; not live-tested against the real reported bio.

- **Focus-border color inconsistency — live-reported 2026-08-12, scoped via `AskUserQuestion`, implemented.** "the hilight for blue buttons... is a bitt so suttle and it seams to be different colors on different buttuns object." Confirmed real: `FjordButton`/`IconCircleButton` already used a white focus/press border; ~43 other press-pulse sites (`NavItem`, `SettingsRow`, `BrowseItem`, `MediaCard`, `CastRow`, `MiniPlayerBar`/`MusicPlayerBar`, `QueuePanel`) used blue instead, several sitting directly on an already-blue-tinted background. User picked "white border everywhere." New `Theme.focus-border` token; ~58 sites across 15 files unified onto it (44 simple + 14 embedded-in-ternary, found via a second pass). Deliberately left alone: `home.slint`'s 4 sort/filter pills' active-pill white-border (a separate, intentional two-color design, only the non-active-focused branch changed), `ToggleSwitch`/`profile_edit.slint`'s checkbox (selected-state indicators, not focus rings). Build/clippy/test clean; not live-tested.

- **Person detail from a Discover-context cast member — live-reported 2026-08-12 ("in the descover detail you cant enter person detail"), scoped via `AskUserQuestion` (twice — once for the whole approach, once resolving the user's own clarifying counter-question), implemented.** `RequestDetailScreen`'s `CastRow` previously had `item-selected` deliberately unbound. User picked "Try local match first, TMDB fallback" — new `resolve_local_person` (name search + `ProviderIds` cross-check, single-candidate fallback) opens the real native person screen when a local Jellyfin Person match is found, else a new TMDB-only fallback screen (bio via a new `SeerrClient::get_person`, filmography via the same pipeline "Other Work" already uses) reusing the same `AppState.person-*` properties. Caught and fixed a real pre-existing keyboard dead-end along the way: Down from the person screen's header never fell through to Other Work when Filmography was empty, which the new TMDB-only screen would always hit. Build/clippy/test clean; not live-tested — the new `search_persons_by_name` Jellyfin endpoint+param combination is the single least-certain piece, never exercised against a real server before.

- **Pausing didn't pause the Up Next / ask-timed skip countdowns — live-reported 2026-08-14 ("if you pause the video after the up next timer have started it dont paus the timer so when it runs out it starts the next video, this is probobly the same for all the timers"), fixed.** Both were genuine, separate wall-clock-based countdowns with no pause awareness at all: the Up Next banner's spawned countdown task slept a flat 1s/iteration regardless of mpv's pause state; the ask-timed skip-segment overlay computed elapsed time from a fixed `Instant` inside the 16ms timer, which keeps ticking through a pause too. Both rewritten to freeze while paused (the banner task now polls `is_paused()` every 250ms and only consumes elapsed time when unpaused; the skip-segment overlay folds the paused duration forward into its anchor on resume) and resume counting down from where they left off, not from wherever wall-clock time had drifted to. See CLAUDE.md's Episode auto-advance section. Build/clippy/test clean; not live-tested.

- **Profile name showed the raw user_id GUID on an old/migrated login — live-reported 2026-08-14, fixed.** Two real gaps: `do_login` never actually backfilled `display_name` from the login response despite a doc comment claiming it did, and `push_current_profile_tile` (the sidebar avatar row) was only ever called from a fresh login/switch, never from ordinary auto-login — so a normal relaunch never even tried to show a real name. New `JellyfinClient::get_user_info()` (`GET /Users/{id}`) backfills a blank `display_name` once, best-effort, on auto-login; `do_login` backfills it too, in all 3 branches; `spawn_auto_login` now pushes the sidebar tile on every launch, not just a fresh login. See CLAUDE.md's new "Profile display name" section. Build/clippy/test clean; not live-tested.

- **Profile picker: THIRD layout pass — live-reported 2026-08-14 ("the profile screen is not right the profiles is not in the middle and the fjord loggo is to small"), real root cause found and fixed.** The second pass's centering formula was algebraically correct but the outer `VerticalLayout` was a Layout element placed directly inside a plain `Rectangle` with no explicit `height` — the same documented shape `ConfirmDialog`/`RequestOptionsOverlay` already broke once for an identical reason. Fixed with the same remedy (`height: self.preferred-height;`). Icon grown again, 64px→120px, a genuine hero size for the one screen that's entirely about the logo + a profile choice. Build/clippy/test clean; not live-tested — third attempt at this specific screen, worth a real screenshot check this time.

- **Season-tab context menu — live-reported 2026-08-12, scoped via `AskUserQuestion`, implemented.** Right-click / `C` on a season tab in the Series screen now opens the generic Jellyfin context menu, narrowed to Mark Watched/Unwatched, Favourite, and View Details (user picked "Full menu (Recommended)" over a bespoke minimal one). `SeasonEntry` gained `played`/`is-favorite` (threaded from data `get_seasons()` already fetched, no new network call); View Details special-cases `item-type == "Season"` to open `season.rs`'s own detail screen instead of the generic DetailPage, which has no season concept; Play from Start/Play Next/Add to Queue are excluded for a Season (not playable itself); `update_card_in_all_models` gained a `series-seasons` patch block so reopening the menu on the same tab shows fresh state. Build/clippy/test clean; not live-tested — particularly whether Jellyfin's season-level mark-played cascades a `UserDataChanged` per episode so the visible episode list self-corrects via the existing WS path.

- **Playback resilience: network outages — live verification.** New 2026-08-09 feature, root-caused from a real HTPC `fjord.log` (a genuine ~30s network outage caused a false EOF that auto-advanced to the wrong episode). Fixed: honest two-row cache setting (seconds + MB cap, replacing a single "Cache (MB)" row that never actually raised the real buffer — it only ever adjusted mpv's `cache-secs`, not `demuxer-max-bytes`); explicit ffmpeg HTTP reconnect tuning; the stall-watchdog generalized from a fixed start-position check to a rolling "no progress in 5s" check; recovery changed from a same-connection seek to a full stream reload, capped at 2 attempts; a duration guard that never treats a premature EOF as a natural end (no mark-played, no advance) regardless of cause; a new "Reconnecting…" overlay distinct from the buffering spinner. **Real regression found live the next day, 2026-08-11** ("i got 'connection issues'... it was realy prominent when i seeked back"): the "genuine forward progress" check was forward-only, so any backward seek stranded the stall baseline at the pre-seek position — completely normal post-seek playback then looked identical to a stall within 5 seconds, confirmed directly from the log (`seek_to → 1228.4s` then 4.65s later `"stalled: 5.0s with no progress at 1232.57s"`, position had genuinely advanced ~4.2s in that window). Compounded by the per-item reload-attempt cap being effectively permanent once exhausted — two false positives early in a video disabled real recovery for the rest of it. Fixed: a direction-agnostic single-tick jump detector (≥2.0s between consecutive 16ms ticks) resets the stall baseline on any seek/chapter-jump/gapless-transition, and the reload-attempt cap now forgives itself after 120s of sustained healthy playback on the same item. **A second, same-shaped false positive, live-reported the next HTPC test**: pausing for an extended period (~3 minutes) and resuming immediately triggered the identical false-stall/reload path — the checkpoint's clock kept aging for the whole pause since nothing refreshed it while paused, so the first tick after resuming read the full pause duration as "stalled for." Fixed the same way as the seek case: `is_paused || buffering` now also refreshes the checkpoint every tick. See CLAUDE.md's "Playback resilience: network outages" section. Build/clippy/test clean; not yet watched live against a real outage, the seek-jump fix, or an extended pause/resume.

- **Skip-segment fade-to-black — live verification.** New 2026-08-11 feature, direct request ("make intro skip etc more gradual, it feels instant and jarring"). All 3 skip paths (auto always-skip, ask-timed countdown expiry, manual "Skip" confirm) now arm a delayed seek instead of jumping instantly — the screen dips to black (`AppState.skip-fade-active`, mirrored into a full-screen `Rectangle` opacity in `player.slint`), the real `seek_to()` fires while hidden, then it fades back in. Scoped to same-video segment skips only — the credits→next-episode transition (a heavier `start_playback` reload, not a same-stream seek) was deliberately left untouched. **Same-day follow-up, direct feedback ("mabey the 200ms is to short? mabey make it a setting so i can test, and... the audio as i use passthrou it cant be faded right?")**: duration is now a Settings row (`Config.device.skip_fade_ms`, Off/100/150/200/300/400/500/750/1000ms, default 200, Settings → Player → Seeking), read live by both the Rust wait and the Slint animation so they can't drift apart; audio now fades alongside the video — a real volume ramp for PCM (`Player::set_volume`, new absolute setter), a mute held for the whole transition window for SPDIF passthrough (`Player::set_mute`, since a raw bitstream can't be volume-ramped — the same reason Volume Up/Down already skip themselves there). **Second same-day follow-up** ("add a setting for mute during fade for audio passthrou so i only can turn off that and not the video fade"): new Settings → Audio → Passthrough → "Mute during skip fade" toggle (`Config.device.skip_fade_mute_passthrough`, default on), gating only the passthrough mute — the video fade and the PCM ramp stay unconditional regardless. See CLAUDE.md's "Intro Skipper plugin" → Skip fade section. Build/clippy/test clean; not live-tested — the passthrough mute specifically is untested against real AVR relock behavior, flagged to the user as the one part of this genuinely uncertain rather than just unverified.

- **RequestDetailScreen/RequestOptionsOverlay — 5 fixes, live verification.** New 2026-08-12, from a real Discover live-test pass: (1) the screen no longer shows a blank page while its Seerr fetch is in flight — now uses the shared `app-content-loading` overlay, same pattern as every native detail screen; (2) the Request Options modal's Profile/Tag chips now show a real accent-colored border on keyboard focus (previously only a 1px→2px width bump on the same gray, genuinely hard to see, inconsistent with the rest of the app); (3) the Cancel/Request confirm row rendering outside the dialog box is fixed — `dialog-content` now binds `height: self.preferred-height`, the same proven fix `ConfirmDialog` already shipped for the identical bug shape (a first attempt reading a specific child's real geometry hit a genuine Slint binding loop instead, see CLAUDE.md's Seerr integration section for the full trace); (4) Quality/Tags/Profile choices are now remembered permanently per media type (movie/tv), mirroring Seerr's own web UI behavior — resolved via `AskUserQuestion` since same-item memory already worked, so "remember" had to mean carrying forward to the *next* item; (5) a brand-new (not edited) request now also adds the item to the Watchlist, unless it's already there. Build/clippy/test clean throughout; not live-tested — see CLAUDE.md's Seerr integration section for the specific things worth checking first.

All 4 items below had a rigorous code-review pass (2026-07-31, 4 parallel agents) — 7 real bugs found and fixed (see CHANGELOG.md), everything else listed here checked out clean in the code but still needs an actual live pass.

- **Phase 183 (Deep Seerr integration) — live verification.** Confirmed via a real 2026-08-01 dev-machine log: Calendar ongoing-series, Missing Seasons keyboard nav, and **Collection Missing Items** (watched it live — Avengers/Bourne collections both initially failed to resolve on a stale cache-hit, then correctly resolved via the multi-member loop on the next open once the background revalidate had refreshed `boxset_items_cache`'s ProviderIds — the multi-member fix and the revalidate-on-open system working together as designed, not just individually); **Detail's Recommended row** also confirmed with real data (13 not-owned recommendations for a real movie). Since that log: fixed the "next open" part above — a first-attempt failure now awaits the parallel revalidate and retries once in the same open, no reopen needed (needs its own live check). Still open: (1) Series' own `spawn_recommended` (Detail's twin implementation — not exercised in the same log, only Detail's was); (2) Series Missing Seasons — per-season pill correctness, Request Options preselect on click; (3) Person Other Work — TMDB person-id resolution accuracy for real actors; (4) a Seerr disconnect/reconnect or sign-out clears `person_tmdb_id_cache`/`person_other_work_cache`.

- **Discover search-grid flash — live verification.** Two fixes (2026-08-02): fresh-query commits now carry posters forward by id instead of blanking overlapping results, and page 2/3/4 auto-load now appends to the live model instead of swapping it (true incremental append). Build/clippy/test clean; not yet watched live.

- **Coming Up dashboard rows + watchlist watched-state sync — live verification.** All 2026-08-02, user-requested. Coming Up now shows on Home (mixed)/TV (series-only)/Movies (movies-only) dashboards, same split as the Watchlist rows; a `section-y` scroll-position gap for the Watchlist row's own height (never needed until Coming Up landed right after it) was caught and fixed in the same pass. Watched items are now kept in sync with watchlist membership both directions: marking something watched removes it from the watchlist (hooked into WS `UserDataChanged`, covers Mark Played/credits auto-mark/other clients alike) — EXCEPT a still-`Continuing` series, which stays on the watchlist while fully caught up and is only removed once Jellyfin later reports it as no longer Continuing (`maybe_spawn_delta_refresh`'s own check) — and re-adding an already-watched item to the watchlist marks it unwatched instead (a real `mark_unplayed` call, not just a local flag). Build/clippy/test clean; not yet watched live — especially whether Jellyfin reports a Series as played (and thus triggers removal) only once every episode is watched, or sooner, and the Continuing→Ended deferred-removal path, which needs a real status change server-side to exercise.

- **Seerr Blocklist support — live verification.** New 2026-08-06 feature, planned in full via `/plan`: add/remove from Discover context menu + RequestDetailScreen, bulk-blocklist a collection (with confirm dialog), and a new Manage Blocklist screen (Settings → Integrations). No new persistent id-set was needed — Blocklisted rides on the existing `CardItem.availability` field. Two same-day fixes: (1) blocklisting now actually removes the item from Discover (filtered at the fetch source + removed live from whatever's on screen) instead of just marking it — the original "patch in place" design was wrong, per direct live feedback; (2) a watchlisted-and-blocklisted item was still resurfacing on the Movies/TV dashboard's Watchlist row and Coming Up — those 5 dashboard-split models weren't covered by fix (1), and the Watchlist/Coming-Up row builders never filtered Blocklisted status at all, both now fixed at the root. Build/clippy/test clean; not yet watched live at all — particularly the eligibility gate against real titles across all 3 surfaces, the Manage Blocklist screen's pagination/remove, blocklisted items genuinely staying gone from Discover AND the dashboard Watchlist/Coming-Up rows after both fixes, and `⛔` glyph rendering on the HTPC.

- **Phase 182 (U+FE0E removal) — live HTPC verification.** The removal itself is confirmed clean (re-verified via direct byte-sequence grep). Most likely actual fix for the originally-reported tofu square: the pause/play flash icon's missing font pin, now fixed. Check that specific icon on the HTPC first.

- **Video-only-audio black screen on first play — root cause confirmed and fixed, live verification pending.** Phase 181's captured mpv internal log finally caught it recurring: a genuine race between mpv's `vo=libmpv` VO-init (which can fire before Slint's `BeforeRendering` notifier ever creates Fjord's render context) and that render-context creation — confirmed directly from mpv's own error text ("No render context set."), which is never retried once it fails. Fixed at the root, 2026-08-11: `Player::new()` no longer loads anything, only builds the mpv core; a new `Player::load(url)` is called exactly once, from `wire_rendering_notifier`'s `BeforeRendering` handler, immediately after the render context is created — same GL thread, same tick, so the ordering is now a same-thread guarantee instead of a race. See CLAUDE.md's "NVIDIA HTPC: video-only-audio black screen" section. Build/clippy/test clean; not live-tested — needs a real HTPC pass confirming a fresh launch's first playback shows video immediately with no restart needed.

- **Phase 166+167 (Discover Filters + keyboard nav) — live verification.** Stale filter-bar-focus on card clicks and Type=All pagination sort-order are fixed. Still open: Genre/Provider popups populate correctly from a real instance; multi-select genuinely broadens results (OR not AND); Sort/Rating/Year narrow/reorder correctly; popup overlay fixed pixel dimensions fit real content; the remaining Groups 1/4/5 of the keyboard-nav pass (mouse-click focus sync on Storyline/Request buttons, filter-popup staying closed across tab switches, context-menu request-state completeness).

- **Phase 174-177 (Watchlist + Calendar + dashboard rows) — live verification.** Calendar ongoing-series, dashboard Watchlist rows, star-badge resync, and the RequestDetailScreen button-row overflow are confirmed/fixed. Still open: the watchlist-toggle toast mystery (confirmed NOT a silent-return bug — every path through `discover_toggle_watchlist` calls `show_toast`, root cause still unknown); calendar month-grid rendering at real font metrics; day-popup keyboard nav; Home/Movies/TV dashboard Watchlist row keyboard reachability (Up/Down/Left/Right hitting the new 6th row correctly); star badge doesn't visually collide with a progress bar; a full HTPC pass — everything in this whole feature has only ever been tested on the dev machine.

---
## Issues
(none open)


## future additons

(none open — the two items previously listed here, watchlist/calendar and search filters, both shipped in Phases 166/168 and are documented in CLAUDE.md's Seerr integration section)


## Architecture notes

### mpv render API

mpv uses `vo=libmpv`. Two FBOs alternate each frame:

```
BeforeRendering:
  mpv_render_context_render(fbos[back])
  expose textures[back] as BorrowedOpenGLTexture → Slint draws it
  back = 1 - back

AfterRendering:
  if did_render: mpv_render_context_report_swap()   ← vsync feedback
```

`MpvRenderCtx` must be dropped before `Player`. Enforced in `VideoState` and the rendering teardown path.

### Disk cache

```
~/.cache/fjord/home.json         home row data    always refresh in background
~/.cache/fjord/movies.json       full movie list  refresh once per session on grid open
~/.cache/fjord/series.json       full series list refresh once per session on grid open
~/.cache/fjord/posters/<id>      poster bytes     permanent (never expire)
~/.cache/fjord/backdrops/<id>    backdrop bytes   permanent (never expire)
```

Warm start: all caches loaded synchronously before `window.run()` — window opens fully populated on the first frame.

### Poster loading pipeline

```
Tokio worker:
  fetch bytes (disk cache or HTTP, 8-connection semaphore)
  decode JPEG → SharedPixelBuffer<Rgba8Pixel>   ← Send
invoke_from_event_loop:
  Image::from_rgba8(buffer)                     ← !Send, must be on UI thread
  push HomeItem with poster into VecModel
```

### Thread model

```
main thread       Slint event loop + GL rendering notifier
tokio runtime     API calls, poster fetch/decode, home data refresh
16 ms timer       mpv event poll, position update, skip-segment (Intro/Recap/Preview/Commercial), credits auto-advance check, controls idle, progress report
```

---

## Deferred / future

- **Theming / layout customisation**: accent colour palette, dashboard row visibility toggles, row reordering — needs the full layout system in place first before it makes sense to build.
- **Vulkan rendering path** — second render backend alongside the current OpenGL path. Requires: Slint WGPU backend, `MpvRenderCtx` initialized with `MPV_RENDER_API_TYPE_VULKAN`, Vulkan FBO management replacing the current `gl::*` code. Enables true zero-copy decode on AMD (`hwdec=vulkan`, no CPU roundtrip). Legacy NVIDIA hardware needs OpenGL; selection persists in Config as `gpu_renderer: "opengl" | "vulkan"` and takes effect on next restart.
- Gamepad / remote control — d-pad maps to arrow keys; formal evdev/udev support deferred
- **Dashboard row reorder** — drag-to-reorder; part of the future theming/layout customisation update
- **Trickplay** — seek bar scrub thumbnail popup. Requires: fetch Jellyfin trickplay manifest (`GET /Videos/{id}/Trickplay/{width}/tiles`), parse tile sheet dimensions (tile size, columns, rows, interval), cache tile images per video, render a thumbnail above the seek bar while scrubbing (position computed from `seek-hover-pos`). Deferred because it's a separate subsystem from chapter nav and the API surface needs more investigation.
- ~~**Multi-account / multi-server support**~~ — done. Fully subsumed by the Bonfire/JellyProfiles `Config` restructuring (`DeviceConfig`/`Vec<ProfileSettings>`) and `ProfilePickerScreen`; each profile carries its own `server_url`, so this is genuine multi-server too, not just multi-account on one server. See CLAUDE.md's Bonfire section.
- **Display mode auto-sync (resolution/refresh-rate/HDR to match video content), KDE-only for now** — live-reported 2026-08-14, a real HTPC hitch traced to the user's own separate `media_display_sync` script (polls Jellyfin's API independently, hit a 5s timeout, briefly reverted the display mode then switched back ~10s later — a real, visible HDMI renegotiation, but not a Fjord bug; Fjord's own stream never noticed). Discussed as a genuine future candidate: building this into Fjord directly would be strictly better than the external-poller approach — Fjord already has the real container framerate/resolution/HDR metadata synchronously from mpv the instant a file loads (no separate network poll to time out), and a mode switch could ride the existing skip-fade-to-black mechanism instead of happening as a raw visible renegotiation mid-frame. The real cost is that it's Wayland-compositor-specific, not a Jellyfin/mpv API surface — the user's HTPC (and only currently-testable machine) runs KDE Plasma, so a first cut would go through KDE's own KWin/kscreen D-Bus interface and be KDE-exclusive until/unless a second compositor is in scope to test against; no wlr-output-management (Sway/Hyprland-style) support without a way to verify it. Not scoped further yet — worth a proper `/plan` pass (mirroring how Watch Trailer/Bonfire were planned) once actually picked up, and reading the user's own `media_display_sync` script first (repo visibility TBD) to see exactly which mechanism it already leans on.
  - **Sharper motivating hypothesis surfaced 2026-08-15, during the HDR passthrough investigation above**: the script changes resolution/Hz *after* the video has already started playing — a live connector mode-set happening concurrently with (or right after) mpv's own render-context setup and `target-colorspace-hint` negotiation is a plausible, sensible interference mechanism (a DRM mode-set is a heavyweight, connector-level operation; racing it against mpv's own HDR-hint negotiation could easily reset or drop whatever was being negotiated) — genuinely the same *shape* of race this project already found and fixed once for a completely different reason (the VO-init-vs-render-context-creation race, see "NVIDIA HTPC: video-only-audio black screen" above). User's own planned test, not yet run: same file/script, once with resolution/Hz already correctly set *before* Fjord even starts (no mid-playback mode-set at all) vs. the current after-the-fact behavior — if the pre-set case works and the mid-playback one doesn't, that's a clean, direct confirmation. User's own words: "if the script interfere then i will push up the integration into fjord" — i.e., a confirmed race here is the trigger condition for actually picking this item up, not a hypothetical someday. If/when that happens, the fix this points toward specifically is doing the mode-set *before* starting mpv's own playback pipeline for that item (matching the already-established "resolve first, don't create the race in the first place" discipline this project keeps landing on elsewhere), not just retrying/reordering after the fact.
- **Poster/card scaling setting** — from Phase 117: with titles/episode names now always shown in full (no truncation), a very long title on the smallest card breakpoint (115px wide) shrinks that card's poster noticeably to make room. User's proposed fix: a settings toggle to use larger posters in the library/dashboard (more text budget per card before this becomes visible) rather than reintroducing a truncation cap. Would likely hang off the existing `dash-card-w`/`dash-card-h` breakpoint functions in `main.slint`, e.g. as a user-chosen size multiplier or an extra breakpoint tier. Alternative approach floated in the same conversation, not mutually exclusive: render the poster image full-bleed behind the title/subtitle text with a dark semi-transparent scrim, instead of a separate text block below the poster — sidesteps the whole shrink-to-fit problem structurally rather than giving the text more room. Neither implemented yet; revisit when picked up.
- **Profile picker: 2-tier account→profile redesign, plus a real login-screen keyboard-nav gap — live testing notes left directly in the Phase 1/2 checklist, 2026-08-14, not yet scoped or planned.** Substantive design feedback, several interlocking points, worth a proper `/plan` pass (not something to improvise inline) before any code:
  - **Core concern**: the picker today treats every `Config.profiles` entry as one flat, undifferentiated list — a genuine Bonfire sub-profile of the current household (which a PIN is meant to gate) sits in the exact same tile row as a completely independent Add-Account login to a DIFFERENT server that has no Bonfire at all and thus can never have a PIN. Since the picker never distinguishes "this household" from "any account I've ever added," a PIN-protected profile's whole point is underminable — anyone can just pick the other, unprotected account instead and get straight in.
  - **User's proposed fix**: two layers — pick an ACCOUNT/server first, then (only if that account has Bonfire) pick a PROFILE within it. Mirrors how a real multi-server + multi-profile-per-server model should nest, rather than flattening both concepts into one grid.
  - **Related, smaller points from the same feedback, all bundled into the same redesign rather than separate items**: a "remember login" setting so the user can choose whether resuming a plain (non-Bonfire) account still requires re-entering its password each time; the picker's "+" tile is mislabeled — "Add Profile" (create a new Bonfire sub-profile under the CURRENT household, routing to `ManageProfilesScreen`'s existing create flow) is conceptually different from "Add Account" (a genuinely new Jellyfin login, possibly a different server) and today's single tile conflates them; whichever tile creates a new Bonfire sub-profile should be gated on Bonfire's own max-profiles-per-master cap (5), matching what `ManageProfilesScreen`'s own "+" tile already does — the startup/sidebar picker's tile has no such gating today since it doesn't go through Bonfire's create-profile flow at all.
  - **Separate, concrete bug also reported in the same batch, likely worth fixing regardless of how the redesign above lands**: "login screen dont have keybord nav" — confirmed real by reading `keys.rs`: `show-login` returns `false` for everything except Ctrl+Q, relying entirely on Slint's native `LineEdit` focus/Tab handling with no Fjord-authored keyboard support at all. `pass-input`'s `accepted =>` callback does exist (Enter-to-submit works once a field has focus), but nothing grabs initial keyboard focus when the screen opens, and Tab (not Up/Down) is the only way to move between fields — a real gap for a remote/D-pad with no Tab key. This is the same class of gap the original Bonfire plan's own Phase 3 ("full on-screen keyboard retrofit... swap Login... over to open it as an overlay") already anticipated and deferred — worth deciding whether to pull a narrow slice of that forward (at minimum: grab initial focus on screen show) now, or fold it into the bigger picker redesign since the entry points may change shape together.
  - Not implemented, not yet scoped in detail — revisit as its own planning pass.
