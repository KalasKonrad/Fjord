// ── fjord-app · login.slint ──────────────────────────────────────────────────
//   LoginScreen  server URL + username + password fields; submits via AppState.do-login
//                (4th bool arg is login-append-mode, see app_state.slint); small Quit
//                button bottom-right corner (Ctrl+Q also works, keys.rs); a floating
//                "← Back to Profiles" corner button (Bonfire Phase 1, 2026-08-09) shows
//                only in append mode (opened from ProfilePickerScreen's "+ Add Account"
//                tile) — cancel-add-account() closes LoginScreen and reopens the picker
//                without touching any already-signed-in profile
// ─────────────────────────────────────────────────────────────────────────────
import { Theme } from "theme.slint";
import { AppState } from "app_state.slint";
import { FjordButton } from "widgets.slint";
import { LineEdit } from "std-widgets.slint";

export component LoginScreen inherits Rectangle {
    background: Theme.bg;

    card := Rectangle {
        width:         min(420px, parent.width - 48px);
        height:        inner.preferred-height;
        x:             (parent.width  - self.width)  / 2;
        y:             (parent.height - self.height) / 2 - 24px;
        background:    Theme.surface;
        border-radius: Theme.radius-lg;
        drop-shadow-blur:  28px;
        drop-shadow-color: #00000070;

        inner := VerticalLayout {
            padding: Theme.sp-xl;
            spacing: Theme.sp;

            VerticalLayout {
                spacing: 4px;
                Text {
                    text:                 "FJORD";
                    color:                Theme.accent;
                    font-size:            38px;
                    font-weight:          700;
                    horizontal-alignment: center;
                }
                Text {
                    text:                 "Jellyfin media player";
                    color:                Theme.text-muted;
                    font-size:            13px;
                    horizontal-alignment: center;
                }
            }

            Rectangle { height: 6px; }

            server-input := LineEdit {
                // A bare host/IP (no scheme) works now too — do_login tries
                // https then http automatically, live-reported 2026-08-14.
                // Still shown with a scheme in the example since a raw IP:port
                // is the one case (a local server with no real TLS cert) where
                // typing http:// explicitly saves the wasted first attempt.
                placeholder-text: "Server (e.g. jellyfin.example.com or 192.168.1.10:8096)";
                height: 40px;
            }
            user-input := LineEdit {
                placeholder-text: "Username";
                height: 40px;
            }
            pass-input := LineEdit {
                placeholder-text: "Password";
                input-type:       password;
                height:           40px;
                accepted => {
                    AppState.do-login(server-input.text, user-input.text, pass-input.text, AppState.login-append-mode);
                }
            }

            FjordButton {
                text: "Connect";
                clicked => {
                    AppState.do-login(server-input.text, user-input.text, pass-input.text, AppState.login-append-mode);
                }
            }

            if AppState.status != "": Text {
                text:                 AppState.status;
                color:                Theme.text-muted;
                font-size:            13px;
                horizontal-alignment: center;
                wrap:                 word-wrap;
            }
        }
    }

    FjordButton {
        text:   "Quit";
        width:  90px;
        x:      parent.width  - self.width  - 16px;
        y:      parent.height - self.height - 16px;
        clicked => { AppState.quit(); }
    }

    if AppState.login-append-mode: FjordButton {
        text:   "← Back to Profiles";
        width:  170px;
        x:      16px;
        y:      16px;
        clicked => { AppState.cancel-add-account(); }
    }
}
